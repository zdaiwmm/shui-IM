# 梗图面板与本机收藏

本期用户确认：全屏液态玻璃梗图弹窗；默认网络标签、直接联网、热门及分类；点图发送、长按预览与收藏；收藏只在本机，
删除原聊天消息后仍保留独立收藏副本。没有多设备同步和恢复备份。

## 当前实现范围

- 输入框表情按钮打开全屏弹窗，关闭回到原聊天；保留文字草稿，不显示拖动横杠。
- 顶部标题与关闭、玻璃搜索框、横向分类、紧凑图片网格、底部网络／收藏切换；默认网络。
- 复用项目玻璃底色、高光、模糊、阴影、系统字体及浅深色变量，低透明度／高对比度使用实色。
- 聊天背景不可交互且暂停可见消息已读推进；弹窗随可视视口调整高度，兼顾键盘和安全区域。
- 收藏图片点按直接发送，长按或右键预览、取消收藏。聊天单图以及多图消息中选中的单张支持收藏。
- 两名参与者均可使用自己的本机收藏；与创建者专属保险箱相互独立。
- 不再弹出人工联网同意。默认服务为 Imgflip 官方免密钥热门目录，打开即加载；分页24张。
  分类为热门、搞笑、可爱、开心、无语、生气、晚安和打工。名称和人工中文别名用于过滤当前目录。
  **搜索范围是该来源热门图库，并非完整中文斗图库或全网搜索**；国际经典模板较多，页面标明来源。
  无结果不补无关图片，来源失败可重试，不自动转发到另一个供应商。

## 网络边界

官方说明见 [Imgflip API](https://imgflip.com/api)。默认使用 `https://api.imgflip.com/get_memes`，
无需凭据；该目录按服务方热门顺序返回，数量随来源变化。完整图库搜索属于其单独付费接口，未接入。
中文免密钥候选接口核验时失效或超时，ALAPI 也未配置；不声称已经接通完整中文斗图搜索。

客户端只请求同源的设备认证 API。服务端读取关键词做内存过滤，不把关键词、聊天、房间或设备凭据
发给上游；上游会看到服务器 IP 和公开目录／图片请求。这不是端到端加密消息通道。
服务端只保留最多15分钟的公共目录与最多2000条临时图片授权，授权绑定房间和设备；不持久化关键词或原图。
媒体链接只允许 `i.imgflip.com` 的固定图片路径，拒绝重定向、私有地址与携带凭据的链接；
DNS 校验结果直接用于连接，保持 TLS 主机名校验。目录512 KiB、图片8 MiB、超时15秒、下载并发最多8。
客户端关闭取消下载，后端响应关闭也取消媒体工作；公共目录共享刷新在15秒超时内结束。
原图返回前检查文件签名，客户端再进行解码尺寸检查。错误不打印关键词或上游响应。

## 图片与存储

允许 JPEG、PNG、WebP、GIF，每张最多 8 MiB，解码后检查最多 1600 万像素且边长不超过 8192。
先校验文件签名，再交给浏览器解码；不会执行 SVG、HTML 或将网页响应当作图片。
浏览器解码仍属于终端攻击面，大小检查不能声称消除解码器风险。

IndexedDB 版本 8 增加 `memeFavorites`；加密索引包含随机图片 ID、SHA-256、文件名、MIME、
大小与收藏时间。原图单独加密，AAD 绑定房间、存储域和记录 ID；明文摘要、文件名、关键词
不会作为数据库索引。图片与索引在同一事务写入，并复用保险库生命周期锁和预期快照检查。
按内容摘要去重，最多 100 张、总原图 128 MiB；空间不足保留原收藏并提示取消部分收藏。
解密文件后重新核对长度和 SHA-256。取消收藏原子删除原图与索引项，不影响聊天。

收藏是用户主动保存的新副本，原消息的本机删除或全员删除不追溯删除收藏。
清除浏览器站点数据会丢失收藏；收藏不包含在现有 QR3 自动备份或历史归档中，
不会通过设备加入、恢复或后续回复传递给其他设备。

## 发送与生命周期

发送复用现有单图片附件加密、MLS 与待发箱协议，不新增网络消息种类。
新的一次主动发送生成新消息；已进入待发箱的普通网络重试沿用原 ID 和密文。
面板操作期间防止同一操作被重复触发。进入消息提交前，队列与保险库锁都检查取消信号；
已经进入原子消息提交的操作沿用现有事务语义，不回滚部分棘轮状态。

屏幕外缩略图清除对象 URL，最多同时解码三个待加载项目；可见原图在预览和发送时复用。
关闭面板、离开聊天或隐私遮罩立即取消面板工作，清理图片、搜索词、预览和对象 URL；
晚到结果不得重建界面。原有聊天缩略图默认遮挡规则保持不变。

搜索框受信的主指针点击可使用与聊天输入框相同的单次 1.2 秒键盘所有权，
不豁免 hidden、硬生命周期事件、第二次失焦或过期；输入框移除也会使所有权失效。

## 验证

- `tests/meme-media.test.ts`：文件签名、收藏索引和资源限制的失败关闭。
- `tests/meme-server.test.ts`：设备认证、输入限制、分页／别名、来源拒绝、媒体授权和过期。
- `tests/meme-picker.e2e.mjs`：真实 IndexedDB 加密与原字节、去重、旧会话／取消写入拒绝、
  独立副本、点按发送入口、长按不误发、GIF 保存、合成搜索分页与失败、离开聊天的发送状态清理、
  遮罩清理及响应式几何。
- `npm run check:full`：现有附件、回复、生命周期、恢复及整体聊天回归。

合成搜索不证明真实服务可用；浏览器自动化不证明用户 iPhone / iOS 27 / Safari 真机体验。
