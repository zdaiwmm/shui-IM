# GIFs、贴纸合集与本机收藏

## 交互契约

聊天输入框的表情按钮打开半屏液态玻璃面板，输入框保留在面板上方，默认选中 GIFs。
GIFs 展示独立动画；贴纸按完整合集连续排列，顶部 24px 合集缩略图可定位到对应位置，点击热区仍为 44px。
GIFs、贴纸和 GIFs 搜索结果统一每行五个正方形容器，贴纸搜索合集封面使用相同的容器尺寸。
收藏是顶部星形入口，不是全屏搜索底部的分类。顶部可跟手向上拖拽至全屏、向下拖拽回半屏，保留分类与合集定位及其缩略图；点击搜索另进入无底部分类的全屏搜索。收起按钮仅在全屏显示，半屏可通过输入栏表情入口关闭。图片容器统一使用 8px 圆角。
GIFs 搜索返回独立动画，贴纸搜索返回合集，支持查看、整包添加、取消下载及移除本机添加的合集。
合集详情的返回键回到搜索结果，再返回半屏面板。

点击图片发送成功后关闭面板；长按或右键打开操作预览，发送成功后同样关闭。
点击和长按都不缩放图片，长按预览保持来源图片的布局尺寸。
动画在可见面板和已揭示的聊天图片中播放，发送保留原始 GIF、APNG 或动画 WebP 字节。
“GIFs”是独立动画的产品分类，不承诺所有文件扩展名均为 `.gif`。
发送仍走现有附件校验、加密、持久化 outbox 与重试流程，不改变 MLS 算法或密文提交时序。
表情以 image payload 内的 `presentation: "expression"` 标记区分，发送和重试要求所有活跃设备支持 `expression-image-v1`。
聊天中表情默认清晰、无外框和背景，尺寸为原图片展示的 2/3，主动下拖后隐藏，不自动投影到保险箱。
普通列表滚动和被浏览器取消的触摸手势不会隐藏表情；只有从已显示媒体开始、完成明确下拉才隐藏。锁定、离开和隐私遮蔽仍立即清理显示状态。
历史预置表情按原始 SHA-256 匹配；旧版未标记的自定义表情无法可靠区别于普通图片，不按文件名或格式猜测。
长按图片操作改为“收藏为表情”，选中媒体在模糊背景之上显示；锁定同步销毁菜单预览。

## 默认内容与缓存

2026-09-09 起，公共默认列表和搜索均以后台资源库为准，不再展示构建预置清单或触发联网目录检索。
管理员上传或采集后均为待上架，必须显式上架。既有本机收藏与已下载合集保留。
2026-09-09 用户授权将既有预置的 30 个完整合集和 100 张独立动画一次性导入后台并上架；初始化入口见下文。
贴纸选择分类只展示已添加的本机合集；服务端可添加合集仅在搜索页展示，封面进入可返回的详情。
搜索列表与详情均支持添加和解除添加，解除只删除本机副本。GIFs、合集搜索在滚动接近末尾时自动加载，公共列表每页 24 项，失败才提供重试按钮。
GIFs 顶部同样展示已添加合集快捷入口，点击切换到贴纸并定位。
下架和删除立即阻断后续搜索、合集详情及旧媒体凭证；已取得的本机副本和已发送消息不会远程删除。
网格保留可见区域加载、三张并发与离屏 URL 释放；面板内最多缓存 16 MiB 已验证原图，关闭或锁定清空。
不再全量预热构建素材，公开网络图片不加入持久 Cache API。

以下构建素材用于历史表情识别、原文件完整性回归及显式初始化；公共列表仍只以后台上架状态为准：

`src/lib/starter-library.json` 固定预置 30 个完整合集、100 张独立动画；新增 10 个为非露骨的恋爱、亲吻和情侣互动主题。
100 张动画来自这些合集中的不同原始文件，避免额外重复存储。
`public/stickers/sources.json` 保留各原图的作者、来源合集、摘要和未修改声明。
更新素材使用 `node scripts/prepare-sticker-library.mjs`；不能用重复文件凑数。

`src/lib/additional-gifs.json` 的 `noto-gifs-20260909` 批次另含 400 张不同的 Google Noto 原始 WebP 动画（132,633,304 字节），未重编码。`public/gifs/sources.json` 逐张记录作者、原始地址、摘要和 CC BY 4.0 许可；`LICENSE.txt` 随资源分发。来源为 [Google 官方动画库](https://googlefonts.github.io/noto-emoji-animation/)，许可依据 [官方公告](https://developers.googleblog.com/en/updates-to-emoji-new-characters-new-animation-new-color-customization-and-more/)。离线准备入口为 `node scripts/prepare-additional-gifs.mjs`，不扩展运行时上游访问名单。

旧预置资源随应用静态文件交付，通过同源路径读取；新面板不再调用后台预热。
首次使用仍需下载资源，后续打开优先使用本机缓存，不重复等待第三方目录或 CDN。
缓存名 `quiet-room-starter-media-v1` 在 Service Worker 更新时保留。
每次读取按构建清单核对大小和 SHA-256；仅清单内的公开素材可以进入这个未加密缓存。
缓存不包含关键词、聊天附件、本机收藏或用户另行添加的合集。
缓存被浏览器清理或存储不可用时，回退到同源原文件。
网格按可见范围加载、最多同时处理三张图片，离屏与隐私隐藏时释放内存图片和 object URL。

## 网络来源与搜索范围

本节来源仅由后台显式采集任务使用；客户端搜索只查询已入库且已上架的资源。
服务端不再因聊天浏览、搜索或图片读取请求访问上游。

无需用户配置密钥，使用 Signal Stickers 的公开目录与 Signal 公共贴纸 CDN。
这里不是 Telegram 账号接入，也不是 GIPHY 或全网爬虫。
后台采集对公开目录的标题、作者和标签检索，常用中文关键词有英文别名映射。
GIFs 从符合检索条件的动画合集抽取单张资源；前端的 GIF 搜索只返回已上架独立条目。
贴纸以完整合集返回。目录中明确标记 NSFW 的合集不会返回；标签不构成对所有第三方内容的人工审核保证。
公开社区目录没有本项目可承诺的可用性 SLA；采集失败显示状态，不影响已入库资源读取。

来源说明：

- [Signal Stickers 项目](https://github.com/signalstickers/signalstickers)
- [公开目录](https://api.signalstickers.org/v1/packs/)
- [公开贴纸格式与传输说明](https://github.com/signalstickers/signalstickers-client/blob/master/STICKERS_INTERNALS.md)

应用内搜索仅请求同源已认证接口 `/api/rooms/:room/memes/search`、`pack`、`media`。
应用服务器可以看到搜索词；上游只会在后台采集时收到目录和固定媒体请求，不收到搜索词、房间标识、设备令牌或聊天内容。
公开内容搜索不属于消息端到端加密通道，也不需要额外人工联网授权；不记录原始关键词。
GIPHY、KLIPY 等需密钥或限制代理缓存的服务不作为隐藏回退源。

## 网络与存储边界

`server/memes.mjs` 只允许固定 HTTPS 目录与媒体主机，拒绝重定向，DNS 固定到公网 IPv4，禁止任意 URL 代理。
目录上限 8 MiB、合集清单 256 KiB、单张原图 8 MiB，限制请求频率与媒体并发。
公开 Signal 资源先校验 HMAC，再按其公开格式解密；该传输解析与本项目 MLS/用户密钥完全独立。
媒体授权 ID 绑定设备、15 分钟到期，最多保留 4,000 个；目录与清单只在服务端内存缓存。
公开原图与资源元数据持久化于 `quiet-room.sqlite` 的资源表，与聊天密文独立建模；现有在线快照与恢复覆盖它们。
每个合集最多 200 张、64 MiB，资源库最多 10,000 项、去重原图合计 1 GiB。内容摘要去重原图，来源 ID 去重采集条目。
完整合集校验后一次事务提交；失败不留半包。删除清理无人引用的原图；SQLite 可复用空页，不承诺文件立即缩小。
不向浏览器提供目录中的公开解密参数，也不记录聊天关键词。

## 后台管理

本机预置库初始化使用仓库命令 `npm run expressions:initialize -- --data-dir /绝对路径/本机数据目录`。
先停止该目录对应的本机服务；命令仅接受已存在的服务数据库，不联网、不读取本机加密收藏或用户下载合集。
初始化逐张核对原始字节大小、SHA-256、图片类型和来源记录，独立动画另核验动画格式；
全部 130 项及完成标记在同一事务提交，任一文件损坏整批回滚。原图按摘要去重，不更改原文件。
既有同来源合集保留其管理员状态；完成标记随 SQLite 备份恢复，重复执行不会重新上架或恢复已删除项。
此入口不在启动时自动执行，也不是生产发布入口；生产数据写入需单独取得该环境授权。

新增批次使用 `npm run expressions:initialize -- --data-dir /绝对路径/数据目录 --batch noto-gifs-20260909`，仍须停止对应服务，并在写入前保留可恢复快照。400 项与该批次完成标记在单一事务提交；基线批次不重放。生产镜像携带清单，命令从 `dist` 校验公开原图。2026-09-09 本任务用户已明确授权新增普通资源随版本进入生产；上线结果以独立回读记录为准。

`sao.shui.click` 的表情管理沿用密码 + TOTP、精确来源、Cookie 会话和 CSRF。
GIFs 与贴图合集分别支持分页搜索、预览、上传、编辑名称与标签、上下架和删除。
手动上传支持一个 GIF 或多张图片组成合集，每次原图合计最多 8 MiB；只接受 GIF、PNG、JPEG、WebP。
管理员负责确认来源和使用权限；上传不用于接收或恢复聊天原图。上传永远先进入待上架。
后台反向代理仅对 `/admin-api/expressions` 放宽至 12 MiB，以容纳有界 Base64 JSON；其他后台请求仍为 64 KiB。

采集按所选类型设置 1–100 项目标，可填关键词或指定公开合集 ID；GIF 目标按新增独立条目，贴图按新增完整合集计数。
一次只运行一个采集任务，最多扫描 200 个候选合集、运行 10 分钟，遇到重复或失败继续寻找，达不到目标如实标记。
保存最近 20 个任务的类型、目标、新增、跳过、失败和结束状态；不保存原始检索词。
进程重启将未完成任务标为中断，不自动恢复联网；已完整提交的待上架资源保留。
DNS 为非公网地址、重定向、HMAC 或格式验证失败均拒绝，不允许通过任意 URL 或关闭防护绕过。

用户添加的合集保存在本机加密 vault 中，最多额外添加 20 个合集，每个最多 200 张、64 MiB，总计 256 MiB。
全包下载和文件验证完成后，在 vault 生命周期锁下原子提交加密索引与加密原图。
取消或隐私生命周期失效不得留下可见的半包，过期快照不能提交。
预置合集再次出现在网络结果中显示“已添加”，不重复下载为用户合集。
收藏仍限制 100 张、128 MiB，保存独立加密副本；原消息删除不删除收藏或合集副本。
收藏与用户合集不跨设备同步，也不进入恢复备份。解锁终端受到控制时，本机加密不能隐藏已解密的内容。

## 验证入口

- `tests/expression-catalog.test.ts`：入库持久性、上下架、凭证撤销、数量目标、重复采集及半包失败。
- `tests/admin.test.ts`、`tests/backup-admin-ui.e2e.mjs`：资源管理认证与真实上传、编辑、上下架、删除。
- `tests/backup.test.ts`：资源状态与原图随一致性快照恢复。

- `tests/meme-server.test.ts`：公开资源完整性、主机和设备授权边界、目录搜索、分页及错误。
- `tests/sticker-library.test.ts`：完整合集数量、独立动画数量、原文件摘要、真实动画检测及来源记录。
- `tests/meme-picker.e2e.mjs`：半屏/全屏、发送收起、长按、合集下载、缓存、隐私清理及两处动画播放。
- `MEME_WEBKIT=1 node tests/meme-picker.e2e.mjs /private/tmp/sticker-picker-webkit`：WebKit 同范围验证。
- 最终组合执行 `npm run check:full`。自动浏览器验证不等于 iPhone / Safari 真机验收。
