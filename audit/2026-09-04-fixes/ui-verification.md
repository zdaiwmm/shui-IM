# 修复后页面验证

2026-09-04 使用冻结后的源码执行生产构建，再以 Vite preview、真实 Chromium 和隔离的临时会话拍摄。最终一轮完成 **64 张截图，0 个浏览器 pageerror**。所有图片都是脚本内生成的合成 SVG；没有读取或包含用户上传的参考图。测试结束后临时会话、后端数据库和构建目录均删除。

运行命令：`node tests/ui-audit.e2e.mjs audit/2026-09-04-fixes/screenshots`。原尺寸图片位于 `screenshots/`，六张联系表位于 `contact-1.png` 至 `contact-6.png`，逐页 DOM 几何、颜色、玻璃层和动效记录位于 `geometry.json`。最终图片查看器截图等待图片解码及打开动效完成；360 ms 缩略图展开动效仍保留在 JSON 记录中。

覆盖 390×844 手机、320×720 窄屏、1280×900 桌面、844×390 横屏、深色模式及 320×720 / 20 px 根字号压力场景。页面包括欢迎、通行密钥、邀请配对、聊天空态与恢复置顶提示、文字及多图片消息、菜单、恢复包、长按消息、选择复制、设备管理、设备链接、四图片相册、图片查看器和解锁。保持菜单开启的尺寸切换覆盖了旋转后的定位。

已检查所有联系表，并复查横屏菜单、深色相册、设备链接弹窗与全屏查看器的原尺寸图。最终范围内未发现新的发布阻断 UI 问题：

- 64 个页面记录均没有文档横向溢出；菜单、消息操作菜单、恢复/复制/设备链接面板均未越过当前视口。844×390 中更多菜单底部为 365 px，消息操作菜单底部为 322 px，设备链接面板底部为 370 px。较长弹窗使用内部滚动，底部操作不与上方文字重叠。
- 手机与窄屏的通行密钥、解锁按钮、说明及返回操作保持独立间距；大字场景文本允许自然换行。横屏长页面保持可滚动。
- 聊天标题栏和输入栏的底色透明，渐变遮罩从 62% 经 20% 过渡到透明，消息和合成图片可以透过遮罩看到。恢复提示保持置顶条样式，自身状态位于左侧，对方状态位于中间。
- 恢复、选择复制和设备链接面板使用相同的 62% 面板色值，配合各自背景模糊；设备链接不再使用接近不透明的纸张色。窄屏复制/完成操作能换行或纵向排列。
- 深色相册的时间文字为 `oklch(0.99 0.004 255)`，背景为黑色 68% 遮罩；已在深色和浅色合成图片上核实可读。相册标题栏包含渐变遮罩、12 px 模糊与渐隐 mask。
- 图片舞台在 390×844、320×720、844×390、深色 390×844 中均为 `(0, 0)` 到完整视口。原图保持比例，因此横图在竖屏上保留上下黑边。已记录 360 ms 从缩略图位置展开到全屏的动效；菜单具有 180 ms 入场动效，通用弹窗具有 160 ms 透明度和 180 ms 位移动效。

这些截图和几何结果仅描述本次 Chromium 生产构建场景。20 px 根字号是压力模拟；没有把它描述为 iOS 系统字体设置。iOS 原生 Safari 状态栏、系统文件选择器和振动仍需真实设备观察。隐私跨锁屏、MLS 并发、恢复身份替换、历史分页及图片拖拽等行为由独立浏览器和服务端回归测试验证，本截图报告不以静态图片替代行为测试。
