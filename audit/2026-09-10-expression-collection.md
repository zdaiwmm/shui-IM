# 后台表情采集验收（2026-09-10）

任务分支 `codex/admin-expression-collection-fix`，基线 `49a6dc933e13d4e301116dc4463ee6ab3cb4f117`。
下方实测为本地证据。交付后续：候选 `d23c7cbbcc1c8c0a4d1645ae009ca5d80f4d9c2b` 的 [CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34453330816) 全部通过，
[PR #76](https://github.com/zdaiwmm/shui-IM/pull/76) 合并为 `cd7db9000d72e9f3505e3367595e2b64583273c0`。未发布生产。

## 实现与确认的缺陷

- 独立表情采集菜单、渠道切换、分页搜索、原图预览与对应资源管理入口。
- Signal 选中包按稳定 ID 解析，不再把展示标题当作搜索词校验；标题可超过搜索词 80 字限制，原实现会拒绝这类请求。增加长标题回归。
- 重复指定资源返回已存在，不再因新增数为零误报失败；保留部分成功、上游失败与中断状态。
- 新增 Google Noto 官方动画渠道，进入 GIFs 管理，保留来源和作者。限制固定目录与 CDN 路径，保留公网 DNS、大小、格式和动画校验。
- 搜索按钮保持横排，卡片操作放在名称下方，避免窄屏长状态挤压标题。

未取得用户当时失败包 ID 与错误回执，不把已确认缺陷说成该次失败的唯一根因。

## 真实上游验收

构建后运行 `CHROME_CHANNEL=chrome node tests/admin-collection-live.e2e.mjs`。
真实后台登录、搜索、封面解码、点击采集和原图读取，没有拦截或模拟渠道响应。
临时数据库关闭后重新打开，核对条目数与首张原图长度；两个渠道再次搜索均显示已入库。

| 渠道 | 资源 ID | 完整入库数量 | 首张字节 | 状态 |
| --- | --- | --- | --- | --- |
| Signal cat meme | `8771105c23a9dc10df5aafb55105ef02` | 20 张 | 253297 | 待上架 |
| Google Noto smile | `noto-1f600` | 1 个动画 | 369214 | 待上架 |

桌面 1280px 与窄屏 320px 截图、机器结果保存在 `/private/tmp/collection-live-evidence/`。
这是浏览器模拟视口，不是 iPhone 真机；不承诺所有上游资源永久可用。

## 回归

`npm run check:full` 通过：69 个测试文件、544 项单元测试、30 个浏览器专项。
最后的卡片 CSS 调整后重新构建，并重跑 `tests/backup-admin-ui.e2e.mjs` 通过。
真实联网测试为显式入口；首次已成功，后续复验曾遇 Signal 搜索超时，取证后直接目录下载与目录查询成功；补充明确来源错误提示后，最终后台实测两个渠道再次通过，数量与上表一致。
最终错误提示改动后，后台与采集定向测试 15 项通过。默认测试不依赖第三方服务可用性。
