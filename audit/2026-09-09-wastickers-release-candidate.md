# Wastickers 发布候选核验

- 基线与规则来源：本机 main `d5a7a8f9ab19f2555696fbeb958201aa9af681ff`。
- 本任务独占目录 `/private/tmp/quiet-room-delivery`，分支 `codex/fix-worktree-doctor`。
- 发布前复核纠正先前的大小根因推断：用户提供的归档为 5,192,080 字节，包含文本元数据、
  一张封面及 30 张贴图，没有 JSON 清单。旧解析器要求 JSON，故拒绝导入。
- 新解析器在本机只读解析该归档，识别 30 张贴图、5,176,308 字节原图；附件及元数据内容
  未加入仓库，未向生产导入该附件。本机临时数据库入库 30 张并逐字节回读一致，验证后删除
  临时数据库。合成包覆盖同一结构及后台上传流程。
- 上传文件上限保持 8 MiB，与后台 12 MiB Base64 JSON 入口一致；解压后合集上限 64 MiB。
  先前候选中界面宣称上传 64 MiB 的变化已纠正。
- 本机定向验证：归档解析、资源库、后台 HTTP、Git 工作树准备共 21 项通过。
  完整本地回归、PR/main CI、部署及生产回读分别记录，不由本条推断完成。
- 环境纠正：受限执行下 Git 元数据写入、DNS 和 keyring 不可访问；提升权限后 GitHub
  认证与推送成功。无需反复注销登录。开工本机检查与远端认证检查分离，缺少依赖由 agent
  执行锁定安装；公共与工作树 Git 目录分别实际探测写入。
- iPhone / iOS 27 / Safari 真机尚未验证。
