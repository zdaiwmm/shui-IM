# 聊天记录本地备份

本文件描述本机聊天归档。它与 [RECOVERY_BACKUPS.md](RECOVERY_BACKUPS.md) 的自动服务端备份分离：后者继续运行，但不作为普通用户的新显性入口。双人会话恢复见 `src/lib/joint-recovery.ts` 与 [D-041](docs/context/decisions.md#d-041双人恢复后其他旧设备必须重新授权)。

## 当前用户流程与范围

聊天菜单的“备份/恢复聊天记录”打开独立页面。主按钮“备份聊天记录”先验证本机通行密钥，通过后立即导出并用系统分享交出文件；不支持分享或用户取消时回退本机下载，并说明未确认保存。网页不能证明分享面板或下载最终完成。新消息需要重新导出。

导出当前设备已持久化、导出开始时网络游标以内的聊天记录，包含事件及删除投影；不含未发送队列、草稿、设备私钥、MLS 状态、保险箱直传、收藏及置顶。当前实现只封装完整缓存在本机的附件，不联网下载原件；缺失原件逐个计数并明确提示，文件仍含对应的聊天记录和清单。不能称作全部设备历史或全部附件原件备份。

附件保留原始 AES-GCM 密文块，逐块解密仅用于认证与 SHA-256 检查；不解码媒体、不重编码、不降分辨率、不转码。整个文件采用无损 gzip 分块压缩后加密，已压缩的媒体密文通常不会缩小。

导入只读取用户选择的文件，不发起 fetch、上传或在线取件。完整校验后先合并本机聊天隐藏记录和保险箱隐藏记录、应用聊天删除事件，再幂等导入历史和附件。与本机相同序号不同内容发生冲突时拒绝导入。最后回读所有本机记录及附件摘要，只有回读成功才报告完成。中断后的已提交内容保留，重新选择文件只补差异；不推进 MLS、消息游标、回执或未读。

## 格式与密钥边界

- 文件前缀为 `QRL1`、43 字符归档 ID、换行；ID 是定位提示，不是授权证据。
- 内层版本魔数 `QRLOCAL1`，24 字节随机盐，8 字节随机 nonce 前缀。每次导出重新生成。
- 当前实现从本设备已同步的归档谱系取得 256 位随机归档密钥，通过 HKDF-SHA-256、随机盐和独立域 `quiet-room-local-history-v1` 派生 AES-256-GCM 密钥，不直接用短口令或恢复码文本加密文件。面向用户只称“恢复码”和“聊天备份文件”。
- 每条记录使用 8 字节前缀加 32 位顺序计数器组成 nonce。AAD 绑定完整头部及计数器。gzip 内容和记录类型一并认证；结束记录认证总条数，拒绝截断、重排、追加和版本不符。
- 加密的首条元数据绑定房间、角色和归档 ID。导入必须持有本设备或显式恢复继承的同一归档谱系；普通添加设备不自动取得密钥。错误房间、角色或无对应密钥都拒绝。
- 既有换码保留原归档密钥，因此旧本地文件无需重写；旧密文和旧秘密已经被复制时，不能远程销毁其离线解密能力。
- 当前基础能力沿用旧单码归档密钥，不提供新的二取二历史加密承诺。双人授权及无旧在线设备时的恢复需后续协议实现，不能用本文件能力替代。

记录顺序固定为元数据、聊天记录、对应附件可用标志及原始密文块。文件名不作为路径，归档内容不执行、不作为 HTML 解析；消息使用现有严格 payload 校验器。未知版本拒绝，不自动降级；未来迁移须显式增加版本读取器并重新验证归属与完整性。

## 资源与隐私

- 单记录解压后最多 3 MiB，解压过程立即检查输出上限；压缩和解压每次只持有一个有界记录。
- 内层文件及累计解压内容各最多 8 GiB，最多 200,000 条归档记录，最多 100,000 条历史记录；实际可用空间也受浏览器配额限制，失败不显示成功。
- 生成时写入 OPFS 临时密文文件，不将整份备份堆在 JS 内存中。OPFS 临时文件不是外部备份；锁定、返回或失败会清理。系统保存交接允许密文暂存两分钟后清理，以免失焦锁定中断系统读取。页面崩溃可能留下密文暂存，清除网站数据会删除它；不会删除已另存到系统文件位置的副本。
- 导入先完整验证，再分阶段应用删除与内容，再最终回读；大型文件会多次读取，这是内存有界与先验证后显示的取舍。存储写入继续使用现有 vault 版本核验和互斥流程。
- 隐藏、锁定、运行期撤销取消任务；系统文件选择复用现有有界前台交接，不放宽认证和生命周期要求。

## 验证

- `tests/local-archive.test.ts`：压缩往返、随机化、错误密钥、篡改、重排、截断、追加、版本和记录上限、取消。
- `tests/local-history-backup.e2e.mjs`：合成 5 MiB 多分块视频逐字节核验、OPFS、损坏归档不导入、错误身份/角色/房间、重复导入、删除投影、无网络导入、游标不变及窄屏操作。
- 旧云端生命周期和恢复测试继续在 `npm run check:full` 中运行。独立密码学审计、真实 iPhone/iOS 27/Safari 保存、数 GiB 压力和崩溃恢复未验证。
