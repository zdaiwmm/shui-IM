# Quiet Room 项目接手指南

本文是新会话的轻量入口，不替代产品、安全、测试和运维原始文档。开始工作时先读本文件，再按任务读取 `docs/context/` 中的相关页面；不要为了获得上下文而默认通读整个仓库。

## 接手顺序

每个新会话先读本机 `main` 中本文件及
[`workflow.md` 的固定交付流程](docs/context/workflow.md#固定交付流程)，报告规则来源提交和当前任务的授权范围。
可用 `git show main:AGENTS.md`、`git show main:docs/context/workflow.md` 只读取得本机规则，
再按下列顺序读取任务版本的上下文。没有本机 main 时使用当前版本并说明来源；远端同步仍按现有授权规则执行。
规则更新必须提交到 main；新会话加载规则不代表继承其他任务的推送、合并或发布授权，
每次生产发布仍须用户确认本次精确提交。生效范围见[新会话入口](docs/context/maintenance.md#新会话入口)。

0. **任何文件修改前先完成任务隔离**：读取 [工作流开工门槛](docs/context/workflow.md#开工隔离门槛)，核验并报告本任务工作树路径、分支和基线。共享主目录仅供只读接手，不能因目录干净或处于非 main 分支就认定独占。无法确认归属时新建 worktree；隔离失败时不得退回共享目录写代码。
1. 先读 [`docs/context/status.md`](docs/context/status.md)，确认当前提交、工作树、已知漂移和证据级别。
2. 用 [`docs/context/overview.md`](docs/context/overview.md) 定位本次任务涉及的代码、详细文档和测试。
3. 涉及产品语义、安全边界或既有取舍时，读 [`docs/context/decisions.md`](docs/context/decisions.md) 及其中指向的权威来源。
4. 只有任务需要时才读长文档；发布前必须另外读 `RELEASING.md`、`DEPLOYMENT.md`、`OPERATIONS.md`、`SECURITY.md` 和 `PRODUCTION_SECURITY_GATE.md`。
5. 完成实质变更后，按 [`docs/context/maintenance.md`](docs/context/maintenance.md) 判断是否更新知识库。

## 项目不变量

- 无账号、两名参与者，每人最多三台独立密钥设备；正式来源 `https://ai.shui.click` 绑定 WebAuthn 与本机历史，不能随意迁移。
- 服务端只处理密文与必要元数据；普通新增设备不能取得加入前历史，也不能通过后续回复获得旧内容预览；原设备恢复后的显式归档恢复例外见 `RECOVERY_BACKUPS.md`，服务端不保存恢复码。
- 消息与 MLS 状态原子持久化；普通重试复用原 ID 和已生成密文，不重复推进棘轮。成员纪元变化的重加密须遵守现有协议。
- 导入的原始附件逐字节加密与校验；图片与视频可在本机预览和查看，视频首帧仅在完整校验后于内存中生成，不上传；其他普通文件不在应用内执行或预览。
- 遮罩不是认证；在线、未读、送达是不同事实；不得承诺防截图、原生工具栏透明或“零元数据”。
- MLS 标准不等于独立审计。公开高安全声明受 `PRODUCTION_SECURITY_GATE.md` 阻塞；其他长期边界见决策页。

## 状态与证据规则

- 分开记录：`工作树存在`、`自动测试通过`、`真机验证通过`、`CI 通过`、`已部署到生产`；代码或测试文件存在不等于验证、合并或上线。
- 状态页不是实时监控。修改前复核 `git status`、`HEAD` 和相关文件；生产结论需要单独的发布/回读证据。
- 现有未提交内容归用户所有；不要覆盖、回滚、暂存或提交其他任务的改动。
- 文档与代码冲突时先核实并记录；不要让实现自动改写产品意图。安全语义不明确时失败关闭。

## 工作约定

- 用户已于 2026-09-08 确认[有条件自动集成本机 main](docs/context/workflow.md#有条件自动集成)：实施任务验证并提交后默认完成本机集成、必要服务更新与回读；按流程互斥执行，脏 main、产品／安全语义不明确或当前明确禁止集成时暂停。该持续授权不包含 GitHub 同步或生产发布。
- 执行任务时按 [`docs/context/workflow.md`](docs/context/workflow.md) 分级：默认单任务单故障域，先明确验收与验证范围；同一假设连续两次被证据否定后重新取证，不继续盲调。模型建议不是自动切换配置，也不替代下列安全和发布门禁。
- 分支、隔离 worktree、本任务提交及已授权集成由 agent 按工作流管理，用户无需提供 Git 命令或分支名；复用本任务独占环境，不在多个会话共用目录切分支，不混入其他任务改动。普通修复先在独立 worktree 验证并提交，再按授权集成本机 `main`、回读本机服务并进行局域网真机验收；只有用户明确要求同步 GitHub 时才推送任务分支并进入 PR／CI，生产发布需要另外授权。家里和公司分别运行本机环境，代码与规则通过 GitHub 同步，本机证书、数据和保险库不得同步。完整流程见 [`docs/workflows/local-lan-testing.md`](docs/workflows/local-lan-testing.md)。
- 用户只需描述问题与期望；移动交互默认针对用户提供的 iPhone / iOS 27 / Safari，任务明确指定环境时覆盖。默认环境不等于真机验证证据，也不缩减风险匹配回归；固定执行要求及授权继承见工作流。
- 最小范围检索；不默认读取 `data/`、`.deploy.local.json`、非示例 `.env`、密钥或备份。知识库和日志不得包含凭据、邀请、恢复材料、真实消息或生产数据。
- 不新增第三方分析、标签管理、远程脚本、错误收集或会话回放。
- 修改协议、密码学、设备边界、恢复、未读、通话信令、隐私生命周期、存储或部署流程时，先读对应安全文档与测试，再进行改动。
- 代码变更按风险验证：通常运行 `npm run check`；跨页面/生命周期/附件等需 `npm run check:full`；通话按地图补跑专项。纯知识库变更只检查事实、路径、链接、命令与格式，不必重跑产品套件。未运行的验证必须说明。
- 只有用户明确要求发布并确认目标提交后，才按 `RELEASING.md` 使用固定入口发布。不得把本地未提交内容直接传到生产，也不得用知识库中的旧 SHA 作为发布授权。
- 每次发布都要在 `publish.mjs` 核对精确成功回执后完成独立生产回读；回读成功后、任务结束前，按维护页对账知识库并轻治理上下文。结构化入口落地前按 `RELEASING.md` 的人工证据清单执行；回读或对账失败只重试对应后置步骤，不得再次执行生产切换。
- 回读与知识库对账完成后，按[发布后资源清理](docs/context/workflow.md#发布后资源清理)自动清理本批已集成、证据已保存且不再使用的工作树与任务分支；脏目录、归属不明、未集成或活动中的资源保留并说明，不强制删除，不因清理失败重发。

## 来源与更新

用户明确确认的需求决定意图，当前代码/测试证明实现，专项文档定义产品与安全契约；知识库只是摘要。历史报告只对其时间与基线有效。实质变更按维护页同步，不记录每轮聊天，不臆造用户决定或验证结果。
