# 自动加密备份与会话管理

本文件定义自动加密备份的恢复契约。主站功能已随 `6753d010d822ef5cb90b2b3d2582d9fd1f7805c6` 部署；当前验证层级与后台启用状态见 [状态页](docs/context/status.md)。普通会话仍无账号，只有创建者、受邀者及各自最多三台独立密钥设备。

## 用户流程

1. 原设备联网并解锁后，自动建立恢复备份，再分批备份已持久化的本机历史。面向普通用户的入口是“会话保护”和恢复码保存确认。旧“备份与恢复”页（查看同步状态、立即备份、统一历史恢复）仅通过隐藏地址 `#legacy-backup` 保留，不出现在新的主要菜单文案中。锁定、关闭或离开页面暂停自动备份，后续解锁继续；没有后台常驻备份保证。
2. “查看本设备恢复码”先真正锁定当前会话，再验证通行密钥。本机解密显示当前码，一分钟后锁定；取消、离开或失焦清理可见码和会话。恢复码只保存在本机加密保险库中，用户仍应单独保存一份。复制会把码交给系统剪贴板。
3. 新设备的显性入口是双方各自输入恢复码的共同恢复。旧的单码“恢复已有会话”（需另一台已授权设备在线批准 MLS 替换）仅通过隐藏地址 `#legacy-recovery` 保留。该兼容路径仍从码中取得随机备份编号与独立取件令牌，自动获取对应密文并在本地验证、解密；无需账号、管理员选文件或手动导入 JSON。
4. 创建新通行密钥后，先确认清除本机本会话的历史、缓存和待发数据，再发起恢复请求。沿用已实现的 MLS 替换协议：生成新身份、访问令牌和 leaf，原身份只签署短期替换授权。另一台可信活跃设备上线后会直接弹出授权请求，需明确批准；其他活跃设备均需支持该协议。同一房间同时只保留一个待授权恢复请求，重复请求会被拒绝，原请求继续等待。请求最多有效 15 分钟，期间旧设备被隔离，完成后永久撤销。过期请求不会自动弹出或出现在设备管理页。
5. MLS 替换成功后立即生成新恢复码、保留全部历史归档密钥并更换归档访问令牌，在本地持久化完整待上传请求。新恢复包经本地解密自检后上传，服务器在一个事务中更新取件入口、转移全部历史归档并停用旧入口。成功确认后显示新码；未确认前停留在恢复保护页面，重试不会重新随机生成另一个码。
6. 进入会话只接收新的 MLS 消息，不自动取回归档片段。“恢复历史记录”是统一入口，弹窗要求输入一个或多个本会话设备的当前恢复码（每行一个，最多 6 个）。输入框同步聚焦；确认后清空输入、收起键盘和输入弹窗，打开恢复进度。创建者一起恢复聊天和保险箱直传记录，参与者只恢复聊天。客户端逐个验证会话归属后合并归档、去重并核验冲突；要恢复所有设备产生的内容，仍需这些设备各自有效的恢复码。底层保留单独相册导入兼容能力，但页面不再提供第二个恢复按钮。

   先核验片段、对比本机相同序号的记录并取得缺失内容总数，再导入记录；读取阶段显示不确定进度，导入阶段显示进度条、百分比和聊天／保险库已处理条数与总数，保险库行仅创建者可见。相册消息按一条记录计数；计数排除本机相同的已有记录，包含需按删除投影隐藏的缺失内容，不等于新增气泡、可见照片或原图下载数量。每个缺失片段独立提交，重试重新核验并只补剩余差异；本机已齐全的片段在导入阶段不再下载。为避免撤回内容重新出现，必须先发现并应用所有删除投影，旧备份仍需要完成核验才开始导入。全部片段完成后，在保险库互斥锁内通过本机历史读取器逐页回读，重新核对每条内容的认证摘要；缺失、损坏或冲突不能显示成功。此前完成过恢复或备份游标靠后，都不能代替实际本机对比。回读核验通过才显示 100%。弹窗分开显示备份总数、本机原有、本次实际补入、其中可见与隐藏数量，以及备份内容在本机的可见总数。可见数量使用与聊天／保险库相同的撤回投影、仅为我隐藏和保险库隐藏规则，按记录而非图片张数计数；展示新增可见记录的原日期范围。结果摘要仅留在本次页面内存，不上传或持久保存。进度弹窗可收起，原入口变为“正在恢复中，点击查看进度”，点击只打开同一任务。提交后先把任务与输入的恢复码保存在本机加密保险库，再开始网络操作。断网保留任务并显示“重试恢复”；离开恢复页、刷新、锁定和隐私清理暂停网络操作，重新进入并解锁后可以直接重试，不必重新输入恢复码。只有恢复完成或明确“取消恢复”才清除任务与保存的码；“更换恢复码”明确取消旧任务后重新输入。取消保留已经导入的记录。任务不进入云端检查点或恢复导出文件。

   输入弹窗移除粘贴按钮和输入框下方提示，使用系统原生粘贴。面板按可见视口紧凑排列，输入框滚动区域外保留等量上下间距；极小视口、多枚码或放大文字时保留必要的原生滚动。读取阶段显示已找到的待恢复条数，总数仍待确认；完成核验后才显示确定总数。错误区分核验中断与导入中断，不把尚未导入的内容称作已保留恢复进度。

   聊天删除事件只影响聊天/收藏的可见性，保险箱内容仅按自身隐藏投影统计和恢复。旧备份分类计数缺失时，前台按每轮最多 20 个片段认证解密并补齐分类索引，再上传统计，不将未知密文大小猜成内容条数。

   新备份额外加密保存创建者保险箱的本机删除投影，并在设备恢复换码时携带。显式恢复时与当前设备隐藏状态合并，先保存删除投影和聊天删除事件，再导入媒体；延迟保存的偏好不能覆盖已经持久化的隐藏状态。这不会删除服务器原件或产生对端删除事件，也不备份草稿、置顶或收藏。旧备份没有此字段时兼容读取，但已丢失且从未备份的本机删除状态无法追溯。
7. 恢复中断时保留已通过验证的片段，可安全重试；重复记录去重，有冲突的记录失败关闭。导入只影响本机历史，不推进 MLS、网络游标、回执或未读状态。

表情记录的校验接受已发布格式中的 `expression`、`expression-hidden` 和布尔型 `expressionAutoHide`，兼容缺少新字段的旧表情；不丢弃已认证记录，不放宽未知字段、附件清单或解密完整性校验。格式不支持、索引条数/序号不一致和取件失败分别反馈。恢复并换码后应使用新生成的码，后台保留的已撤销设备密文字节数不证明旧码仍能在线取件。

“有效备份”指对应恢复码可解密、身份及完整性验证通过、服务端仍保留且允许取件的恢复包，以及其中列出的归档片段。它不是对最新一条消息已备份的保证，也不代表一枚设备恢复码包含其他设备的独立备份。图片、语音和文件的清单/密钥随消息归档保存，原始加密附件仍须存在于服务器；不会再复制一套原件。原件在用户查看或下载时按既有流程验证并解密。

## 密钥与存储

- QR3 编码 48 个随机字节：16 字节备份定位号 + 32 字节秘密。定位号不是身份或账号。HKDF-SHA-256 使用定位号作为盐，通过 `quiet-room-cloud-fetch-v1` 与 `quiet-room-cloud-encryption-v1` 分离取件令牌和加密密钥。
- AES-256-GCM 保护恢复包与历史片段，每次加密使用新的 96 位随机 IV，128 位认证标签。恢复包 AAD 绑定格式版本与备份号；归档 AAD 绑定格式版本、会话、归档与片段号。认证加密的索引还包含每段密文的 SHA-256 摘要、首末序号和条数。
- 恢复包包含证明原身份所需的私钥、会话成员锚点、归档密钥/读取令牌。它不包含本设备 QR3 码、通行密钥 PRF、当前 MLS 发送棘轮、旧待处理恢复/设备链接或邀请令牌。设备替换始终创建新发送身份。
- 生成的 QR3 码和待上传状态保存在本机主密钥加密的保险库中。主密钥继续由 WebAuthn PRF 包裹。历史导入重新使用目标设备本机主密钥进行 AES-GCM 加密，存放 IndexedDB 的 `history` 或 `restoredGallery`，不会自动写到手机相册或下载目录。
- 应用解锁后明文和密钥可存在页面内存，图片会产生短期对象 URL。浏览器存储隔离及加密不能防止受控系统、恶意扩展或被替换的网页脚本。再次验证通行密钥是正常界面的查看控制，不能使已解锁页面中的恢复码对恶意脚本不可见。JavaScript 字符串不承诺可验证的内存擦除。
- 服务端只保存恢复/归档密文、随机定位号、取件及归档令牌的哈希、会话/设备归属、版本、时间、字节数。请求带来取件令牌，但不会带来恢复码或解密密钥。正文、Authorization 和恢复表单不得进入日志。

## 轮换及失效边界

网络超时并不证明服务器未保存。待上传密文及版本先本地落盘，再发送；响应丢失后用完全相同的请求幂等重试。恢复包保存采用版本比较；只有已完成的签名设备替换允许把原设备归档转移给替代设备。客户端必须包含全部已有归档，不能仅给新消息换码而丢失旧历史密钥。旧归档只读，新消息写入新归档。

等待设备替换期间，只有服务器明确确认请求已过期才允许重新读取备份。重试前再次确认状态；网络错误不能作为删除当前替代设备身份的依据，以免丢弃已获授权但尚未收到成功响应的本机密钥。

旧在线取件入口与旧归档读取令牌在成功轮换后失效。服务器仍保留少量已停用版本的管理信息。已经被他人复制的旧密文和旧码依然可以离线解密，冷备份及异地快照也可能保存旧版本；轮换不能远程销毁这些副本。归档保留的历史本身可被恢复码读取，不能对这部分历史宣称“棘轮删除后无法恢复”。

普通新增设备仍不能获得加入前历史，添加设备的交接不包含恢复码/归档密钥。本功能只允许持有原设备恢复材料的人显式恢复该设备原已备份内容，并不放宽服务端消息接口的加入边界。

如果原设备仍可解锁，可再次验证后查看当前码。如果原设备丢失且码也丢失，管理员无法恢复，用户接受重建会话。即使有恢复码，没有对应服务器备份、没有仍可协助的可信设备或缺少原始加密附件，也不能保证完整恢复。

## 兼容性与资源限制

单次网络请求（包含响应正文读取）超时 30 秒后明确报错。服务端限流等待期间显示等待状态，并保留取消能力。统一恢复每次批量获取同一归档最多 20 个片段，响应最多 4 MiB，过大时返回非空前缀并继续取余下片段。批量接口沿用相同的归档令牌、设备撤销和码轮换校验，每片继续验证摘要、AAD、序号、条数和消息格式。最多保留 8 MiB 跨阶段密文缓存及 8 MiB 批量读取窗口；更大的片段集合在导入时按同一摘要重新获取，不在内存保留整份明文历史。旧归档原样兼容，无需重建。

前端移除恢复 JSON 下载/上传入口。旧 QR2 文件不会自动变成服务器备份：仍可解锁的原设备应升级后完成首次自动备份，再保存 QR3 码。既有旧格式解析和已导入的待恢复保险库保留底层兼容，但本版没有旧文件导入入口；仅剩旧文件与 QR2 码的用户须在迁移前使用受信任旧版本完成恢复。旧非 MLS 会话不启用这套自动备份，需新建 MLS 会话。

每次前台同步最多处理 20 批，每批最多 100 条，按密文大小自动缩小批次。自动同步发现不足 100 条时，持久化首次发现时间并合并后续记录，最多等待 45 秒；前台每 15 秒轮询，正常连续解锁时约一分钟内上传。后续同步不推迟期限，重新解锁沿用已保存的期限；锁定、离线及关闭期间不保证上传。满 100 条、手动“立即备份”和设备恢复后的换码流程立即上传，已经暂存的待上传密文仍使用原请求幂等重试。等待期间记录仅在本机加密存储，页面明确显示待上传状态，不计入已备份条数。片段和恢复包密文上限 1,800,000 个 Base64URL 字符；每设备最多 100 个归档，每归档索引最多 10,000 段。历史密文配额为每会话 512 MiB、全局 5 GiB，独立于现有消息与附件配额。服务端备份路由每 IP 每分钟 120 次；大批恢复和共享网络可能被限流，服务端返回等待时间，客户端会在可取消的等待后继续当前片段，超过重试预算才显示失败。已导入片段保留并可安全重试；超出索引、大小、空间限制会显示失败，不谎报完成。

SQLite 的恢复与归档表随现有在线/冷备份一起保存，附件仍沿用已完成密文块快照。存在恢复备份的单成员会话不再作为无用邀请自动过期清理；管理员应按元数据判断是否删除。只有本机自动备份不等于服务器灾难恢复，仍需要现有异地备份和恢复演练。

## 会话管理后台

后台来源固定 `https://admin.mijiu.cloud`，普通应用仍是 `https://ai.shui.click`，不扩大 WebAuthn RP ID 或允许跨域 API。默认不配置后台凭据时关闭管理页面/API。

2026-09-04 本次生产回读为 `admin-enabled=0`：后台代码已部署，域名、TLS 和管理员密码/TOTP 初始化及后台验收尚未完成。主站自动备份不依赖管理后台启用，具体发布证据见 [发布记录](RELEASING.md#本次线上发布记录)。

后台展示会话列表、最近活动、设备状态、设备对应的备份版本、时间与字节数；无用户账号维度，无恢复码、私钥或明文查看功能。管理员密码经 scrypt（N=131072、r=8、p=1、随机盐）保存，登录同时要求 Google Authenticator 兼容的 TOTP。TOTP 每 30 秒更新，接受相邻窗口，已使用计数器持久化防重放；服务器需准确时间。

管理会话使用主机限定 Secure/HttpOnly/SameSite=Strict Cookie，首次登录后 7 天绝对超时（2026-09-11 用户要求）。
有效请求同步 Cookie 的剩余期限，始终受首次登录的 7 天上限约束；一周内无操作不会提前过期；会话标识不暴露给 JavaScript，
不新增 localStorage 或长期 refresh token。刷新页面先恢复有效会话，过期则重新验证。
前台且聚焦的页面，在有新的真实点击、键盘或滚轮操作后自动续期，最多每五分钟一次；
没有新操作或处于后台时不发送续期请求，网络错误稍后重试，不自动重放业务写操作。
退出会立即撤销服务端会话；服务重启仍要求重新登录。延长时限增加共享设备及已盗会话的可用窗口，
自动续期不防止已控制终端或持有有效 Cookie 的攻击者，7 天绝对期限不能由续期延长。
写操作（包括续期）校验精确 Origin 和 CSRF；验证全局每分钟最多 8 次且只允许一个并行密码校验。
清理还须完整会话编号、再次输入管理员密码及新的动态码。删除会话及在线备份是不可逆管理操作，会关闭关联连接并清理原始密文附件；清理中断由持久化清理队列在重启/定时维护继续。独立快照和设备离线副本不受该操作删除。审计只记录动作、会话编号与时间。

凭据配置独立于聊天数据卷：详见 `DEPLOYMENT.md` 的可选后台。密码哈希及 TOTP 种子都是管理员安全材料，需要受限配置文件；它们不是用户恢复码。服务器被完全控制后，后台因素无法阻止攻击者绕过后台直接访问/删除存储或替换应用脚本。

## 验证入口

- `tests/history-restore.e2e.mjs`：统一恢复、保险箱直传、多备份去重与冲突拒绝、删除状态、取消重试、角色边界、输入焦点、弹窗流程及浅深色小屏布局。

- `tests/cloud-backups.test.ts`：加密与取件域分离、篡改、权限、幂等、完整归档转移及旧入口失效。
- `tests/browser.e2e.mjs`：真实 MLS 替换、自动取件、新码、无自动历史载入、旧码拒绝及新码统一历史恢复。
- `tests/cloud-backup-lifecycle.e2e.mjs`：服务端提交后响应丢失、重开保险库重试、加密删除快照及换码携带、相册独立导入兼容、迟到结果不能覆盖本机会话、待恢复续接、查看码重新认证与退出清理。
- `tests/backup-admin-ui.e2e.mjs`：真实管理员登录、会话设备详情、清理确认、退出及手机表单/画面。
- `tests/admin.test.ts`：密码与 TOTP、重放防护、主机/来源隔离、Cookie、CSRF、删除重新认证。
- `tests/deploy-admin.test.ts`：后台配置持续启用、首次启用回滚、缺失/不安全配置在停机前拒绝；与既有调用及维护门测试一起运行。

本地自动化使用合成会话和虚拟通行密钥，不代替独立密码学审计、真实手机验证、生产 DNS/TLS/容器挂载或异地灾备演练。
