# 2026-09-21 入口页、聊天与保险箱反馈

## 身份与当前阶段

- 需求编号／标题：2026-09-21-ux-media-feedback，十五项入口、聊天、通话与保险箱反馈。
- 当前阶段／下一步：D 远端交付准备中；用户查看 S1 后提出三处修订并明确「调整后执行」。S2 原型、流程及正式页面已按此修订。
- 规则来源 SHA／任务起始基线／分支：本机 main `749e6af1e05686837dd04028dfb8bf1891cb44c6` 的 `AGENTS.md` 与固定流程；GitHub main 与起始基线 `f256c140d9d9ed3cdcb9cc025c1f308a33d6d9c3`；`codex/quiet-room-feedback-20260921`。
- 实际任务目录与 HEAD：`/Users/zhouding/ss-worktrees/quiet-room-feedback-20260921`，设计起始 HEAD `f256c14`，实施提交 `aea4ba5167e550cb7aa72083401024993f84585e`；跨电脑须重新核验路径与 SHA。
- 需求类型／是否涉及页面：页面、动效、输入行为、附件与实时语音；涉及页面。

## 方案 S2

- 目标：按下表修正十五项反馈。页面行为以用户 2026-09-21 本会话的第一轮确认和输入法补充为准。
- 已知现状：入口页已统一底部锚点，但提示色与邀请返回字号有继承差异；按钮全局按下仍缩放；聊天 Enter 只检查事件 `isComposing`；保险箱显隐只处理已加载资源，进入时列表与数量并行解密扫描；保险箱播放器无点击空白切换工具状态。当前生产 `2026.09.20.2` 记录通话关闭且 TURN 不存在。附件失败及“无声”的实际原因尚未复现，不能按静态线索宣称根因。
- 范围：当前创建／设置密钥／邀请／受邀／遮蔽演示／恢复码／聊天／保险箱页面，及这些页面关联的语音、图片／视频发送流程。
- 排除：不修改密文协议、设备权限、恢复规则或生产部署；原型只用模拟数据。

| 编号 | 目标行为与边界 |
| --- | --- |
| 1、2、4、6 | 创建和设置密钥页的两枚底部按钮同宽且对齐同类页；设置与受邀页小字用相同次级字色；邀请标题与同类页标题字号一致，返回按钮与同类返回字号一致。 |
| 3 | Web 输入法组合期，空格选中的候选词上屏；按回车将正在组合的原始字母／文字放进输入框，不发送。组合结束后再次单独按回车才发送；Shift+Enter 仍换行。原生输入法对按键的消费由系统／浏览器决定，正式验收需在目标浏览器实测。 |
| 5 | 应用内可用按钮在按住时有即时的颜色、明暗或边缘反馈，不缩放；禁用按钮不回应；键盘焦点仍清晰可见。 |
| 7、8、9、10 | 遮蔽说明移至下方热区左侧，按住时从热区向外扩散心跳环；恢复中心标题改为「留好双方恢复码，找回空间需要一起使用恢复码」；本设备码页底部为「我已保存，回到聊天页」，点击直接回聊天；盾牌先向斜下短移，再沿开口向上的弧线加速飞向更多按钮，最低点靠左。减少动态效果时直接完成状态变化。 |
| 11、12 | 语音检查采集、连接、对端媒体与播放四段，并用两端有声作为成功标准；媒体发送分别检查准备、加密、分块、完成、消息确认，卡住有截止与可重试失败提示，重试不产生重复消息。错误根因须靠受控复现判定。 |
| 13、14、15 | 保险箱列表尽快呈现本机已知记录，再异步完成缩略图；显示／隐藏作用于全部相册项目，后续分页继承当前状态，离开或锁定重置；视频播放即隐藏所有覆盖工具，点击空白处切换，暂停／结束重新显示；长按照片、视频、文件的操作菜单增加「发送至聊天」，成功生成一条新的聊天消息，失败不删除源项目并可重试。 |
| 本轮修订 | 恢复码说明页的同类图标、标题、说明及底部动作与创建页沿用同一入口布局；保险箱相册缩略图不显示文件名，名称仅供辅助技术和详情页读取；取消「加载更早照片和视频」按钮，在列表末尾继续上拉或滚动到底自动载入，失败后再次上拉可重试。 |

## 设计材料

- 依据：`DESIGN.md`、`PRODUCT.md` 的入口、聊天、保险箱与动效约束，`src/design-system.css` 的浅深色 token；`src/app.ts`、`src/recovery-experience.css`、`src/styles.css`、`src/gallery.css` 的现有页面。参考应用基线 `f256c14`。
- 原型：[prototype/index.html](prototype/index.html)，S2；以模拟页面覆盖入口、遮蔽、恢复、聊天、保险箱。直接在浏览器打开。不连接真实接口或存储。
- 流程图：[flow.md](flow.md)，S2 Mermaid 源码；支持 Mermaid 的 Markdown 查看器可渲染。
- 原型自查：S1 曾在 Chrome 无头浏览器对 1180×900、390×844、390×520 的浅／深色页面截图核对，390×520 底部按钮完整可见；底部两个按钮同宽 348px，邀请标题 32px、返回文字 16px，两处说明色与副标题相同。S2 修改了恢复说明位置、缩略图文件名和上拉加载，已静态检查；S1 的截图与交互结果不能直接算作 S2 浏览器验收。真实键盘输入法与真机未测试。
- 模拟限制：输入法候选、视频和语音、附件加密／上传、恢复码和保险箱数据全部为演示；原生 IME、iPhone Safari、真实通话与生产网络未验证。原型不证明第 11–13 项根因或真实性能。
- 仍需澄清：第 11、12 项的实际环境、设备、通话状态、文件大小与错误文字；用户已被询问，缺失时先用合成数据定位可复现故障。

## 确认与授权

| 节点 | 版本及范围 | 用户确认时间、最小脱敏摘要与来源 | 下一步权限／未决项 |
| --- | --- | --- | --- |
| 第一次：需求方案 | 本记录方案 S1，十五项；输入法按用户修订 | 2026-09-21，本会话用户回复「空格时候选词上屏，回车是将输入的字母等信息直接上屏；其他确认」；无外部消息定位 | 允许制作原型与流程图；未授权正式产品实施 |
| 第二次：原型与流程 | 用户已查看 S1，针对恢复码布局、缩略图名称和加载方式修订为 S2 | 2026-09-21，本会话用户回复「调整后执行」并指定三处变化；无外部消息定位 | 按当前任务直接实施 S2；用户明确要求修改后执行，未要求再等新版原型确认 |
| 变更／本次例外 | 输入法规则由首次方案修订为 S1 表述；本轮三处视觉与分页交互为 S2 | 同上 | 不扩展生产发布授权；远端材料推送曾被自动审批拒绝，未绕过 |
| 交付与发布请求 | 实施后的当前任务，包括版本 `2026.09.21.1` | 2026-09-21，本会话用户明确要求「推送合并并发布」 | 允许推送、PR、CI 与合并；生产切换仍须按仓库规则确认合并后精确 40 位 SHA |

## 验收与交付

| 验收项 | 对应流程／原型 | 实现与验证证据 | 差异／未验证项 |
| --- | --- | --- | --- |
| 页面字色、字号、宽度、按下反馈与动效一致 | 入口、遮蔽、恢复场景 | 正式样式、文案和路由已改；入口浏览器测试和恢复页图标、标题、底部按钮几何检查通过；查看 390×844、390×620 恢复页及 390×844 遮蔽页截图 | 真机待验；S2 原型未重做截图 |
| 组合输入不误发，随后明确发送一次 | 聊天场景 | 正式输入处理已改，合成组合事件浏览器测试通过 | 原生输入法待验 |
| 全相册显隐、视频工具切换、长按发送 | 保险箱场景 | 正式分页、显隐、播放器及发送菜单已改；文件、缩略图、视频浏览器专项通过，照片／文件发送均复用密文附件并保留保险箱原件 | 真实本机大相册和移动触摸待验 |
| 双向有声、媒体上传可完成并可恢复失败 | 通话／上传诊断流程 | 修复对端音轨晚到时未重新播放；上传阶段有超时和重试；通话专项 123 项单元测试及原生／界面／20 个弱网浏览器场景、媒体上传浏览器专项通过 | 两端真实有声和用户网络失败未复现；据本机状态记录，生产通话开关关闭且无 TURN |

- 材料体积、二进制、Base64 和误纳入依赖检查：本需求目录约 76 KiB，仅 Markdown、HTML、CSS、JavaScript；未包含二进制、Base64、依赖或构建产物。
- 本地验证：`npm run check` 通过（80 文件／658 项测试及构建）；浏览器回归第 1 组 1/1、第 2 组 36/36 通过；`npm run test:calls` 通过（9 文件／123 项测试及原生／界面／弱网浏览器专项）；文档检查、原型脚本语法与差异格式检查通过。真机：未运行。
- PR／CI／生产：本次已获用户明确交付请求，发布版本计划 `2026.09.21.1` 已生成并应用；PR、CI、合并及生产状态以随后独立证据更新。
