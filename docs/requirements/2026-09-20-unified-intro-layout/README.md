# 2026-09-20 同类入口页版式统一

## 身份与当前阶段

- 需求编号／标题：2026-09-20-unified-intro-layout／同类入口页版式统一
- 当前阶段／下一步：本地候选与 PR CI 已通过；合并 main 并回查远端提交
- 规则来源 SHA／任务起始基线／分支：本机 `main` 规则 `749e6af1e05686837dd04028dfb8bf1891cb44c6`；GitHub `main` 与本机 `origin/main` 基线 `af92514d49345c33580da7612009593b41e23b8d`；`codex/unified-intro-layout`
- 实际任务目录与 HEAD：`/Users/zhouding/ss-worktrees/unified-intro-layout`；开工 HEAD `af92514d49345c33580da7612009593b41e23b8d`
- 需求类型／是否涉及页面：页面布局优化；涉及欢迎、访问密钥、邀请／设备接入、备份、恢复码等共享入口页

## 方案

- 目标、已知现状与证据：以「有些话，只留给彼此」为标准页，统一同类页面图标、标题、说明和底部操作区锚点。现状由 `gateway-welcome`、`gateway-plain` 和恢复页分别控制间距，页面间存在垂直漂移。
- 范围／排除项：统一共享 gateway 入口页；长内容使用去图标紧凑态，仍溢出时正文滚动、按钮保持底部。二维码等待、双人恢复选择、保存入口等结构不同页面只沿用已有组件，不强行改成同一纵向骨架。不改业务文案含义、恢复协议、认证、安全边界和生产配置。
- 入口、操作、状态变化及异常边界：标准页固定 20px 横向留白、48px 图标、统一标题与说明间距、52px 主按钮和 44px 次按钮；短视口自动隐藏图标并上提标题；内容区不足时独立滚动，操作区保持完整可见。
- 实现思路、关键取舍、兼容及风险：在现有 gateway DOM 上提升共享 `gateway-intro` 骨架，集中 CSS token 和三态规则；保留现有颜色、字体、焦点、动效与系统验证生命周期。风险集中在小屏、大字号及页面转场时的高度和滚动归属。
- 仍需澄清的问题：无。

## 设计材料

- 设计依据：根 `PRODUCT.md`、`DESIGN.md`、`src/styles.css`、`src/recovery-experience.css`；参考基线为用户提供的欢迎页截图和本次确认的标准／短内容／长内容三态效果图。
- 原型路径、版本／内容摘要、启动说明、必要依赖：确认稿 v1 本地路径 `/Users/zhouding/.codex/visualizations/2026/09/20/01a0be08-b2a0-7551-b365-96050d65f240/quiet-room-page-layout-spec.html`；无产品运行时依赖，不纳入公共产物。
- 流程图源码路径、版本／内容摘要及查看方式：[`flow.md`](./flow.md)，Mermaid 源码可由支持 Mermaid 的 Markdown 查看器渲染。
- 已自查的主要路径、状态、尺寸／主题：确认稿覆盖标准、短内容、长内容；正式实现计划覆盖 390×844、375×812、短视口、浅色和深色。
- 模拟能力、未覆盖项和真机限制：设计稿不模拟通行密钥、恢复材料或系统键盘；桌面浏览器视口不能替代实体 iPhone／iOS 27／Safari 验收。

## 确认与授权

| 节点 | 版本及范围 | 用户确认时间、最小脱敏摘要与可核实来源 | 下一步权限／未决项 |
| --- | --- | --- | --- |
| 第一次：需求方案 | v1：梳理同类页面并统一图标、标题、说明、底部操作区及长内容降级 | 2026-09-20 当前会话，用户提出四项版式规范并要求先梳理及给效果图；无外部消息定位 | 制作确认稿 |
| 第二次：原型与流程 | v1：标准内容、短内容、长内容三态效果图；长内容先去图标，仍不足则正文滚动、操作区吸底 | 2026-09-20 当前会话，用户回复「按照此方案实施」；无外部消息定位 | 已授权正式实施与既有持续远端交付；不含生产发布 |
| 变更／本次例外 | 无 | 无 | 无 |

## 验收与交付

| 验收项 | 对应流程／原型 | 实现与验证证据 | 差异／未验证项 |
| --- | --- | --- | --- |
| 标准页图标、标题及底部操作区对齐 | `flow.md` 标准内容分支／确认稿 A、B | `gateway-intro` 共享骨架统一 20px 左边界、图标与标题起点、52px 主按钮、44px 次按钮和 30px 底部安全距离；`tests/local-history-backup.e2e.mjs` 比较欢迎、创建、备份、导入四页几何并通过 | 实体 iPhone 待验收 |
| 长内容去图标，正文可滚动且操作区完整可见 | `flow.md` 长内容分支／确认稿 C | 恢复中心使用 `gateway-intro-dense`；390×844 与 390×520 均验证图标隐藏、标题上提、内容区独立滚动、操作区完整可见 | 大字号真机待验收 |
| 小视口自动进入紧凑态，不横向溢出 | `flow.md` 紧凑分支 | 390×520 与 844×390 自动隐藏图标并上提标题；浏览器断言无横向溢出，390×620、浅色、深色及横屏截图已人工核对 | 系统浏览器工具栏由浏览器控制 |
| 原业务、恢复及认证行为不变 | 全流程 | `npm run check:full` 通过：80 个测试文件／658 项测试、37/37 浏览器脚本；本次只调整入口页 DOM 分组、样式与布局回归 | 不涉及生产发布 |

- 提交前材料检查：需求记录与 Mermaid 流程源码已保存；确认稿为本机轻量材料，不提交大型媒体；差异未包含凭据、真实消息、恢复码或生产配置。
- 本地验证／浏览器／真机：2026-09-20 在任务工作树运行 `npm run build`、`node tests/local-history-backup.e2e.mjs` 与 `npm run check:full` 均通过；完整回归为 80 个测试文件／658 项测试、37/37 浏览器脚本。短屏自动断言使用 390×520，已人工核对 390×844 浅色／深色、390×620 短屏及 844×390 横屏截图。未进行实体 iPhone／iOS 27／Safari 验收。
- PR／CI／实际交付终点：PR [#184](https://github.com/zdaiwmm/shui-IM/pull/184) 的精确 head `0155ba4fbc5a3e3f9048c16a4b66e07364214045`，CI [35503440309](https://github.com/zdaiwmm/shui-IM/actions/runs/35503440309) attempt 2 全部通过。attempt 1 的短屏断言依赖字体换行，被修正为 390×520 确定溢出夹具；通话浏览器组伴随 WebSocket `ECONNRESET`，仅失败作业有界重试后通过。等待 main 合并回查；生产不在本次授权范围内。
- 接续所需材料、尚缺证据和下一步：以本记录、`flow.md`、分支 `codex/unified-intro-layout` 及 PR #184 为准；剩余远端证据为合并 SHA，真机证据仍缺失。
