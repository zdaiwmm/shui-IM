# 全需求确认与原型轻量保存规则

## 身份与方案

- 编号：2026-09-15-confirmation-rules；类型：协作规则／文档，不涉及产品页面。
- 记录冻结阶段：本地文档验证通过，远端交付结果以本分支关联 PR 为准；规则来源及起始基线 `b4f7fe276ae49a7abe7104af4e81e0440c395455`。
- 分支：`codex/requirements-confirmation-rules`；实际目录与 HEAD 由接续会话核验。
- 目标：防止直接按推测实施；页面需求先原型与流程确认，跨电脑可接续且控制 Git 材料增长。
- 范围：入口、工作流、维护／地图／决策／状态文档及最小模板。排除产品代码、CI 策略、自动审批脚本、历史重写、Git LFS 和生产发布。
- 方案：以工作流为唯一详细流程，入口强调门槛；保存最小版本与确认记录。非页面需求确认后实施，页面需求另经设计确认；持续交付在正式实施获准后启动。
- 风险／限制：仓库指令不是强制执行锁；旧电脑需同步、活动会话需重读；没有跨电脑运行或真实需求试运行证据。
- 原型／业务流程图：不适用，本需求不新增或优化产品页面。

## 确认与授权

- 方案版本：本任务会话的完整审计方案，加随后“必要源码与确认记录、大型媒体默认不进 Git”的修订；执行文本见[工作流](../../context/workflow.md#需求澄清与设计确认)。
- 用户确认日期：2026-09-15；最小摘要：“按照此方案实施”。依据为当前任务用户消息及其前序方案讨论；未提供独立消息链接。
- 授权：仅落地协作文档和模板，按已有 PR／CI 流程合并 main；不修改产品功能、不发布生产。第二次页面设计确认不适用，无其他跳过例外。

## 验收

| 验收项 | 核验方式 | 当前证据 |
| --- | --- | --- |
| 所有变更先确认，页面另经设计确认 | 对照入口、任务模式、执行顺序和交付启动条件 | 2026-09-15 文档审阅通过 |
| 设计权限、确认版本与接续可区分 | 审阅模板和纯问答、简单修复、页面、否决、变更、换电脑、例外、发布场景 | 2026-09-15 文档审阅通过 |
| Git 仅保存必要轻量材料 | 检查差异体积、文件类型及轻量保存条款 | 八个 Markdown 文件，约 21 KB 文本增量，无二进制／素材／依赖／产物 |

- 本地验证：`node scripts/check-docs.mjs` 通过；复用 `checkDocumentation({files})` 显式检查全部八个变更文件通过（默认入口未覆盖工作流与新模板）；`git diff --cached --check` 通过。自定义调用首次用标准输入触发入口路径错误，改用 `node --input-type=module -e` 后通过，未修改检查器。
- 本次只做文档验证，不执行本地产品全量测试或真机测试；PR 必需 CI 按现有策略执行，CI／合并结论从本分支关联 PR 回查，不以本记录推断。
- 交付接续：已有 PR 时继续同一 PR 的 CI／合并步骤，不重复实施；已合并则读取包含本规则的 main。生产不在本次范围内。

## 场景审阅（文档推演，非工具强制验证）

| 场景 | 规则要求 |
| --- | --- |
| 纯问答／只读审计 | R 模式回答，不创建记录或工作树 |
| 简单修复 | 可以一轮方案确认；不能自行免确认 |
| 页面新增／优化 | 需求确认后仅 S 设计，原型／流程通过并获实施授权后进入 I |
| 原型被否决 | 保持设计阶段，修改受影响材料再确认，不能进入正式实施 |
| 实施中业务或视觉变化 | 暂停受影响部分，更新版本并重新确认 |
| 跨电脑接续 | 核对规则与需求版本，确认缺失标未知；已确认部分不重复询问 |
| 用户明确本次跳过 | 记录精确例外范围，不改变长期默认及生产门禁 |
| 请求生产发布 | 仍需确认本次精确 SHA；需求或原型认可不授权发布 |
