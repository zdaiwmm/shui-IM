# v8 双人恢复与聊天记录本地备份

## 确认与授权

- 2026-09-16，创建本任务的交接消息明确确认 v8 原型、要求正式实施并完成本地验证与提交；不推送、不建 PR、不合并、不发布。
- 来源任务：`01a0a456-5cc4-7971-bfe2-59c0a40cbbf1`。原型位于该任务的 `quiet-room-recovery/prototype/index.html`，交接 `05_handoff.md`，审计 `audit-v8/REPORT.md`。原型中的认证、恢复与备份为模拟，不作为实现证据。
- 规则与起始基线：`d7b33b1d5c63718cc71d30626006588e45d10997`；开工时 GitHub main、本机 main、origin/main 一致。
- 本任务独立 worktree：`/Users/zhouding/ss/b692/ss`；分支 `codex/recovery-local-backup-v8`。

## 范围与实现计划

创建主线、邀请状态与庆祝、可选遮蔽演练、入口卡片和盾牌；双方分别输入恢复码，支持单方或双方恢复及范围变更重新确认；本人本地聊天备份的压缩、加密、原字节附件封装与导入。沿用 DESIGN.md tokens 和组件，不复制原型评审工具。

现有 QR3 自动服务端备份依赖可信在线设备批准 MLS 替换，不能满足双方均丢失访问时的恢复。旧协议、轮换、自动同步和历史恢复兼容入口需保留。新增流程不得把旧单码归档宣传为二取二保护，也不得把旧码和已复制密文远程失效当作能力。

本地归档需要独立版本、域分离密钥、分块认证、完整结束标记、资源上限与身份约束；只封装当前设备权限内的内容，完整校验后幂等导入，保留删除投影，不推进 MLS 或网络游标。附件不得重编码。

## 验收与当前状态

- 当前：本地归档与独立页面基础已实现，完整需求尚未完成。已确认原型源码轻量保存为 [prototype-v8.html](prototype-v8.html)，可直接在浏览器打开；保留模拟标识，不作为正式产品实现。
- 已验证：4 项归档格式单元测试；Chromium 合成 5 MiB 多分块视频原字节、零网络导入、错误身份/角色/空间、损坏文件不导入、幂等、删除投影、游标不变；375/390 宽页面实际生成文件、44px 控件与锁定清理通过。
- 完整门禁：`npm run check:full` 通过，构建通过、650 项测试通过、36/36 浏览器脚本通过（浏览器阶段 437.52 秒）。首次沙箱端口 EPERM 属环境失败；提权重跑后新增浏览器脚本引起测试清单数量断言失败，已按实际清单修正并定向通过，再完成本次完整门禁。旧自动备份生命周期、统一恢复、备份界面包含在通过清单中。
- 真机：未验证。CI、远端交付和生产：不在本次授权范围。

## 待决边界与后续范围

原型没有定义双方均失去访问时，重建 MLS 群组如何处理各自其他已登录设备。已提出选择：保留身份、下次进入明确确认后重新加入新群组；或本次恢复撤销其他设备后重新添加。尚未收到本任务答复，不能推定撤销授权，也不能用旧在线批准协议冒充无旧设备恢复。

双人恢复、新恢复码保存/双方准备状态、范围变化重新确认、创建/邀请状态与庆祝、可选遮蔽演练、保存入口与盾牌、旧备份隐藏入口，均仍待后续实施。当前旧自动备份协议/入口原样保留；本地归档暂沿用本人旧归档谱系密钥，不声称新的二取二历史保护。

本地备份只封装完整本机缓存原件，缺失明确计数；若最终要求自动补取服务器可用附件，应补充导出阶段受控取件、失效/限速与缓存配额处理后复验，不能默称当前实现已覆盖全部原件。

## 本地评审

在本任务工作树运行 `npm run dev`，通过启动输出的本机地址打开应用；准备合成会话及恢复码后，在聊天菜单打开“聊天记录本地备份”。不要迁移生产保险库、证书或真实消息。无真实会话的自动演示可运行 `node tests/local-history-backup.e2e.mjs /private/tmp/quiet-room-local-backup-ui.png`，使用临时合成数据验证并输出页面截图。

原型文件 SHA-256：`5feb89856dee16970ed7c818e5a6ef71e3e060f70569626ef40283baa12f3f49`。未纳入截图、压缩包、node_modules、构建产物、备份文件或凭据；新增原型源码约 125 KB。
