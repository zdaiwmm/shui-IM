# v8 双人恢复与聊天记录本地备份

## 确认与授权

- 2026-09-16，创建本任务的交接消息明确确认 v8 原型、要求正式实施并完成本地验证与提交；不推送、不建 PR、不合并、不发布。
- 2026-09-17，续作授权要求完成全部 v8 范围：创建/邀请/遮蔽/入口卡片/盾牌、双人恢复、本地备份，以及旧自动备份隐藏兼容；旧设备在双方确认后需要重新授权。
- 2026-09-17，iPhone 复验要求新页面沿用现有 Liquid Glass 按钮、控件、透明度和纹理；修复导航文字穿透；邀请页只展示不超过 10 位的邀请编号但不得缩短实际安全能力；邀请方设置访问密钥必须直接触发系统验证。
- 来源任务：`01a0a456-5cc4-7971-bfe2-59c0a40cbbf1`。原型 `quiet-room-recovery/prototype/index.html`，交接 `05_handoff.md`，审计 `audit-v8/REPORT.md`。
- 规则与起始基线：`d7b33b1d5c63718cc71d30626006588e45d10997`；独立 worktree `/Users/zhouding/ss/b692/ss`，分支 `codex/recovery-local-backup-v8`。

## 范围

创建主线、邀请状态与庆祝、可选遮蔽演练、入口卡片和盾牌；双方分别输入恢复码，支持只恢复我/对方/两人及范围变更重新确认；本人本地聊天备份的压缩、加密、原字节附件封装与导入。沿用 DESIGN.md tokens 和组件；页面画布保持不透明，玻璃材质只用于有边界的按钮、状态卡与弹层。

现有 QR3 自动服务端备份作为隐藏兼容入口保留（`#legacy-recovery`、`#legacy-backup`），不得在新显性流程中宣传为二取二保护。

## 旧设备取舍

双方签名确认同一恢复范围后，服务端原子撤销全部旧活跃/待加入设备。协助方保留本机历史并更换令牌与 MLS。不承诺远程销毁旧密文。见 [D-041](../../context/decisions.md#d-041双人恢复后其他旧设备必须重新授权)。

## 验收

- 本地归档、双人恢复协议、创建/邀请/遮蔽/入口与隐藏兼容入口均已接入产品。
- 首次访问无本机会话时不再经过旧三选一网关或遮蔽首页，直接进入原型欢迎页：创建空间、设置访问密钥、邀请页二维码+链接+对方状态、打开链接加入、庆祝后可选遮蔽。
- 首次创建、恢复和粘贴邀请的网关页面直接替换，不保留上一页文案参与动画；其他产品子页面继续沿用既有方向动画与不透明目的画布。邀请页展示 10 位邀请编号；二维码、复制与分享仍使用包含完整高熵能力的真实链接。受邀方的系统访问密钥请求在可信点击栈内启动，且非受信任 HTTPS / 不支持 WebAuthn 时显示明确原因。
- 2026-09-17 本工作树：`npm run check` 80 文件 / 653 项；浏览器组 1（1/1）与组 2（36/36）均通过，合计 37 脚本。页面截图覆盖欢迎、访问密钥和邀请页，确认网关旧文案不再与目的页同时绘制。
- 真机：用户已在局域网 iPhone 上指出旧流程未对齐原型、旧文案穿入目的页及受邀方无法调起系统访问密钥；本轮已修复并将 5173 重启为经开发 CA 验证的 HTTPS，待用户刷新复验。每台参与手机必须安装并信任该 CA，绕过证书警告不构成 WebAuthn 安全上下文。实际系统文件保存、多 GiB 压力：未验证。CI、远端交付和生产：不在本次授权范围。

## 本地评审

在本任务工作树运行 `npm run dev`，通过启动输出的本机地址打开应用。不要迁移生产保险库、证书或真实消息。

隐藏兼容入口：解锁后将地址改为 `#legacy-backup`；无本机会话时使用 `#legacy-recovery`。

原型文件 SHA-256：`5feb89856dee16970ed7c818e5a6ef71e3e060f70569626ef40283baa12f3f49`。
