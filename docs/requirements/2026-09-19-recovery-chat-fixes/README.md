# 2026-09-19 恢复码、邀请页与创建后聊天报错

## 身份与当前阶段

- 需求编号／标题：2026-09-19-recovery-chat-fixes／恢复码展示、填写弹窗、设置密钥间距、邀请按钮字号、创建后 MLS 欢迎
- 当前阶段／下一步：已授权实施
- 规则来源 SHA／任务起始基线／分支：GitHub `main` `ce902e8ae31698e32d4b6d7b53cb6431264cc8cb`；分支 `codex/v8-recovery-chat-fixes`
- 实际任务目录与 HEAD：`/Users/achilles/Documents/Codex/shui-IM-worktrees/v8-reverify-12`（本会话复用；上一胶囊任务已合并）
- 需求类型／是否涉及页面：混合（功能缺陷 + 既有页面间距／字号）。用户要求直接修复，本次跳过新原型。

## 方案

- 目标：通行密钥验证后能进入本设备恢复码页；恢复码填写弹窗贴在键盘之上且输入与提交更近；设置访问密钥的主按钮与返回间距对齐欢迎页；邀请页复制链接字号与同类主按钮一致；创建并进入聊天后不再因重复 Add 欢迎报错。
- 范围／排除项：不改 D-041、不改胶囊爱心、不发布生产。
- 入口、操作、状态变化及异常边界：我的恢复码页验证通行密钥后进入展示页；填写弹窗随可见视口；欢迎页与设置密钥页主/次按钮间距 16px；创建者 MLS 已含加入者时不再提交 Add。
- 实现思路：恢复流页面不跳过设备验证保护；弹窗钉到 visualViewport 底部；欢迎 `gap: 16px`；主按钮 `font-size: 1rem`；`prepareCreatorWelcome` 发现加入者已在组内则保持 active。
- 仍需澄清的问题：无

## 设计材料（非页面需求注明不适用）

- 设计依据：沿用现有 gateway / recovery / pairing 组件与 `DESIGN.md` token；不新增视觉方向。
- 原型路径：本次用户要求直接实施，不适用。
- 流程图：既有恢复与创建流程，行为保持。
- 模拟能力：系统通行密钥与真机键盘由测试夹具近似，不作为 iPhone 真机证据。

## 确认与授权

| 节点 | 版本及范围 | 用户确认时间、最小脱敏摘要与可核实来源 | 下一步权限／未决项 |
| --- | --- | --- | --- |
| 第一次：需求方案 | 五条缺陷按真机描述修复 | 2026-09-19 会话：列出四条 UI／恢复问题后追加创建聊天 MLS 报错，并要求「刚才说的问题也要修复」 | 正式实施与远端合并；不发布生产 |
| 第二次：原型与流程 | 不适用：既有页面缺陷，用户要求直接修复 | 同上 | 已授权实施 |
| 变更／本次例外 | 跳过原型 | 用户明确要求修复全部列出项 | 仅本批 |

## 验收与交付

| 验收项 | 对应流程／原型 | 实现与验证证据 | 差异／未验证项 |
| --- | --- | --- | --- |
| 恢复码页通行密钥后展示 | 我的恢复码 → 验证 → 本设备恢复码 | `withDeviceVerification` 对 `.recovery-flow-page` 启用保护；system-surfaces 夹具 | 实体 iPhone 待用户刷新复验 |
| 填写弹窗在键盘之上、输入贴近提交 | 恢复私密空间填写弹窗 | visualViewport 钉住 overlay；空错误区不占位；browser.e2e 短视口 | 真机键盘高度未测 |
| 设置密钥与返回间距 = 欢迎页 | 设置访问密钥 | `gap`/`margin-top` 16px；browser.e2e 对比欢迎页 | |
| 复制邀请链接字号 | 邀请重要的那个人 | `.primary-button` 1rem；browser.e2e 对比欢迎主按钮 | |
| 创建后聊天无 Add 已在组内 | 创建／对方加入后聊天 | 重复 `prepareCreatorWelcome` 不再 createCommit；mls.test | 用户现场房间未读 |

- 生产若不在范围内写明不适用。
