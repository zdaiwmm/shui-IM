# 2026-09-19 保存入口页与备份恢复改版

## 身份与当前阶段

- 需求编号／标题：2026-09-19-save-entry-pages／解锁自动通行密钥、入口页对齐欢迎页、备份/恢复数据
- 当前阶段／下一步：已授权实施
- 规则来源 SHA／任务起始基线／分支：GitHub `main` `140450f7817db21dcd3f674bca7d4a33b9b67d77`；分支 `codex/v8-save-entry-pages`
- 实际任务目录与 HEAD：`/Users/achilles/Documents/Codex/shui-IM-worktrees/v8-reverify-12`
- 需求类型／是否涉及页面：混合（既有页面布局、文案、备份/恢复交互）。用户明确要求跳过原型直接实施。

## 方案

- 目标：解锁页自动拉起通行密钥并保留共同恢复入口；设置密钥/邀请/恢复码/备份/恢复页对齐欢迎页按钮位置；胶囊左侧 TA、右侧我；聊天 toast 在胶囊下方；备份与恢复拆分并改为摘要弹窗。
- 范围／排除项：不改 D-041、不改胶囊爱心路径、不发布生产。
- 入口、操作、状态变化及异常边界：邀请页返回上一页（设置访问密钥）；备份「下载备份」验证通行密钥后走本机下载，不走系统分享；恢复选文件后弹窗，立即恢复再验证，进度在弹窗内。
- 实现思路：复用 `gateway-welcome` 底部 `welcome-actions`；本地备份增加分类计数与预览；toast 用独立位移定位。
- 仍需澄清的问题：无

## 设计材料（非页面需求注明不适用）

- 设计依据：沿用欢迎页与 `DESIGN.md` token；不新增视觉方向。
- 原型路径：用户明确跳过原型。
- 流程图：既有创建、解锁、备份、恢复流程，行为见方案。
- 模拟能力：通行密钥与系统下载由测试夹具近似，不作为 iPhone 真机证据。

## 确认与授权

| 节点 | 版本及范围 | 用户确认时间、最小脱敏摘要与可核实来源 | 下一步权限／未决项 |
| --- | --- | --- | --- |
| 第一次：需求方案 | v1 十项改版，并确认保留解锁页恢复入口、邀请返回上一页、去掉恢复码状态两行、备份走本机下载 | 2026-09-19 会话，用户逐条确认后要求「直接实施」 | 正式实施与远端合并；不发布生产 |
| 第二次：原型与流程 | 不适用：用户明确跳过原型 | 同上 | 已授权实施 |
| 变更／本次例外 | 跳过原型 | 用户明确「直接实施」 | 仅本批 |

## 验收与交付

| 验收项 | 对应流程／原型 | 实现与验证证据 | 差异／未验证项 |
| --- | --- | --- | --- |
| 解锁自动通行密钥且保留恢复入口 | 使用通行密钥解锁 | 白屏长按进入后 `autoUnlock` 拉起通行密钥；保留 `#recover-without-passkey`。隐藏/失焦与隐私返回不自动解锁。PR #180 CI 通过 | 真机待复验；main CI 旧密码页 holdCover 等待已在后续补丁修正 |
| 设置密钥按钮位置与字号 | 设置访问密钥 | `welcome-actions` 与欢迎页同 16px 间距；提示 0.95rem | |
| 邀请去锁、返回上一页、一屏 | 邀请重要的那个人 | 去掉 `#pairing-lock`，`#pairing-back` 回到设置访问密钥 | |
| 我的恢复码文案与底部返回 | 我的恢复码 | 新文案、无状态两行、底部查看+返回 | |
| 本设备恢复码布局 | 本设备恢复码 | 隐私说明移到副标题下，底部复制+返回 | |
| Toast 在胶囊下、动效更明显 | 聊天页 | `#notice` 固定在胶囊下 10px，入场位移加大 | |
| 胶囊左 TA 右我、半框居中 | 聊天顶栏 | 发送电弧从右侧发出 | |
| 备份数据摘要后本机下载 | 备份数据 | 摘要弹窗后通行密钥，`downloadBlob({ preferShare: false })` | 真机系统下载面板 |
| 恢复数据弹窗进度 | 恢复数据 | 选文件后摘要弹窗，验证后分类进度，完成回聊天 | 真机待复验 |
| 保存入口点击不缩放 | 上述入口页 | 欢迎/设置/邀请/恢复/备份页 `:active` 无缩放 | |
