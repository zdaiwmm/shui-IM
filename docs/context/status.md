# Quiet Room 当前状态

生产证据快照：2026-09-05 01:22:02（Asia/Shanghai）。本页用于接手定位，不代替实时 Git、CI 或生产回读；后续发布记录文档提交不代表应用重新部署。

## 当前发布候选

`codex/mobile-gesture-continuity` 基于 `f94b6f22a08823f383a62a82ccb5b15b5663fc4e` 的工作树已实现 D-020：聊天／保险箱标题逐帧固定、输入栏稳定后淡入、键盘松手收起、显露媒体阻尼下拉、语音自动提交状态、消息末行时间、无空帧页面进入、80px 遮蔽热区和有界原生前台交接。逐项入口及系统限制见[验收页](../../audit/2026-09-05-mobile-continuity/README.md)。

本地冻结工作树 `npm run check:full` 全程通过（44 文件、316 项、22／22 浏览器入口，浏览器 171.28 秒），原生通话两项通过，WebKit 生命周期、位置、时间线、遮蔽、语音和媒体专项通过。当前只是**本地自动验证通过**；本节尚不表示提交、PR、CI、合并或生产部署，真实 iPhone 仍未验收。生产仍为下节记录的应用提交。

## 已发布应用与范围

正式来源为 `https://ai.shui.click`。完整应用提交 `718f7d2451413a1e5e67e0d56ddeee15acd9f8d6` 已由 [PR #12](https://github.com/zdaiwmm/shui-IM/pull/12) 合并并部署，服务器 `deployed-at=20260904T172045Z`（01:20:45，发布批次时间），独立回读确认版本与发布目录一致。本轮包含下列时间线改动，并保留此前 PR #10 的 24 项交互修整。

- **聊天时间线**：时间与等宽待发／送达图标放在气泡内右下方；图片使用对比底色，文字、语音和文件保留紧凑尾行。设备本地日期在每个已加载日期的第一条消息上方居中，跨年显示年份，同日历史加载迁移分隔而不重复；日期不参与未读或消息序号。
- **回到最新消息**：按钮固定在输入栏上方，按真正末条下缘与按钮上缘渐显／渐隐；点击保留键盘，短距离缓行、长距离加快。存在未加载的新历史时保留入口并按原本机分页读取到最新，失败可重试；用户手势、引用定位、离页与锁定可中断。另修复冷尾部图片使恢复位置暂时不可达时过早结束，以及 WebKit 尺寸观察反馈。范围见 D-019 与 [TEST_PLAN.md](../../TEST_PLAN.md)。
- **聊天稳定性**：聊天可见时持续采样工具栏／键盘几何，固定控件直接更新位置；发送只平移消息内容，回执保留媒体节点；图片与视频卡片缩小并使用稳定尺寸。
- **媒体与保险箱**：打开媒体不先缩放；图片支持双指缩放与平移；已验证视频优先请求原生全屏，不支持或拒绝时保留原生控件回退；保险箱与聊天头部对齐，零项／未知数量无横线占位，标签保持同一组控件并平移切换，显隐使用透明度过渡。
- **语音**：自然放大、连续水平阻尼、156px 左滑取消，移除上滑锁定；取消立即停止采集和清理草稿，再收起空视觉形状。保留短按／键盘免手持入口、暂停、试听、继续录音和发送。
- **页面与系统交互**：普通解锁仅保留底部通行密钥按钮与按需错误；本机安全菜单移除通知／立即锁定快捷项；备份概览紧凑；页面与菜单淡入淡出；普通长按抑制原生菜单，保留文本编辑与主动选择文字。上传点击不误触遮罩，真实窗口失焦立即清理，选择器结果只能在原房间／设备按原流程解锁后续传。
- 额外修复排队滚动帧误关菜单、查看器返回后视口采样未恢复、手势清理和迟到全屏请求。24 项映射见 [交互验收](../../audit/2026-09-04-interactions/README.md)，长期契约见 [决策页](./decisions.md) D-017／D-018 与 [PRODUCT.md](../../PRODUCT.md)。

加密协议、原始附件逐字节校验、普通设备历史边界、恢复范围和部署流程保持既有契约。QR3 自动密文备份、恢复后换码、显式历史恢复与桌面隐私恢复沿用；服务器不保存恢复码。

## 验证与生产证据

- [最终 PR 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33898587091) 对应 `b09701623490ad5df892217a22b4ca4dd97db5d1`；[精确 main 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33899412990) 对应上述应用提交，合并前后源码树相同。两者均通过构建、311 项单元／集成、两组共 21 个浏览器入口、通话专项、凭据扫描、生产依赖审计及完整汇总任务。
- 最终应用代码本地 `npm run check:full` 全程通过（43 文件、311 项、21／21 浏览器入口，浏览器汇总 163.92 秒），两项通话专项与最终 WebKit 生命周期／回到底部／时间线通过；两引擎各 24 种布局、共 50 张合成截图，新时间线浏览器错误为 0，关键明暗／大字号／极端媒体／历史按钮画面已检查。
- 初轮本地回归发现冷图恢复偏移，随后 WebKit 暴露尺寸观察反馈；均修正应用并增加回归后重新通过最终验证，没有放宽位置断言或过滤浏览器错误。详见 [TEST_PLAN.md](../../TEST_PLAN.md)。
- 固定入口返回 `DEPLOY_OK` 与 `DEPLOY_VERIFIED`，精确 SHA 回执一致；切换前冷备份校验通过，公开 WebSocket 验证通过。成功入口总耗时 86.813 秒，服务器阶段 55 秒，不作为同条件提速对照。目录、冷备份、分段耗时及前两次只读门禁中止见 [发布记录](../../RELEASING.md#本次线上发布记录)。
- 01:22:02 独立回读确认维护标记消失、应用容器运行且健康、备份容器运行，两容器实际 Image ID 均匹配目标镜像。备份容器没有配置健康探针，未将运行状态写成探针成功。HTTPS `ok`／`database`／`storage` 全为 true；首页、Service Worker 和首页引用的 CSS／JavaScript 与容器产物逐字节匹配。检查前后部署与容器身份不变。
- 本轮发布通过命令行与 SSH 完成，临时使用防休眠；未人为锁屏进行端到端测试。自动化使用合成会话、虚拟通行密钥与合成媒体，不代表真实 iPhone 或独立安全审计通过。

## 仍需处理与系统边界

- **真机界面与权限**：真实 iPhone Safari 工具栏／键盘合成、滑动手感、系统权限、播放器和缩放手势尚未验收。网页不能预知原生合成器未公开的几何，不能强制工具栏／状态栏永久透明、静默授权或永久保留权限，也无法保证所有系统输入放大镜与原生视频手势行为。
- **后台与恢复**：本次回读 `admin-enabled=0`，后台未启用。`sao.shui.click` DNS／TLS、管理员因素初始化、真实通行密钥与恢复矩阵、灾备恢复及独立密码学审计仍待完成。旧 QR2 迁移条件见 [RECOVERY_BACKUPS.md](../../RECOVERY_BACKUPS.md)。
- **通话**：本次回读 `calls-enabled=0`，TURN 未启用；客户端可尝试直连，但公网／蜂窝互通、强制中继、长通话和切网尚未真机验证，见 [CALLS.md](../../CALLS.md)。
- **运维与安全门槛**：证书自动 DNS 续期、异故障域不可变备份与外部告警仍待完成，详见 [OPERATIONS.md](../../OPERATIONS.md)。公开高安全声明继续受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。
- **历史扫描误报**：本轮手动完整历史扫描命中两处旧测试公开 RFC 示例，已逐字节核实为测试向量／握手样例；未更改扫描规则、豁免或历史。正常主干完整 CI 已通过，但手动全历史扫描仍可能再次命中，出处和运行记录见 [发布记录](../../RELEASING.md#本次线上发布记录)。

## 接手复核

应用发布后本地 main 已同步上述应用提交，发布记录随后在独立文档分支更新。重新读取 `git status`、HEAD、GitHub main、精确 CI 与生产 SHA；工作树、本地验证、CI、真机和生产分别取证。正式来源绑定 WebAuthn 与本机历史，不能随意迁移；后续发布仍遵循 [RELEASING.md](../../RELEASING.md)，不得把本页旧 SHA 当作新的发布授权。
