# Quiet Room 当前状态

快照时间：2026-09-04 17:05（Asia/Shanghai）。本页记录本次发布候选的证据，不代替实时 Git、CI 或生产回读。

## 本次集成范围

用户已要求把同一工作目录中已完成任务统一审阅、同步 GitHub 并准备上线。整合基线为 `b6d817fcbdea5e9c53837cb26e3d8b428b7acab4`；候选分支为 `codex/integrate-completed-features-20260904`。改动原本已经共存于同一工作树，远端 main 没有额外分叉；重点是功能交互审查与统一测试。

- 任意非空、单个不超过 256 MiB 的文件附件，混合文件选择、加密分块、续传、下载完整性验证及创建者相册“图片 / 文件”分栏。
- 聊天键盘间距与上滑定位、在线状态布局、回应菜单/徽标、引用层级、玻璃边框和未读计数修复。
- 普通桌面浏览器的 30 分钟无操作期限及仅内存恢复，F 两秒与右下角一秒入口；移动/平板/PWA 保持严格策略。恢复重新解密已认证的持久化快照，不带回中断操作留下的未提交状态。
- 双人实时语音/视频通话、本机 MLS 身份与设备声明验证、加密信令、临时路由、原生媒体和页面测试，以及可选 coturn 配置与部署覆盖。
- 项目接手知识库、测试地图、安全语义与运维说明。

云端自动备份和通过恢复码取件仍是讨论方案，不属于本次已实现内容。

## 集成修复与文档一致性

通话不再复制和长期保留完整旧 MLS 私有棘轮状态；适配器只保留公开成员证明与通话信令必需的静态身份。真实 MLS 树验签、精确对象认证及对端身份声明仍保留，伪造或修改的通话快照失败关闭。

`SECURITY.md` 已同步桌面/移动隐私分流与未读首次注册语义。下载密文不等于用户读过；只有可见聊天推进已读游标。`CALLS.md` 已补齐，项目地图不再把它记为缺失。移动端前台通话暂停原有空闲锁；桌面通话仍按真实操作的 30 分钟期限锁定，媒体或网络更新不会续期。

浏览器独立测试页禁用 Vite SPA fallback，避免意外启动第二个应用实例干扰全局事件；桌面、移动、平板和 PWA 的测试身份明确区分。历史恢复夹具已适配异步连接初始化；文件菜单夹具补齐正式入口的遮罩样式，避免未定位的遮罩干扰滚动和菜单坐标。通话原生媒体及界面专项已加入 CI。

## 自动验证证据

本次集成工作树已通过锁定依赖安装、生产构建和 208 项单元/服务端测试。完整浏览器回归的各项脚本及通话专项均已通过；修复测试夹具后，对失败项和未执行的后续脚本分别补跑。CI 会在干净环境重新顺序运行完整套件，并保留凭据扫描和生产依赖审计。本地依赖审计请求发生网络超时，不计为审计通过；最终结果以本次 PR 和精确 main 提交的 CI 为准，不能用本页推断任意后续提交通过。

本地 Chrome 测试使用虚拟 PRF 认证器与合成音视频，不等于真实 iPhone/Android、Safari、公网 NAT 或强制 TURN 验收。不要把模拟浏览器结果升级为真机证据。

## 已回读的生产状态

本次通过固定 `deploy:doctor` 与只读 SSH 核验：

- GitHub 代码、Actions 和生产 SSH/helper 可访问。
- 当前线上应用仍为 `18cb2d4bb49c2026c7914bedbff362d3db81b6eb`，应用容器健康；尚不包含本次集成候选。
- 已安装 helper 为 root 所有、0755，摘要为 `c4cb9d53b15cd0997403c98259e78383a94702f54590ce36dd274eda98820d5e`，与本次含可选中继支持的候选文件不同。其原子更新是独立的受审阅运维步骤，普通应用发布不会更新它。
- `/opt/quiet-room/shared/calls.env` 与通话启用状态标记均不存在，当前 Compose 项目只有应用与备份容器。

没有 STUN/TURN 配置不等于客户端通话入口关闭：新代码仍可尝试可达网络的直连。公网/蜂窝跨网络可靠性、coturn 容器、云防火墙、强制中继、长通话和切网都尚待单独配置及实际验证。详细前提见 `CALLS.md` 与 `DEPLOYMENT.md`。

## 发布边界与接手事项

- 用户授权整合、审查、同步与准备发布；真正切换生产前仍按 `AGENTS.md`、`RELEASING.md` 确认最终完整 main SHA，并使用 `node scripts/publish.mjs --sha <40位提交号>`。不能发布未提交工作树或用旧 SHA 代替新目标。
- 公开“高安全”门槛没有通过；独立密码学/应用审计、独立可信客户端、真机矩阵和运营门槛仍见 `PRODUCTION_SECURITY_GATE.md`。这些与日常私有服务版本更新的证据不同。
- 证书自动 DNS 续期、异故障域不可变备份与外部告警仍是既有运维待办；本次只读预检没有改变这些配置，也没有重新验证证书续期或异地备份成功。
- 接手时重新读取 `git status`、HEAD、对应 CI、生产 SHA 与实际 helper/中继状态；原始发布记录在 `RELEASING.md`，不要从历史截图或测试文件存在推断上线。
