# Quiet Room 当前状态

生产证据快照：2026-09-04 20:19（Asia/Shanghai）。本页用于接手定位，不代替实时 Git、CI 或生产回读；后续文档提交不代表应用重新部署。

## 已发布版本与范围

正式来源为 `https://ai.shui.click`。已部署并回读确认的完整应用提交为 `22f752e1bf711c3d4b9c57abcc03eacdff027c61`，来自已合并的 [PR #3](https://github.com/zdaiwmm/shui-IM/pull/3)；服务器 `deployed-at` 为 `20260904T121727Z`。本次变更包含：

- 聊天键盘坐标同步、工具栏局部位移及滚动合帧；已读可见范围查找跳过屏外历史，减少长记录滚动开销。
- 紧凑等高状态栏、中性玻璃材质，以及按输入栏自身有界宽度计算的桌面输入框；移除消息旁悬停回复按钮，保留菜单回复。
- 创建者相册改为保险箱，每次进入默认模糊，首次点击显露单张、再次点击放大；眼睛图标显示全部／隐藏全部，离开或锁定后重置。
- 保险箱收为单行 44px 分类与操作控件，弱计数并入图片／文件分类；缩略图无提示与时间，放大详情显示对应发送／上传时间。

普通文件附件、桌面 30 分钟无操作期限及页面内存恢复、实时音视频通话应用代码和官方 npm 审计有限重试沿用上次生产版本，并非本次新增。产品与安全契约见 [决策页](./decisions.md)、[SECURITY.md](../../SECURITY.md) 和 [CALLS.md](../../CALLS.md)。云端自动备份及通过恢复码取件不属于本次已部署范围；`codex/session-recovery-backups` 开发分支未合并，也未被本次发布修改。

## 验证与生产证据

- [PR CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33871313999) 与 [精确应用提交的 main CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33871665048) 均通过构建、单元/集成、浏览器回归、通话专项、凭据扫描和依赖审计。精确 main CI 的官方 npm 首次审计取得有效 v2 零漏洞报告；该结论只对应当时检查，不外推后续提交或漏洞库变化。
- 本地最终 `npm run check:full` 通过（构建、228 项单元/集成及完整浏览器回归），补充的前端生命周期回归在 Chrome 和 WebKit 均通过；覆盖连续键盘帧、工具栏收缩、长历史有界读取、锁定清理、保险箱分页隐藏及桌面四宽真实滚轮固定位置。保险箱小屏、大字号、长计数和桌面聊天明暗截图已检查。详细范围见 [测试计划](../../TEST_PLAN.md)。
- 固定发布入口返回 `DEPLOY_OK` 与 `DEPLOY_VERIFIED`。独立回读确认应用提交一致、维护标记解除、应用容器健康、备份容器运行；HTTPS 健康、首页和 Service Worker 响应正常，首页引用的公开 CSS／JavaScript 与已部署构建产物逐字节匹配。WebSocket 检查来自发布程序的成功验证。
- 服务器发布程序沿用上次安装版本，本次未升级，独立回查摘要一致；发布前冷备份已校验。完整摘要、目录、时间和验证明细集中保存在 [发布记录](../../RELEASING.md#本次线上发布记录)。
- 浏览器自动测试使用虚拟认证器和合成媒体，不等于真实 iPhone/Android、Safari 或公网网络验收。

## 仍需处理的事项

- **界面真机验收**：本轮修改已通过自动测试、截图检查、CI 与生产回读；真实 iPhone Safari 键盘／工具栏同步和滑动流畅度仍未验证，不能由模拟 viewport 或桌面浏览器结果推断通过。
- **通话中继与真机验收**：`calls.env` 未配置，`calls-enabled=0`，TURN 未启用。客户端仍可尝试直连；公网/蜂窝跨网络、强制中继、长通话与切网可靠性尚未验证。详见 [CALLS.md](../../CALLS.md) 与 [DEPLOYMENT.md](../../DEPLOYMENT.md)。
- **macOS 发布路径**：临时目录别名可能使发布子程序静默退出。使用 `TMPDIR=/private/tmp` 运行同一固定入口已完成本次发布，源码路径规范化仍待修复；处理方法和成功判据见 [RELEASING.md](../../RELEASING.md)。
- **既有运维待办**：证书自动 DNS 续期、异故障域不可变备份与外部告警尚未完成；本次发布未改变这些配置。详见 [OPERATIONS.md](../../OPERATIONS.md)。
- **公开高安全门槛**：独立审计、独立可信客户端、真机矩阵及运营要求仍受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。

## 接手时复核

重新读取 `git status`、本地 HEAD、GitHub main、目标 CI 和实际生产 SHA；文档版本与已部署应用提交可以不同。后续发布仍须取得当次授权、确认完整目标提交并遵循 [RELEASING.md](../../RELEASING.md)，不能把本次提交号当作新一次发布授权。
