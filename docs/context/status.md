# Quiet Room 当前状态

生产证据快照：2026-09-04 20:56（Asia/Shanghai）。本页用于接手定位，不代替实时 Git、CI 或生产回读；后续文档提交不代表应用重新部署。

## 当前整合状态（2026-09-04）

- [PR #7](https://github.com/zdaiwmm/shui-IM/pull/7) 的 CI 与发布提速改动已合入 `main` 提交 `f6b02e07e0ea520fa7a3f25446a853e3215556a8`，[精确 main CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33878392552) 完整通过；该提交尚未部署。PR 完整 CI 实测 2 分 37 秒，对比相同应用代码的原流程 3 分 38 秒，缩短约 28%；这不是服务器部署提速实测。
- 独立分支 `codex/release-media-voice-20260904` 整合已结束的四项开发：键盘／工具栏与发送追随、视频预览和保险箱相册、备份页间距与聊天媒体隐私、长按录音／锁定／暂停试听。源共享工作树的基线为 `c4fa77f5a98147266cfa115564b5fbe050cc6bce`；完成后取得固定快照，未直接发布共享目录内容。
- 新增聊天隐私、相册加载、视频、语音手势四项已纳入浏览器完整入口（共 19 项）；CI 两组分别为 1／18 项。整合版本本地构建、311 项单元/集成、19 项浏览器分段覆盖和两项通话浏览器专项通过；锁定后旧语音绑定释放及重新进入回归已补齐。未将首次在语音时长断言超时的 `check:full` 记为单次全程成功；精确提交 CI 与生产部署仍需另行核对。阶段性开发证据见 [测试计划](../../TEST_PLAN.md) 及 [语音交互验收](../../audit/2026-09-04-voice/README.md)。
- 文档轻量绿色不能用于应用发布；固定入口要求精确 main SHA 的最新完整 CI。发布入口路径规范化与成功回执已经合入；服务器分段计时 helper 尚待独立安装和生产实测，冷备份、维护门与回滚边界保持既有契约。

## 已发布版本与范围

正式来源为 `https://ai.shui.click`。已部署并回读确认的完整应用提交为 `6753d010d822ef5cb90b2b3d2582d9fd1f7805c6`，来自已合并的 [PR #5](https://github.com/zdaiwmm/shui-IM/pull/5)；服务器 `deployed-at` 为 `20260904T125135Z`（20:51:35）。独立功能分支 `codex/session-recovery-backups` 的提交 `16b69c802f59558a754c1466508a07d54a51024e` 已经集成并发布。本次变更包含：

- 解锁联网时自动上传加密恢复包和本机历史归档；QR3 恢复码自动定位密文并在本地解密，移除手动恢复 JSON 下载/上传入口。
- 本机加密保管恢复码，重新验证通行密钥后查看；服务器和后台均不保存恢复码。
- 可信设备协助完成 MLS 身份替换后，持久化新码并转移全部历史归档，原子停用旧在线取件入口。响应丢失可复用原请求重试；等待替换时仅在服务器明确确认过期后才允许丢弃替代身份。
- 进入会话不自动导入历史；用户主动输入当前新码恢复历史消息或保险箱。保险箱独立恢复写入本机加密存储，不生成旧聊天气泡或回复预览。
- 后台代码提供按会话、设备和备份元数据管理，密码加 TOTP 登录及重新验证清理；生产管理入口当前关闭，见下文。

上次已发布的聊天键盘/滚动优化、紧凑状态栏、桌面输入布局及创建者保险箱默认模糊等界面改动完整保留。普通附件、桌面隐私恢复及通话应用能力沿用既有版本。产品与安全契约见 [决策页](./decisions.md)、[恢复备份契约](../../RECOVERY_BACKUPS.md)、[SECURITY.md](../../SECURITY.md) 和 [CALLS.md](../../CALLS.md)。

## 验证与生产证据

- 集成工作树本地 `npm run check:full` 通过（构建、241 项单元/集成及完整浏览器回归），`npm run test:calls` 通过（56 项及原生 DTLS、通话 UI）。恢复入口统一“保险箱”命名后重新构建并通过 `backup-admin-ui.e2e.mjs`。详情见 [测试计划](../../TEST_PLAN.md)。
- [PR CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33874161493) 与 [精确应用提交的 main CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33874534302) 均通过构建、单元/集成、浏览器回归、通话专项、凭据扫描和生产依赖审计；精确 main CI 验证任务于 `2026-09-04T12:50:41Z` 成功结束。
- 固定发布入口返回 `DEPLOY_OK` 与 `DEPLOY_VERIFIED`，切换前冷备份已校验。独立回读确认应用 SHA 一致、维护标记解除、应用容器健康、备份容器运行；HTTPS 健康、首页和 Service Worker 正常，公开首页及其引用的 CSS/JavaScript 与运行中构建产物逐字节匹配。未认证的合成未知备份取件请求被拒绝（401），未读取真实恢复材料。WebSocket 检查来自发布程序的成功验证。
- root 发布程序已独立审阅并原子升级至含可选后台持久覆盖的版本，核对属主、权限、语法及摘要，保留旧副本；发布后独立回查摘要一致。完整目录、摘要和时间见 [发布记录](../../RELEASING.md#本次线上发布记录)。
- `admin-enabled=0`：后台代码已随应用部署，`sao.shui.click` 尚未启用。管理员配置、域名独立证书及 Nginx 站点不存在，服务器解析器未取得该域名地址；需完成 DNS/TLS、密码与 Google Authenticator 初始化及生产后台验收。主站自动备份不依赖后台启用。
- 浏览器测试使用合成会话、虚拟通行密钥与合成媒体，不等于真机验收或独立安全审计。代码上线也不证明每台设备已有有效备份：仍需设备解锁、联网并完成首次成功上传。

## 仍需处理的事项

- **恢复与后台验收**：真实 iPhone/Android 通行密钥与恢复矩阵、真实 Google Authenticator 扫码、后台 DNS/TLS/维护门与管理员配置挂载验收、灾备恢复及独立密码学审计尚未完成。仅剩旧 QR2 文件与恢复码的用户须在迁移前用受信任旧版本恢复；完整兼容性和恢复条件见 [恢复备份契约](../../RECOVERY_BACKUPS.md)。
- **界面真机验收**：真实 iPhone Safari 键盘／工具栏同步和滑动流畅度仍未验证，不能由模拟 viewport 或桌面浏览器结果推断通过。
- **通话中继与真机验收**：`calls.env` 未配置，`calls-enabled=0`，TURN 未启用。客户端仍可尝试直连；公网/蜂窝跨网络、强制中继、长通话与切网可靠性尚未验证。详见 [CALLS.md](../../CALLS.md) 与 [DEPLOYMENT.md](../../DEPLOYMENT.md)。
- **macOS 发布路径**：上述旧生产发布使用 `TMPDIR=/private/tmp` 避开临时目录别名问题。CI 提速分支已加入源码路径规范化及发布完成回执，验证与采用状态见本页提速工作树记录；旧版本处理方法和成功判据见 [RELEASING.md](../../RELEASING.md)。
- **既有运维待办**：证书自动 DNS 续期、异故障域不可变备份与外部告警尚未完成；本次发布未改变这些配置。详见 [OPERATIONS.md](../../OPERATIONS.md)。
- **公开高安全门槛**：独立审计、独立可信客户端、真机矩阵及运营要求仍受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。

## 接手时复核

重新读取 `git status`、本地 HEAD、GitHub main、目标 CI 和实际生产 SHA；文档版本与已部署应用提交可以不同。后续发布仍须取得当次授权、确认完整目标提交并遵循 [RELEASING.md](../../RELEASING.md)，不能把本次提交号当作新一次发布授权。原有功能分支和独立 worktree 保留；未将发布记录写入用户的其他开发改动。
