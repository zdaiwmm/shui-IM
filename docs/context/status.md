# Quiet Room 当前状态

生产证据快照：2026-09-08 14:02:06（Asia/Shanghai）。本页用于接手定位，不代替实时 Git、CI 或生产回读；后续发布记录文档提交不代表应用重新部署。

执行规则更新（2026-09-08）：[固定交付流程](workflow.md#固定交付流程)收敛整条交付与失败续跑，
根 `AGENTS.md` 要求新会话先读本机 main 的规则并报告来源 SHA。规则与应用版本分开；
规则任务仅更新文档和本机入口，后续已随下述应用提交纳入生产源码。生效条件见[新会话入口](maintenance.md#新会话入口)，
流程加载不授予或继承其他任务的远端写入与发布权限。

## 聊天模糊接缝修正（2026-09-08，待本机集成）

- 基线及规则来源为本机 main `28dbe2fa109799a34b66b81ae6d354c6545d1cf1`；
  独占工作树 `/private/tmp/quiet-room-media-blur-seams`，分支 `codex/media-blur-seams`。
- 聊天遮蔽缩略图改为只绘制一个 cover 模糊层，边缘外扩与 48px 模糊半径关联；
  原图透明保留布局尺寸，揭示后恢复显示，避免不同裁切的前后景叠加形成色带。
  图片、组合图片和视频预览沿用同一入口，原始附件及显隐／隐私生命周期语义不变。
- WebKit 图片隐私专项通过，覆盖点击揭示、查看器、下拉遮蔽、迟到解码、回执重建和锁定清理。
  390px／1280px 明暗主题合成图像素比较中，相邻行平均亮度最大跳变从约 0.64–0.79
  降至 0.16–0.18（0–255 标度）；截图保存在 `/private/tmp/media-blur-qa`。
  修复前后合成图都未复现用户截图黑块，不能据此声称真机黑块已解决。
- `npm run check:full` 单次通过构建、61 个文件的 500 项测试及 26／26 浏览器入口（357.26 秒）；
  文档检查通过。本机集成结果待本任务收尾回填。iPhone / iOS 27 / Safari 真机待验收；
  GitHub 未推送，CI 未运行，生产未发布。共享源目录四个既有修改未纳入。

## 聊天标题栏修正（2026-09-08，未发布）

- 规则及开发基线为本机 main `5a2cae398c78449f23ac02ffb3476af7a1f18405`；
  独占工作树 `/private/tmp/quiet-room-chat-header-presence`，分支 `codex/chat-header-presence`。
- 在线点改为独立绿色，按用户对两版效果的反馈取中间值：2.4 秒一轮，适中的明暗／缩放差异和柔和光晕；离线／未知和减少动态效果不播放动画；
  顶栏与输入栏采用一致的普通／禁用图标颜色。触屏 Safari 顶栏移除 sticky 覆盖，
  使用既有 fixed 原点校正，避免受文档流及 sticky 容器边界影响。
- “最近上线时间未知”来自缺少内存中的离线转变时间：重启后、尚未观察到离线、
  或超过 10,000 个房间的内存容量淘汰时可能出现；普通重连不清空记录。
  本轮只解释该既有语义，没有新增行为元数据持久化，也未读取生产运行记录推断具体发生原因。
- 本地 `npm run check` 通过构建、61 个文件的 500 项测试；最终绿色样式追加后构建再次通过。
  完整 `npm run test:browser` 通过 26／26 个入口（324.58 秒）；WebKit 的
  `frontend-lifecycle.e2e.mjs` 与 `chat-bottom-control.e2e.mjs` 均通过，含明暗截图、
  底部往返滚动、在线／离线与减少动态效果、键盘和原生坐标校正。
  首次定向执行受沙箱端口限制，后获准通过；新增夹具已明确启用原生坐标分支，
  旧 sticky 类型断言按新定位契约更新，保留原几何断言。
- 源提交 `466d71c715639dc87e765d3776eddd16c3f74bf0` 完成后，锁内发现 main 已前进至
  `3b8eb4b72f2d6ea83917f88812b9573402130acd`。在独立工作树
  `/private/tmp/quiet-room-chat-header-integration`、分支 `codex/chat-header-integration`
  保留相册过渡／查看器边缘修复并合入本任务，组合提交
  `182a05611e09826e4439f54ac50370a0f025f48e` 的 `npm run check:full` 单次通过构建、
  61 个文件的 500 项测试及 26／26 浏览器入口（325.47 秒），WebKit 底部／键盘专项也通过。
  额外完整验证仅由 main 新增共享布局改动触发。
- 组合已快进本机 main。可信局域网 HTTPS 回读的聊天／通话 CSS 与候选逐字节一致，
  后端 ok/database/storage 均为 true；前后端 cwd 均为 `/Users/zhouding/quiet-room-local-main`，
  本轮通过 Vite 热更新交付，未重启后端。随后文档对账提交不改变已验证应用文件。
- 呼吸灯强度反馈最终采用 2.4 秒中间档，仅调整局部动画样式；`npm run check` 通过 500 项测试，
  WebKit `frontend-lifecycle.e2e.mjs` 通过，减少动态效果仍禁用动画；本次未重新生成截图。
  此次未重复跨页面完整套件；前述完整门禁对应原集成候选。
  调整提交 `8211240` 已快进本机 main，可信 HTTPS 返回的 CSS 与该候选逐字节一致，
  后端健康全部通过；前后端仍从 main 运行，无需重启。
- iPhone / iOS 27 / Safari 真机未验收，
  GitHub 未推送、CI 未运行、生产未发布；共享源目录的四个既有未提交文件未纳入。

## 图片属性与动图本机验收（2026-09-08，未发布）

- 用户已确认效果图并授权沿用当前视觉规范实施；Live Photo 明确暂不纳入。
  分支 `codex/photo-details-motion`，独占工作树 `/private/tmp/quiet-room-photo-details-motion`，
  基线及本轮规则来源为本机 main `2c91341e4e0ca93d72f7a4db33ee318a05684db4`；未同步远端。
- 保险箱相册的照片长按菜单新增查看详情，展示原图已有的 EXIF/GPS 与文件属性；聊天、视频和文件列表不增加属性入口。
  聊天与保险箱的 GIF、动态 WebP、APNG 保留原始字节，查看时自动播放，可暂时切为静态，重新打开恢复动态。
  属性解析及静态帧只在本机内存处理，关闭、翻页和隐私生命周期清理；具体边界见 `PRODUCT.md`、`SECURITY.md`。
- 冻结实现的构建和 59 个文件的 484 项单元／集成测试通过；WebKit 图片属性及动图专项通过，
  包含实际像素变化、暂停、重开、翻页、迟到解析和锁定清理。Chrome 的 25／25 浏览器入口全部通过。
  `npm run check:full` 首次因进程组清理 `kill EPERM` 中断；分组重跑时发现旧菜单数量断言，
  已改为核对 `details/pin/delete` 完整内容。产品实现未变，通过现有 runner 顺序续跑余下入口；
  上述结果为分段验证汇总，不声称完整命令单次零退出。
- 独立本地预览 `http://localhost:8797` 的健康、构建资源及受 CSP 约束的打包解析 worker 已回读。
  使用独立合成测试数据，不等于共享 main／局域网环境更新。生产依赖审计无漏洞。
- 用户随后授权集成本机 main 并更新服务。应用提交 `8dddb992c2aad0c6a487baf0a826d894a9f6fe44`
  已从上述基线快进集成，新增依赖安装完成；原手机可信 HTTPS 来源继续复用。
  原前端虽从 main 目录启动，但临时 Vite 配置覆盖 root 指向旧输入抖动候选；已停止该启动器，
  使用 main 的 `scripts/dev-lan.mjs` 正式入口重启。前后端 cwd 均为本机 main，可信证书 HTTPS
  健康回读的 ok/database/storage 全部为 true；前端回读确认属性入口、动态控制和解析 worker 已加载。
  这次只切换本机测试代码来源，没有迁移或覆盖测试数据。
- 六项视觉／交互反馈修正来自 `17f2662`：保险箱查看器移除下载，信息按钮使用顶栏玻璃材质；
  玻璃详情抽屉支持外围点击、下拉关闭及短拉回弹，620ms 上行／520ms 下行，入场中途关闭保持当前位置；
  隐私清理仍立即执行。进入保险箱的退场聊天图片在整段过渡中不可见，图片本身不因抽屉开关突然缩放。
- 修正源完整检查单次通过 484 项测试和 25／25 浏览器入口，WebKit 专项通过。
  本机 main 随后已含梗图任务，因此在独立工作树 `/private/tmp/quiet-room-photo-polish-integration`、
  分支 `codex/photo-polish-integration` 从 `d9b9d3e` 合入修正，保留梗图并检查共享页面生命周期。
  最终组合 `ab86c25626d082bd6e7cff8c10f24b1bee9624a1` 的 `npm run check:full` 单次通过构建、
  60 个文件的 494 项测试与 26／26 浏览器入口（321.04 秒），已快进本机 main。
  最终组合的 WebKit 图片专项也通过。原可信 HTTPS 来源回读确认手势、慢动画及过渡修正代码，
  前后端仍从 main 运行，健康全部通过。
- Chrome 明暗／横竖屏截图和真实触摸回归通过；本机自动化 WebKit 的最小独立 backdrop-filter 示例也未绘制模糊，
  其截图不能证明 iPhone 上的磨砂质感，真机需复核玻璃观感与闪图反馈。
- 后续四项反馈修正 `7cd4fa6b9cf337962e60bcac54696923b841f26f` 已快进本机 main：隐藏属性滚动条并保留滑动，
  顶部手柄／标题可向上拖至页面全屏；查看器边缘和系统主题色提示随当前明暗主题对齐，浏览器工具栏最终由系统决定。
  长按菜单先向已连接的查看器转交焦点再移除自身，并阻止同次长按迟到的 contextmenu 重开菜单；
  未放宽 blur、hidden 或其他隐私生命周期。iPhone 偶发进入遮蔽的原始故障尚未复现，不能据此宣称真机问题已根除。
  此提交的 `npm run check:full` 单次通过构建、60 个文件的 494 项测试及 26／26 浏览器入口（324.59 秒），
  WebKit 图片专项通过；覆盖全屏扩展、真实触摸、内容滚动、焦点交接和主题色恢复。
  原手机 HTTPS 来源经指定局域网解析并校验原根证书，健康及最新模块回读通过；本机环回同端口存在其他任务 HTTP 测试服务。
  独立 Chrome 新配置的 HTTPS 回读因未信任该本地 CA 失败，未绕过证书校验，不记为浏览器 HTTPS 验收通过。
- 截图反馈后的修正 `6af1bd39ed73726e5f006adc9c8dc2f4948ff6d6`：退场聊天只作为动画层，不再触发活动聊天的
  文档高度规则；进入保险箱时整段消息内容（含时间与回执）立即不可见，相册在过渡中保持满屏。
  新回归先在旧实现复现消息元信息外露，再确认修复后逐帧不可见；选择器保留原优先级，弹窗滚动锁定回归通过。
  根据真机视觉反馈移除查看器顶底额外色带，恢复始终透明的 theme-color 提示，保留玻璃按钮；这替代上一版不透明色值方案。
  最终文件树构建、61 个文件的 500 项测试通过；浏览器前 22 项通过后，第 23 项 Chrome 启动超时（用例尚未执行），
  随后用现有 runner 续跑末 4 项全部通过，合计 26／26，不能记作完整命令单次通过。WebKit 图片专项及明暗截图通过。
  本机 main 新增的其他任务验收文档已保留；持有本任务集成锁从 `5a2cae3` 快进修复，原可信 HTTPS 来源校验证书后
  健康与实际 CSS／模块回读通过，前后端仍从 main 正式入口运行，前端热更新无需重启。iPhone 修复后观感仍待用户复核。
- 本轮修正的 iPhone / iOS 27 / Safari 真机未验收，CI 未运行；GitHub 未推送，生产未发布。
  保留任务工作树用于后续验收，原共享目录既有修改及旧输入抖动分支未纳入；以下线上记录不代表该候选已部署。

## 当前发布与集成状态（2026-09-08）

### 全屏梗图改版候选

本轮基线及规则来源 `22b763465b0eb05b8575eab8ca46b4dd21afee27`，独占工作树
`/private/tmp/quiet-room-meme-fullscreen`、分支 `codex/meme-fullscreen`。
用户确认全屏液态玻璃弹窗、移除横杠、默认网络标签、直接联网、热门及分类，
并确认将[有条件自动集成](workflow.md#有条件自动集成)作为持续本机授权固定到流程。
实现复用现有玻璃变量和对话框焦点隔离，暂停被遮挡聊天的可见已读推进；保留原草稿与独立加密收藏。

默认网络源已接入 Imgflip 官方免密钥热门目录，关键词按名称和中文别名／分类过滤当前热门图库。
这不是全网检索，也不是完整中文斗图库；ALAPI 未配置，中文免费候选接口实测失效或超时。
真实联调确认热门首屏24项、JPEG下载、分类、中文别名、长按预览；
Chrome 320／390／1440全屏几何与深色截图已核验。自动化测试仍用合成夹具隔离外网。
用户随后提出应扩展为 Telegram 式 Stickers / GIFs；该方向正在讨论，不能把当前热门模板代理称为完整实现。
源候选 `83b4891` 的完整检查通过500项测试和26／26浏览器入口（315.64秒）。
开发期间 main 前进到 `7cd4fa6`，在独立 `codex/meme-fullscreen-integration` 工作树保留图片查看器修复，
最终产品组合 `33a11bf` 完整检查通过构建、61个文件的500项测试及26／26浏览器入口（326.64秒）。
随后只合入 `c25ab4b` 的验证文档，产品文件不变；集成 `2c1fe2ac92aaa9cfd308a0cc6db464808ac608bc`
已按有条件自动集成规则、持有公共 Git 目录互斥锁快进本机 main。
正式 LAN 启动器已重启以加载新后端，原来源经实际局域网连接并校验主机名和 CA，
健康 ok/database/storage 与全屏模块、玻璃 CSS 回读通过；环回同端口的其他测试服务不作为本机入口证据。
独立 WebKit 真实上游分类、中文别名、预览、三个全屏视口及深色检查通过；仍不代替 iPhone 真机验收。
没有 GitHub 同步或生产发布授权，未推送或发布。贴纸／GIF 完整来源及素材类型扩展尚未实施。

### 梗图初版与本地集成记录

2026-09-08，独立工作树 `/private/tmp/quiet-room-meme-picker`，分支 `codex/meme-picker`，
起始基线 `2c91341e4e0ca93d72f7a4db33ee318a05684db4`。实现和限制见 [MEMES.md](../../MEMES.md)。
本地面板、聊天图片收藏、加密独立副本、直接发送和长按预览已存在；网络搜索仅完成客户端接口，
ALAPI 代理因具体外发目的地授权待确认而未写入，凭据及真实来源联调也未完成。
用户已确认收藏仅本机、原消息删除后继续保留；不增加跨设备同步或恢复备份。

`npm run check:full` 通过构建、59 个文件的 483 项单元／集成测试和 25／25 浏览器入口（295.06 秒）。
之后修正发送过程中离开聊天的忙碌状态清理，并通过补充梗图专项和构建；专项验证合成搜索、
真实本机存储、发送取消及四个视口，已人工查看手机和桌面截图。本机独立预览前后端健康回读成功，
上述独立候选验证不代表共享 main 服务更新、iPhone 真机、CI、远端集成或生产发布。
用户随后授权本次合入本地 main 并准备手机验证；未授权 GitHub 同步或生产发布。

本次本地集成：从 `73faf411604d24f34c3421156206053df245147e` 在独立分支
`codex/meme-local-integration` 合入梗图候选 `e5eaa5d7899d926a5e25a781ded31075beb7a090`，
保留已有图片属性和动图控制；无文本冲突，修正两个任务合并后的浏览器清单计数。
最终组合 `npm run check:full` 单次通过构建、494 项测试和 26／26 浏览器入口（308.82 秒）。
应用集成提交 `ca475141494b7f2470ed31abcee7f8a12cf3fa1d` 已快进进入本地 main。
原 LAN 启动器的 5173 回读为 HTTP，已通过正式 `scripts/dev-lan.mjs` 入口重启；
前后端 cwd 均为 `/Users/zhouding/quiet-room-local-main`，原可信 HTTPS 来源的健康和梗图源码回读成功。
本次不更换来源、不迁移测试数据。手机真机待用户验收；真实网络搜索仍未接通。
初版交付时长期规则尚在征求意见；用户随后明确确认有条件自动集成，边界以上方流程链接为准。

### 已发布快照

- 已发布应用为 `18e07b08e3c1422ac693a7f98a6a66ac1277ef85`，版本 ID `2026.09.08.3`。
  [PR #46](https://github.com/zdaiwmm/shui-IM/pull/46) 已合并并发布，包含
  [PR #44](https://github.com/zdaiwmm/shui-IM/pull/44) 工作流文档；应用及测试文件与已验证候选一致。
- 页面导航结束不再重复淡入，聊天及相册媒体遮挡统一为 48px 模糊。
  本地完整验证通过 58 个文件、473 项测试、24／24 浏览器入口（292.01 秒），
  WebKit 页面切换、原生通话和通话界面专项通过。
  [PR 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34187013152) 与
  [精确 main 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34187412121) 全部成功。
- 固定入口核对精确成功回执，耗时 111,670ms；14:02:06 独立 `READBACK_OK`，耗时 2,759ms。
  版本、镜像、HTTPS 健康、公开与容器产物及 WebSocket 一致；维护标记不存在，
  应用运行且健康，备份运行且无健康探针，后台和通话均未启用。详见[本次发布记录](../../RELEASING.md#本次线上发布记录)。
- 本机 main 已从 `d60067c` 安全快进到本次应用提交；不据此声称本机服务或真机验收通过。
  本批没有新增 iPhone 真机证据；共享目录的既有未提交改动未纳入。

## 同步循环修复发布（2026-09-08 12:13，历史快照）

- 已发布应用为 `a493aa06459c86a89049978e1df218245cce3836`，版本 ID `2026.09.08.2`。
  [PR #43](https://github.com/zdaiwmm/shui-IM/pull/43) 已合并并发布，合并文件树与
  候选 `b49af39189ec1f95178aa370685fc9a5baae6b1a` 一致。
- 修复分页外旧消息回执反复请求同一游标的空同步、空同步重建相册，以及返回动画中的
  退场相册被误认为当前页面的问题。旧回执从本机加密历史读取，继续执行签名、消息绑定和角色校验；
  已持久化游标内的历史缺失时失败关闭，不跳过回执、不重复推进 MLS。
  空同步不刷新媒体投影，新增消息只刷新当前页面，不重开退场相册。
- 本地 `npm run check:full` 通过构建、58 个文件的 473 项单元／集成测试及
  24／24 浏览器入口（289.24 秒）。新增生命周期回归覆盖旧回执无循环、持久送达、
  显示页范围不变、无效签名拒绝、历史缺失停止、空同步保留相册与返回，
  以及返回动画期间收到加密新消息不重开相册。
  版本说明变更后的构建与更新提示专项也通过；没有新增 iPhone 真机验证。
- [PR 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34185394350) 与
  [精确 main 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34185773418)
  的构建、单元／集成、两组浏览器、原生通话、依赖审计、凭据扫描、文档与完整汇总全部成功。
- 本机 main 工作树从 `4b426f7` 安全快进至上述应用提交；未观察到双向分叉。
  本次只核验 Git 工作树，不把它记为本机服务回读或真机验收。
- 原共享目录的 `src/app.ts`、`src/chat-layout.css`、`src/styles.css` 和 `vite.config.ts`
  未提交修改保留原处，未纳入本次发布；不得从旧分支直接重放覆盖后续键盘实现。
- 固定入口返回精确 `DEPLOY_OK`／`DEPLOY_VERIFIED` 并核对回执，总耗时 387,651ms，
  其中等待 main CI 297,569ms。12:13:56 独立 `READBACK_OK` 确认版本、镜像、健康、
  公开与容器产物、HTTPS 和 WebSocket；独立回读耗时 3,108ms。
  应用运行且健康，备份运行且无健康探针；维护标记不存在，后台与通话均未启用。
  详见 [该次发布记录](../../RELEASING.md#历史线上发布记录2026-09-08-1213)。发布前实时预检为 `0d835dc`。
- 发布对账在独立文档工作树完成，文档提交不代表应用重新部署。下列真机缺口继续保留，
  不能用 CI 或生产回读成功替代；本次未取得用户设备的 WebSocket 关闭码。

## UI 集成来源（2026-09-08，已发布）

- `codex/ui-integration-release-20260908` 基于 `a493aa06459c86a89049978e1df218245cce3836`，
  完整合入页面闪屏修复 `8b35afac605c717a2a433c489c810c6adc55a15e` 和媒体模糊增强
  `94714618c77533035cc35419adb258fd49383e1a`。版本 ID 为 `2026.09.08.3`。
  随后同步主线发布记录 `db4afcc172b7ee218b13c2e88e47ffdb05a17de8`，仅解决文档冲突，应用文件树不变。
- 最终组合 `npm run check:full` 通过 58 个文件、473 项测试与 24／24 浏览器入口（292.01 秒）；
  WebKit 页面切换、原生通话和通话界面专项通过。审查未发现阻断问题，iPhone / iOS 27 / Safari 真机效果未验收。
  [集成 PR #46](https://github.com/zdaiwmm/shui-IM/pull/46) 已取得完整 CI、精确 main CI 与生产回读证据，见页首。
- 媒体模糊增强来源任务基于 `4b426f7`，将聊天图片、视频封面、相册缩略图及聊天遮挡背景统一为 48px 高斯模糊。
  来源任务的完整测试及 390/1280px 合成图像样式检查通过；已随本批发布。

## 页面过渡闪烁修复（2026-09-08，已发布）

- 分支 `codex/page-transition-flash-20260908` 从已核实的远端 main
  `4b426f7fe8fb8e68a54b82d8b7ddc72fb1fd9a1c` 建立，独立工作树位于
  `/private/tmp/quiet-room-page-transition-flash-20260908`。
- 浏览器逐帧复现滑入结束后内容透明度降至 `0.76`：临时过渡标记移除重新触发了内容淡入。
  修复让通过导航进入的页面持续跳过重复淡入，直接挂载页面仍保留原有淡入。
- 本地 `npm run check:full` 通过 473 项单元／集成测试、24／24 个浏览器入口
  （浏览器 289.66 秒）；WebKit 的 `tests/system-surfaces.e2e.mjs` 也通过。
  新增保险箱、设备管理、备份与恢复进入及返回聊天的逐帧透明度检查。
- 已随本批合并并取得精确 main CI 与独立生产回读证据，未取得 iPhone / iOS 27 / Safari 真机验证。

## iPhone 输入抖动复查证据（2026-09-07 历史快照）

以下是当时本地诊断记录。相关源码后续已进入 GitHub main（见上节），其中“当前候选”、
本机 main 和禁止推送的描述仅反映当时状态，不是本轮已有用户集成授权的撤销。

- 本机 LAN 基线为 `eac6a509c477064daf17dc8514ba3fc2f6b23602`；本机 main 当前停在首轮
  部分修复 `4787e0ef00addcb811d1725aa4bf992367b6722f`。后续候选位于独占工作树
  `/private/tmp/quiet-room-iphone-input-jitter-20260907`，分支 `codex/iphone-input-jitter-20260907`。
- 当前候选：iPhone 原生编辑区域同步增高、IME 临时缩行延到下一帧；底部输入和发送保持原生文档
  位置，用相对消息位移跟随；独立固定底边探针避免浏览器工具栏遮挡。弹起键盘提前定位最新消息，
  包含本地历史分页，消息内容以 300 ms 缓出上移，并在原生动画等待阶段保持起始位置；关闭时使用
  合成动画平移消息，输入栏在最终位置直接显现。
- 真机已确认：光标正常、历史位置输入改善、底部输入／发送抖动显著减轻；弹起及时到最新消息，
  输入栏不遮挡且收键盘后能及时出现。最新对照去掉列表重复的固定原点补偿，用户确认
  从历史打开后收键盘“下移连贯了”，五轮采集不再出现原先 376 px 回跳。
  本轮真机确认消息弹起不再先跳到终点再反向移动；放缓后的两轮几何采样连续到达终点。
  **仍未通过完整真机验收**：用户仍报告列表末尾闪动，标题是否独立闪动尚不能确定。
- 原生截图证明 DOM top=0 不能单独证明标题正确绘制。sticky 加独立原点补偿的候选已恢复；
  无补偿 fixed/top=0 的独立对照被用户确认标题会消失，未写入源码。聚焦绘制临时对照
  已补采实际操作，仍出现原点切换与闪动／回跳，已撤掉。临时对照都有自动恢复期限。
- 前一列表回跳提交 `68347cd` 的完整 `npm run check:full` 通过：55 文件、465 项单元／集成、24／24 浏览器入口
  （浏览器 291.12 秒）；WebKit 布局专项通过。此前间歇出现的相册返回 88 px 锚点问题已用
  迟到旧页面图片 decode 定向用例稳定复现并修复，原失败用例及完整入口通过，未降低断言。
- 本轮上移候选的 WebKit 专项通过，包含等待启动帧、连续视口变化接续和不变采样不重启动画；
  最新完整门禁正在执行，不沿用前一提交的通过结果。
- 候选保留待继续真机诊断，不集成本机 main，不推送或发布。标题自身坐标、取消合成提示和
  单独文档定位均未消除闪动；标题滤镜对照未获得明确改善结论，已自动恢复。详细过程和证据层级见
  [本地修复证据](../../audit/2026-09-07-iphone-input-jitter.md)。本条不改变下列生产记录。

## 九月移动交互修复发布（2026-09-07 08:57）

- [PR #37](https://github.com/zdaiwmm/shui-IM/pull/37) 已合并为
  `fd7a8ab7732afff98c4f0a6a3b60c31ea67bf213` 并发布。首次加入与新增设备绑定会在 WebAuthn
  结果返回后等待页面真实可见且聚焦，再继续会话创建、邀请领取或六位安全码步骤；隐藏、
  `pagehide`、`freeze`、锁定、入口替换和总期限仍失败关闭。
- 图片详情边缘缩放、回复左滑阻尼、备份与恢复页层级和蓝牙音频授权交接已随本批上线；未归属
  失焦和硬生命周期仍立即清理。图片拖至边缘的最小视觉尺寸为适配尺寸二分之一，回复位移上限
  为可见视口六分之一。协议、恢复权限、附件校验和设备历史边界未改变。
- iOS 发送滚动与延迟 `visualViewport` 更新期间，标题栏和输入工具栏按绝对目标固定；textarea
  伸缩、消息插入与列表位移仍共用逐帧动画。用户提供的 iPhone／iOS 27／Safari 专项真机证据
  覆盖多行输入、单行／多行发送和高度复位的布局稳定性；本机调试后端没有既有房间授权，因此
  该真机证据不证明真实网络送达。Tesla 蓝牙、首次加入／设备绑定修复后真机复测仍未完成。
- 精确 main [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34050348842)通过构建与
  单元／集成测试、两组浏览器回归、原生通话、生产依赖审计、凭据扫描、文档检查、
  `Full application verification` 和最终 `verify`。固定入口返回 `DEPLOY_OK`／
  `DEPLOY_VERIFIED`；08:57:28 独立 `READBACK_OK` 确认精确版本、镜像、健康、HTTPS、公开与
  容器产物及 WebSocket 一致。

## 输入框与列表联动发布（2026-09-06 21:03）

- [PR #35](https://github.com/zdaiwmm/shui-IM/pull/35) 已合并为
  `fc3c0cdd28f5acac81f32e30b8c57b28536b0f2c` 并发布。输入栏改为布局底边定位，输入框伸缩、
  新消息插入和消息列表位移由同一逐帧更新驱动；标题栏与输入工具栏保持固定。连续组词、伸展
  途中发送和多行发送后的高度复位从当前可见位置平顺衔接，阅读旧消息时保持锚点。
- 新增键盘视口、连续换行、发送打断与历史锚点的浏览器回归。未修改的 main 副本在新增历史
  锚点断言下出现约 44px 上移后跳回；修复候选的 WebKit 专项和最终本地 `npm run check:full`
  均通过，后者覆盖构建、54 个测试文件／454 项单元集成及 24／24 个 Chromium 浏览器入口
  （265.30 秒）。
- PR 首轮 CI 的浏览器第 1 组暴露既有保险箱返回用例在滚动账务提交前读取基线的竞态；产品
  断言保持不变，夹具等待两个动画帧后再记录同一消息与像素偏移。本地主浏览器流程、修订后的
  [PR 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34034272592)及精确 main
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34034568250)全部通过。
- 固定入口发布批次为 `20260906T130200Z`，返回精确 `DEPLOY_OK`／`DEPLOY_VERIFIED`；
  21:03:09 独立 `READBACK_OK` 确认版本、镜像、HTTPS、公开与容器产物、健康和 WebSocket
  一致。自动化、CI 和生产回读不替代真实 iPhone／Android 验收。

## 晚间集中发布（2026-09-06 19:37）

- [PR #31](https://github.com/zdaiwmm/shui-IM/pull/31) 已集中合并遮蔽焦点观察、回复与相册手势、
  已验证密文媒体缓存、语音文案与时间对比度、输入框布局动画，以及版本更新广播、提示条和
  版本说明；[PR #33](https://github.com/zdaiwmm/shui-IM/pull/33) 补齐生产 Docker 构建输入。
- 线上应用为 `981b21612b16bc5b9d1e4007ed051f17a3170256`。固定入口返回精确
  `DEPLOY_OK`／`DEPLOY_VERIFIED`，19:37:32 独立 `READBACK_OK` 确认版本、镜像、HTTPS、
  公开与容器产物、健康和 WebSocket 一致；详情见发布记录。
- 首次发布 `624abe606674140b0c3de5a44043c8b273ad5e02` 在 Docker 构建阶段发现缺少
  `release.json`，停止于维护门和生产切换之前；修复通过新 PR、新 main SHA 和完整 CI 后才
  发布，没有重试失败 SHA。
- 自动化、CI 和生产回读不替代真实 iPhone／Android 验收；下午交互修复的真机缺口不变。

## 晚间批次开发证据（2026-09-06）

- PR #31 于 19:10:55 合并为 `624abe606674140b0c3de5a44043c8b273ad5e02`；生产构建修复
  经 PR #33 于 19:25:50 合并，最终应用 SHA 为 `981b21612b16bc5b9d1e4007ed051f17a3170256`。
- 遮蔽长按进入 v3 网关后新增 1.5 秒静默焦点观察，覆盖 Safari 更新 `hasFocus` 却不派发
  `focus` 的短前台边缘；未聚焦绝不调用 WebAuthn，正常焦点监听、可重试按钮、请求中止与
  生命周期失败关闭保持有效。网页仍不能保证浏览器自有验证弹窗必然呈现。
- 回复左移上限改为实时可见视口宽度三分之一；照片双指缩小松开回到默认 1×，放大保留；视频
  详情与照片共用整屏容器。相册翻页先原子转交残余位移再刷新媒体归位，避免 WebKit 中间居中帧。
- 媒体明文和预览仍只存在于解锁运行时；服务器密文分块可留在本机 IndexedDB，再进入聊天时
  重新完成逐块认证、长度和 SHA-256 校验而无需重复网络下载，损坏缓存删除后回源一次。
- 语音蓝色态文案为“松手发送，左滑取消录制”，红色态为“松手取消录制”；消息时间使用满足
  小字号 4.5:1 对比度的浅灰。来源工作树 `npm run check:full` 已从头通过构建、52 个测试
  文件／422 项单元集成和 23／23 个顺序 Chromium 浏览器入口（257.91 秒）；来源测试本身不等于生产发布或修复后真实 iPhone 验收。
- 版本更新来源工作树已新增单一
  `release.json` 版本／更新报告来源、Service Worker 新版本广播、聊天顶部更新条及更新后首次
  进入聊天的有序版本说明。首次安装不误报更新；待展示状态跨遮罩页刷新保留；只在本机记录
  版本 ID，不修改消息、房间、设备历史或服务器元数据。该路径现已随本批发布并完成生产回读。
- 该来源工作树 `npm run check` 已通过 53 个测试文件、426 项测试；`npm run test:browser` 已通过新增
  版本更新专项在内的 24／24 个浏览器入口，汇总 255.59 秒。构建产物已核对 Service Worker
  版本占位符被当前发布 ID 替换。真实旧版到新版生产升级与真机均未验证。
- 输入框高度候选将 textarea 高度变化并入同一帧的聊天布局提交，保持标题栏、消息列表、输入
  工具栏和发送位移动画同步；任务治理候选补充只按审查清单清理已合并、干净且非活动状态的
  本地任务分支/worktree 命令。最终集中组合 `npm run check:full` 已从头通过构建、54 个测试
  文件／453 项单元集成和 24／24 个 Chromium 浏览器入口（259.98 秒）；文档与差异格式检查通过。
  Docker 修复后 `npm run check` 通过 54 个文件／454 项测试；本机没有 Docker 命令，镜像构建
  由发布服务器真实执行并通过，不能把本机缺失命令记作本地 Docker 验证。

## 下午集中发布与真机边界（2026-09-06，历史功能范围）

- 今天下午分散在多个独立 worktree 与共享目录的改动已按提交、patch 等价性和工作树归属逐一
  对账；早先已经由 PR #26 合并的遮蔽激活补丁未重复引入，其余通行密钥回焦、媒体准备、图片
  查看器、左滑回复、语音反馈、任务隔离与结构化生产回读已由 PR #28 集中合并并发布。
- 通行密钥候选针对真机 probe 显示的后台恢复 `hasFocus=false` 路径：未聚焦时不发原生请求，
  先请求并等待真实焦点且只启动一次；锁定、隐藏、离页、冻结和超时中止等待或遗留请求。
  既有自动化证明调用顺序和失败关闭，不证明 iPhone 系统验证框必然出现。
- 媒体准备中点按不会提前显露，完整认证、长度/SHA-256 校验与解码完成后仍先显示默认模糊
  缩略图；图片查看器按比例适配且无装饰黑边。左滑回复在手指离开原消息行后继续跟随到松手；
  语音取消阈值为 220 CSS 像素，越界不提前停录，发送/取消状态可回拖切换。
- Safari 的通行密钥选择、Face ID／设备密码提示及浏览器工具栏属于系统 UI；网页不能静默验证、
  强制生物识别或保证原生弹窗呈现。修复后的真实 iPhone iOS 27 Safari 尚未验收，不能把本轮
  自动化、CI 或旧版真机对照写成真机通过。

## 已发布应用与范围

正式来源为 `https://ai.shui.click`。当前完整应用提交
`fd7a8ab7732afff98c4f0a6a3b60c31ea67bf213` 已由
[PR #37](https://github.com/zdaiwmm/shui-IM/pull/37) 合并并部署。服务器
`deployed-at=20260907T005618Z`（08:56:18），08:57:28 独立回读确认版本、镜像、健康、
公开产物与 WebSocket 一致。本次新增范围见页首。

- **设备与验证**：常驻聊天安全码已移除，新增设备流程的六位核对码保留。遮蔽层完成长按后
  移除扩散环／烟花并保留按住提示；v3 原生请求支持 AbortSignal，后台恢复未聚焦时先恢复真实
  焦点再发起且仅发起一次。实际解锁仍在生命周期锁内重新读取当前持久记录；加入前历史不变。
  首次加入和新增设备绑定在 WebAuthn 返回后等待真实可见且聚焦，再继续创建、领取和安全码步骤。
- **键盘和输入栏**：原生视口事件按动画帧合并，按关闭／中间／打开目标态提交；中间态只
  移动固定栏，不重排列表或提前显现输入栏。目标稳定后统一结算留白、贴底、分页、阅读锚点
  和向下按钮；无几何键盘、原生抖动与挂起恢复有界处理。可信点按当前移动聊天输入框可
  创建一次最长 1.2 秒的可见失焦交接，避免误触发白色隐私帘；程序化聚焦、重复失焦、超时、
  hidden 与硬生命周期仍失败关闭。真实但仍报告 visible 的窗口离开可能在此短窗口内延迟
  遮挡，这是有界可用性取舍，不是认证，完整边界见 D-026。照片、空闲语音和向下按钮与
  输入栈共用闲置 84%／聚焦 92% 表面透明度与高光，禁用态不让整块背景变淡，并有无模糊、
  降低透明度、高对比和强制色彩回退。发送滚动时固定 iOS 标题栏和输入工具栏，并抑制延迟
  `visualViewport` 回调造成的二次抖动；关闭引用的键盘意图规则保持不变。
- **菜单、页面和手势**：tips 使用 320ms 对称渐显渐隐与触点变换原点，删除菜单不预选；
  页面进入/退出使用 380ms 双层交接且不露白。左滑回复在离开消息行后仍跟随手指，松手才回弹
  并激活，当前上限为可见视口六分之一并使用连续有界阻尼；语音长拖拽通过蓝/红状态与文案预览
  松手结果，越过阈值不提前停止录音。
- **文件、安全投影与系统阅读器**：聊天文件自动进入本机保险箱文件投影。点击可阅读白名单
  文件时，在受信点击中预开系统阅读器表面，完成逐字节解密和完整性校验后导航至对象 URL；
  PDF、JSON、纯文本、CSV、Markdown 之外仍只下载，不在应用内执行或任意预览。文件按压不再
  缩小内部 UI。
- **媒体与录音**：聊天图片/视频容器与加载后缩略图边界完全一致，缩略图撑满容器；相册最大化
  按宽高适配容器且不添加装饰黑边，支持 1/3×–5× 正常缩放和越界阻尼。冷媒体明确显示下载、
  解密、验证预览阶段，完成后仍默认模糊；已验证密文分块可从本机缓存重新认证，损坏时删除后
  回源一次。图片拖至视口边缘时最低缩至适配尺寸二分之一。录音请求继续使用兼容蓝牙输入的
  通用音频约束，授权失焦交由应用生命周期的一次性前台交接统一处理。
- **备份界面**：备份与恢复页使用与设备管理页一致的标题、分组设置表面、状态和危险操作层级；
  原有备份、恢复码、历史恢复、权限和协议语义保持不变。
- **版本更新与布局动画**：`release.json` 同时驱动应用和 Service Worker 的唯一版本；新版广播
  后显示聊天顶部更新条，更新后首次进入聊天展示有序版本说明。textarea 高度变化与标题栏、
  消息列表和输入工具栏在同一布局帧提交；输入栏按底边定位，输入框伸缩、新消息插入与列表
  位移共用逐帧动画，标题栏和输入工具栏保持固定；减少动态效果时关闭对应过渡。
- **发布治理**：写入任务要求独立 worktree；`npm run deploy:readback -- --sha <精确SHA>` 只读核对
  生产 SHA、容器/Image ID、HTTPS、公开产物与 WebSocket，失败只能重跑回读，不能重发应用。

消息与 MLS 状态的原子持久化、普通重试复用 ID/密文、设备历史边界、原始附件逐字节
校验、恢复范围和创建者权限保持原契约。详细交互以 [PRODUCT.md](../../PRODUCT.md)、
[SECURITY.md](../../SECURITY.md)、[TEST_PLAN.md](../../TEST_PLAN.md) 和 D-024 至 D-030 为准；自动化
不能外推真实 iPhone/Android 手感。

## 验证与生产证据

- PR #37 合并为精确 main `fd7a8ab7732afff98c4f0a6a3b60c31ea67bf213`；该 SHA 的
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34050348842)全部成功，覆盖构建、
  单元／集成、两组浏览器回归、原生通话、凭据扫描、文档检查、生产依赖审计、完整应用汇总与
  最终 `verify`。合并前各来源工作树的 `npm run check:full` 证据已在目标提交历史中记录，
  不能替代精确 main CI。
- 固定入口返回精确 `DEPLOY_OK`／`DEPLOY_VERIFIED`，入口总耗时 92,173ms，服务器发布 59 秒；
  发布目录 `/opt/quiet-room/git-releases/20260907T005618Z-fd7a8ab7732a`，已校验冷备份
  `/opt/quiet-room/backups/predeploy/data-20260907T005618Z-fd7a8ab7732a.tar.gz`。08:57:28 独立
  `READBACK_OK`：维护标记不存在，应用运行且健康、备份运行且无健康探针，二者实际镜像匹配
  `sha256:4b52b219a0cfad66da1a1aca4dd0f54d90056f3fa0b55c7be72476d7f2be9972`；HTTPS、数据库、
  存储、公开／容器产物和 WebSocket 均通过。`admin-enabled=0`、`calls-enabled=0`，TURN 不存在。
- PR #35 [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34034272592)及精确 main
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34034568250)全部成功。首轮 PR CI
  仅暴露既有图库返回夹具在滚动账务提交前读取基线的竞态；未放宽产品断言，修订夹具后完整
  套件通过。最终本地 `npm run check:full` 通过 54 个文件／454 项测试和 24 个浏览器入口。
- 固定发布返回精确 `DEPLOY_OK`、`DEPLOY_VERIFIED`，总耗时 362,808ms，其中等待 main CI
  276,063ms、隔离克隆 7,804ms、部署与入口回读 71,883ms；服务器阶段 58 秒。21:03:09
  独立 `READBACK_OK`，维护标记不存在，应用运行且健康、备份运行且无健康探针，实际镜像匹配
  `sha256:1dd0dbdd60dec11d8ec7c92c917e61a4c1afb88ba868632739d6fc27def9762b`；HTTPS、公开／容器
  产物一致性和公开 WebSocket 均通过。

- PR #31 [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34029235057)、PR #33
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34029933055)及精确 main
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34030231719)最终全部成功。main 首次
  浏览器第 1 组时序失败经日志分类后只重跑同一 SHA，浏览器两组、完整汇总与 `verify` 均通过。
- 本地集中 `npm run check:full` 从头通过 54 个文件／453 项测试和 24 个浏览器入口（259.98 秒）；
  Docker 修复后 `npm run check` 通过 54 个文件／454 项测试。CI 同时覆盖通话、凭据扫描、生产
  依赖审计和完整汇总。
- 固定发布返回精确 `DEPLOY_OK`、`DEPLOY_VERIFIED`，外层核对回执。总耗时 88,711ms，服务器
  阶段 56 秒；发布目录和已校验冷备份路径见发布记录。
- 19:37:32 独立 `READBACK_OK`，维护标记不存在；应用运行且健康、备份运行且无健康探针。
  实际镜像匹配 `sha256:0d8bc95ce285143a9cffe6dfa82dafd4300ef643ae9afe14ca8a1f286e9b17ff`；
  HTTPS 健康、公开／容器产物一致性和公开 WebSocket 均通过。未读取真实消息或备份内容。
- 上一版 `b2c1b65e6a3ee80dcd6eb4b158e3e8182e2f0e49` 的发布与回读记录保留在
  [上一发布记录](../../RELEASING.md#上次线上发布记录2026-09-06-1910)。

## 知识库治理状态

- 固定发布回执和独立生产回读证明线上应用为 `18e07b08e3c1422ac693a7f98a6a66ac1277ef85`；
  本次知识库治理从该精确 main 建立独立分支，只存在于工作树时不算合并或部署。最终文档
  提交在运行时复核，不把包含本页的提交号写成自引用“当前应用值”。
- D-021 已把每次发布后的固定触发点设为外层 `publish.mjs` 核对精确成功回执之后的独立生产回读，并要求在发布任务结束前完成一次轻治理。对账失败与生产失败分开，只重试对应后置步骤，不能重发应用。
- D-025 固化系统阅读器与下载的 MIME 白名单、安全解密交接和保险箱自动文件投影边界；
  D-024 的保险箱本地整理与聊天本地/全员删除边界保持不变。
- D-026 至 D-030 的既有边界不变；本批首次加入／设备绑定回焦、iOS 固定栏、媒体与回复手势、
  备份界面和蓝牙音频授权交接已取得精确 main CI、发布与独立回读证据。真机证据只覆盖页首明确
  列出的输入与发送布局范围，不外推至 Tesla 蓝牙、绑定流程、Android 或其他系统表面。
- 当前事实同步仍由发布任务根据可审查证据完成。仓库内独立结构化生产回读入口已合并、通过
  PR/main CI，并在生产发布后真实取得 `READBACK_OK`；隔离文档 PR 生成仍未实现，因此
  不能声称知识库已经无人值守自动更新。
- 后续实现应只自动写可机械验证的生产事实；产品、安全、真机和独立审计结论继续审阅。知识库文档提交与已部署应用提交始终分开记录，避免自引用发布循环。

## 仍需处理与系统边界

- **真机界面与权限**：除页首已专项验证的 iPhone Safari 多行输入与单行／多行发送布局外，
  真实 iPhone Safari／Chrome 和 Android Chrome 的浏览器工具栏、选择器、权限、播放器、
  后台恢复和其他手势仍未验收。网页不能强制原生工具栏永久透明、关闭
  操作系统自己的弹窗、静默授予或永久保留权限，也不能保证系统输入放大镜和原生播放器手势。
- **后台与恢复**：本次回读 `admin-enabled=0`，后台未启用。`sao.shui.click` DNS／TLS、管理员因素初始化、真实通行密钥与恢复矩阵、灾备恢复及独立密码学审计仍待完成。
- **通话**：本次回读 `calls-enabled=0`，TURN 未启用；公网／蜂窝互通、强制中继、长通话和切网尚未真机验证，见 [CALLS.md](../../CALLS.md)。
- **运维与安全门槛**：证书无人值守续期、异故障域不可变备份与外部告警仍待完成，详见 [OPERATIONS.md](../../OPERATIONS.md)。公开高安全声明继续受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。
- **历史扫描误报**：手动完整历史扫描仍可能命中两处旧测试公开 RFC 向量；本轮正常 PR 与 main 凭据扫描均通过，没有增加豁免或改写历史。出处见 [RELEASING.md](../../RELEASING.md#本次线上发布记录)。

## 接手复核

已部署应用为 `18e07b08e3c1422ac693a7f98a6a66ac1277ef85`；本发布记录随后由独立文档
提交形成，文档 SHA 不代表应用重新部署。原始开发目录仍可能包含不属于本发布的未提交内容，
不得覆盖或混入本次知识库 PR。接手时重新读取 `git status`、HEAD、GitHub main、精确 CI 和
生产 SHA；工作树、本地验证、CI、真机和生产分别取证。后续发布仍遵循
[RELEASING.md](../../RELEASING.md)，不得把本页旧 SHA 当作新的发布授权。
