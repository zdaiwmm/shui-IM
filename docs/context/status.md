# Quiet Room 当前状态

生产证据快照：2026-09-05 02:32:39（Asia/Shanghai）。本页用于接手定位，不代替实时 Git、CI 或生产回读；后续发布记录文档提交不代表应用重新部署。

## 已发布应用与范围

正式来源为 `https://ai.shui.click`。完整应用提交 `e619afb8f9f732839872815c591ea56e454f40e3` 已由 [PR #14](https://github.com/zdaiwmm/shui-IM/pull/14) 合并并部署，服务器 `deployed-at=20260904T183121Z`（02:31:21，发布批次时间），02:32:39 独立回读确认版本、镜像与公开产物一致。本轮实现 D-020，并保留 PR #12 的聊天时间线与此前 24 项交互修整。

- **标题与底部定位**：聊天和保险箱标题固定在可见屏幕顶部，保险箱在无 `resize`／`scroll` 事件时也持续采样；输入栏在键盘、工具栏或手动列表滚动的中间几何期间隐藏，稳定 80ms 后重测并用 140ms 淡入。返回最新按钮与输入栏共用位置和可见状态，间距仍为 8px。
- **键盘与消息列表**：键盘打开后，列表垂直触摸不能滚动或在移动中收键盘；最后一指松开才收起。草稿、选区、标题位置与阅读锚点保留。发送只平移可见消息，待发泡不再整块降透明度，避免回执到达时颜色闪动。
- **气泡时间与日期**：文字气泡在最后一行尾部预留时间／对勾，稍低的浅灰时间保持次要层级，宽度不足才自然换行。单字、长链接、表情、RTL 和大字号已覆盖；日期仍只在设备本地每个已加载日期的第一条消息上方显示一次，返回最新按钮语义不变。
- **语音**：触摸录音回焦不出现蓝色圈，键盘焦点仍可见。松手后的自动处理／上传仅显示状态，不露出删除、试听长条或发送按钮；失败恢复原冻结草稿和原消息 ID。既有自然放大、水平阻尼、156px 左滑取消和无上滑锁定保持。
- **媒体与相册**：已显露照片、相册或视频预览随向下手势产生阻尼位移，达到阈值后在松手时隐藏；短拖返回，取消及隐私清理立即处理。相册批量显隐只动画屏幕内已解码项目，全部项目仍同步更新。原始附件、完整校验、照片缩放及视频原生全屏优先保持。
- **前台系统弹窗**：用户打开的文件选择器最长 5 分钟、麦克风／摄像头授权最长 30 秒，可拥有一次可见窗口失焦交接；返回焦点即消费，结果先到最多等待 250ms。隐藏、离页、冻结、再次失焦、显式锁定和超时仍清理；上传仍要求页面可见聚焦且房间／设备一致。浏览器不能可靠说明可见失焦原因，这是用户明确接受的有限易用性取舍，不是认证能力。
- **遮蔽入口与页面动效**：右下角热区为 80×80px，支持指针捕获；触摸按满 1 秒后播放 180ms 弱扩散再进入验证，验证页立即发起通行密钥。漏抬手、迟到焦点、重复返回、旧读取和旧认证结果不能阻塞或跨尝试。桌面 F 仍需 2 秒。页面立即切换，新内容 160ms 渐显，标题、背景和输入栏不出现空帧。

加密协议、消息与 MLS 原子持久化、普通重试、设备历史边界、原始附件字节、恢复范围和创建者权限没有改变。详细映射见[交互验收](../../audit/2026-09-05-mobile-continuity/README.md)，长期契约见 D-020、[PRODUCT.md](../../PRODUCT.md) 与 [SECURITY.md](../../SECURITY.md)。

## 验证与生产证据

- [PR 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33905364719) 对应 `1fbd131264d4eb17914932c94cfabd7035f0ca83`；[精确 main 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33905829879) 对应上述应用提交。两次均通过构建、316 项单元／集成、两组共 22 个浏览器入口、原生通话、凭据扫描、生产依赖审计及完整汇总。
- 本地冻结工作树 `npm run check:full` 全程通过（44 文件、316 项、22／22 浏览器入口，浏览器 171.28 秒），`npm run test:calls:e2e` 两项通过。WebKit 的系统生命周期、键盘／标题、底部按钮、时间线、桌面隐私、语音提交和图片隐私专项通过。
- Chrome／WebKit 时间线各 32 种布局，时间最小对比度 4.52258:1；语音自动提交连续采样 Chrome 28 帧、WebKit 21 帧均未露出草稿操作。保险箱无事件连续 5 帧标题偏差为 0，采样器没有查询选择器或读取消息边界，锁定后停止读取视口。
- 首次本地完整回归找到两个旧测试仍按离场延迟／首次选择器失焦锁定，改为本轮真实事件契约后第三次完整命令从头通过。PR 首轮 Linux CI 又找到字形框基线固定余量的跨平台假阳性；气泡没有重叠或溢出，断言改用真实行高区分一行与两行，Chrome／WebKit 复跑及上述精确 PR/main CI 通过。没有改变产品 CSS 或放宽重叠、溢出、短消息同行、原件和续传断言。
- 固定入口返回 `DEPLOY_OK` 与 `DEPLOY_VERIFIED`，精确 SHA 回执一致；切换前冷备份校验和公开 WebSocket 验证通过。发布入口总耗时 348,456ms（包含等待 main CI），其中实际隔离部署与回读 70,301ms；服务器阶段 56 秒。
- 02:32:39 独立回读确认维护标记消失，应用容器运行且健康、备份容器运行；两者实际 Image ID 与目标镜像一致。备份容器未配置健康探针，未记作探针成功。HTTPS `ok`／`database`／`storage` 全为 true；首页、Service Worker、CSS、应用 JavaScript及 preload 脚本与容器产物逐字节匹配。
- 发布使用固定命令行、GitHub CLI 与 SSH 流程，并通过临时防休眠保持主机可用；未人为锁屏进行端到端验证。自动化使用合成会话、虚拟通行密钥与合成媒体，不代表真实 iPhone 或独立安全审计通过。

## 仍需处理与系统边界

- **真机界面与权限**：真实 iPhone Safari／Chrome 的工具栏、键盘、选择器、权限、播放器和手势尚未验收。网页不能强制原生工具栏永久透明、静默授予或永久保留权限，也不能保证系统输入放大镜和原生播放器手势；本轮选择在中间几何隐藏输入栏，再在稳定位置显示。
- **后台与恢复**：本次回读 `admin-enabled=0`，后台未启用。`sao.shui.click` DNS／TLS、管理员因素初始化、真实通行密钥与恢复矩阵、灾备恢复及独立密码学审计仍待完成。
- **通话**：本次回读 `calls-enabled=0`，TURN 未启用；公网／蜂窝互通、强制中继、长通话和切网尚未真机验证，见 [CALLS.md](../../CALLS.md)。
- **运维与安全门槛**：证书无人值守续期、异故障域不可变备份与外部告警仍待完成，详见 [OPERATIONS.md](../../OPERATIONS.md)。公开高安全声明继续受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。
- **历史扫描误报**：手动完整历史扫描仍可能命中两处旧测试公开 RFC 向量；本轮正常 PR 与 main 凭据扫描均通过，没有增加豁免或改写历史。出处见 [RELEASING.md](../../RELEASING.md#本次线上发布记录)。

## 接手复核

本地 main 在应用发布时已同步 `e619afb8f9f732839872815c591ea56e454f40e3`；本发布记录随后由独立文档提交形成，文档 SHA 不代表应用重新部署。接手时重新读取 `git status`、HEAD、GitHub main、精确 CI 和生产 SHA；工作树、本地验证、CI、真机和生产分别取证。后续发布仍遵循 [RELEASING.md](../../RELEASING.md)，不得把本页旧 SHA 当作新的发布授权。
