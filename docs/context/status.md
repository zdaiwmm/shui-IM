# Quiet Room 当前状态

生产证据快照：2026-09-05 11:06（Asia/Shanghai）。本页用于接手定位，不代替实时 Git、CI 或生产回读；后续发布记录文档提交不代表应用重新部署。

## 已发布应用与范围

正式来源为 `https://ai.shui.click`。完整应用提交 `98aee2aa7491564d30c9a93cc3869c5f758610a3`
已由 [PR #17](https://github.com/zdaiwmm/shui-IM/pull/17) 和测试可移植性修正
[PR #18](https://github.com/zdaiwmm/shui-IM/pull/18) 合并并部署。服务器
`deployed-at=20260905T030000Z`（11:00:00，发布批次时间），11:06 前的独立回读确认版本、
镜像、健康、公开产物与 WebSocket 一致。本轮保留 D-020，并新增 D-024 的整理与删除分层。

- **设备链接**：参与者邀请与新增设备链接严格分开；已有设备后再次新增设备进入原设备审批
  和六位安全码核对，不再误报参与者邀请被使用。加入前历史边界不变。
- **查看器和媒体**：照片/视频横划采用跟手阻尼与 380ms 交接，标题常驻；双击缩放逐帧
  过渡。横划进入视频时默认暂停，视频仍可继续横划到图片，离开即暂停并卸载来源。
- **输入栏和原生交接**：滚动及键盘开合首帧隐藏输入栏，视口稳定 160ms 后以 280ms 从
  下方渐显；草稿、焦点和选区保留。选择器/系统 tips 的 DOM 入口在失焦或失效时拆除，
  页面重新聚焦不会保留旧遮蔽；操作系统自身表面仍由浏览器控制。
- **回应、回复与时间**：回应增删使用有界 FLIP，使后续气泡平滑下移；时间使用弱化灰色。
  气泡向左拖拽带阻尼，达到阈值松手进入回复并聚焦键盘；引用与输入框共用外轮廓，摘要
  最多一行。
- **删除和保险箱整理**：保险箱长按可本地置顶/删除，只改变该设备相册或文件投影，不影响
  聊天排序或气泡。聊天删除二级菜单区分“仅为我删除”和有权限/能力门禁的“为所有人删除”；
  后者使用加密 tombstone，不承诺擦除已读、导出或备份副本。
- **遮蔽入口和视觉连续性**：右下角长按反馈在后台恢复后仍可重复进入，并直接发起验证；
  关闭验证后不残留红色提示。文件/相册计数、图片模糊填充、标题和过渡不再产生已知闪动。

消息与 MLS 状态的原子持久化、普通重试复用 ID/密文、设备历史边界、原始附件逐字节
校验、恢复范围和创建者权限保持原契约。详细交互以 [PRODUCT.md](../../PRODUCT.md)、
[SECURITY.md](../../SECURITY.md)、[TEST_PLAN.md](../../TEST_PLAN.md) 和 D-024 为准；自动化
不能外推真实 iPhone/Android 手感。

## 验证与生产证据

- 应用 [PR #17 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33938924491) 成功；测试
  可移植性 [PR #18 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33939464149) 重跑成功。
  [精确 main 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33940176339) 对应上述应用
  SHA，约 4 分钟完成构建、400 项单元/集成、两组共 23 个浏览器入口、原生通话、凭据扫描、
  生产依赖审计及 `Full application verification`／`verify` 汇总。
- 冻结候选的本地 `npm run check:full` 通过 50 个测试文件、400 项测试和 23／23 个浏览器
  入口，浏览器部分 200.34 秒；PR #18 仅调整跨字体回应位移夹具，随后定向
  `tests/frontend-lifecycle.e2e.mjs` 通过。没有用该修正放宽 300ms FLIP、首帧零跳动、DOM
  保留、最终移除或 reduced-motion 断言。
- 合并 PR #17 后的首个 main run `33939125012` 只因 Linux 字体回退下回应位移为
  4.920654296875px、旧固定阈值要求大于 8px 而失败；产品状态与时间断言通过。PR #18 改为
  兼容字形行框且仍能排除无位移的阈值。一次 Playwright 安装卡住被取消，只重跑同一提交。
- 固定入口第一次在服务器调用前的隔离浅克隆等待 120,011ms 后超时，不构成发布；只读 SSH
  探测成功后安全重试。同一 SHA 的成功入口返回 `DEPLOY_OK` 与 `DEPLOY_VERIFIED`，总耗时
  145,543ms：隔离克隆 61,664ms，部署及入口回读 72,217ms，服务器阶段 58 秒。冷备份为
  `/opt/quiet-room/backups/predeploy/data-20260905T030000Z-98aee2aa7491.tar.gz`。
- 独立只读回读返回 `READBACK_OK`：维护标记不存在；应用容器运行且健康，备份容器运行且
  没有健康探针；两者实际 Image ID 均匹配目标镜像
  `sha256:1e7aa9cbdeac8089600581d7b7fbe5a63cf6a2a7013b7cdfe74dadfed0f06a6b`。
  HTTPS `ok`／`database`／`storage` 全为 true，首页、Service Worker、CSS、应用 JavaScript
  和 preload 脚本与容器产物逐字节一致，公开 WebSocket 独立连接成功。回读未访问真实消息、
  附件或备份内容。
- 交付时长与 token 成本复盘见[2026-09-05 交付复盘](../../audit/2026-09-05-mobile-ux-delivery-retrospective.md)。
  真实 iPhone/Android、原生键盘/选择器/播放器手感和独立安全审计仍未完成。

## 知识库治理状态

- 固定发布回执和独立生产回读证明线上应用为 `98aee2aa7491564d30c9a93cc3869c5f758610a3`；
  本次知识库治理从该精确 main 建立独立分支，只存在于工作树时不算合并或部署。最终文档
  提交在运行时复核，不把包含本页的提交号写成自引用“当前应用值”。
- D-021 已把每次发布后的固定触发点设为外层 `publish.mjs` 核对精确成功回执之后的独立生产回读，并要求在发布任务结束前完成一次轻治理。对账失败与生产失败分开，只重试对应后置步骤，不能重发应用。
- D-024 固化保险箱本地整理与聊天本地/全员删除的边界；`maintenance.md` 新增大批量变更的
  验收分组、测试漏斗、失败分类和发布提速约束。
- 当前事实同步仍由发布任务根据可审查证据完成；仓库内结构化生产回读、隔离文档 PR 生成、幂等恢复和失败续跑尚未实现，因此不能声称知识库已经无人值守自动更新。
- 后续实现应只自动写可机械验证的生产事实；产品、安全、真机和独立审计结论继续审阅。知识库文档提交与已部署应用提交始终分开记录，避免自引用发布循环。

## 仍需处理与系统边界

- **真机界面与权限**：真实 iPhone Safari／Chrome 和 Android Chrome 的工具栏、键盘、
  选择器、权限、播放器、后台恢复和手势尚未验收。网页不能强制原生工具栏永久透明、关闭
  操作系统自己的弹窗、静默授予或永久保留权限，也不能保证系统输入放大镜和原生播放器手势。
- **后台与恢复**：本次回读 `admin-enabled=0`，后台未启用。`sao.shui.click` DNS／TLS、管理员因素初始化、真实通行密钥与恢复矩阵、灾备恢复及独立密码学审计仍待完成。
- **通话**：本次回读 `calls-enabled=0`，TURN 未启用；公网／蜂窝互通、强制中继、长通话和切网尚未真机验证，见 [CALLS.md](../../CALLS.md)。
- **运维与安全门槛**：证书无人值守续期、异故障域不可变备份与外部告警仍待完成，详见 [OPERATIONS.md](../../OPERATIONS.md)。公开高安全声明继续受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。
- **历史扫描误报**：手动完整历史扫描仍可能命中两处旧测试公开 RFC 向量；本轮正常 PR 与 main 凭据扫描均通过，没有增加豁免或改写历史。出处见 [RELEASING.md](../../RELEASING.md#本次线上发布记录)。

## 接手复核

应用发布目标为 `98aee2aa7491564d30c9a93cc3869c5f758610a3`；本发布记录随后由独立文档
提交形成，文档 SHA 不代表应用重新部署。原始开发目录仍可能包含不属于本发布的未提交内容，
不得覆盖或混入本次知识库 PR。接手时重新读取 `git status`、HEAD、GitHub main、精确 CI 和
生产 SHA；工作树、本地验证、CI、真机和生产分别取证。后续发布仍遵循
[RELEASING.md](../../RELEASING.md)，不得把本页旧 SHA 当作新的发布授权。
