# Quiet Room 当前状态

生产证据快照：2026-09-06 13:38:29（Asia/Shanghai）。本页用于接手定位，不代替实时 Git、CI 或生产回读；后续发布记录文档提交不代表应用重新部署。

## 发布后真机反馈与当前候选（2026-09-06）

- 用户真机回报 `Host=ai.shui.click`、`App=true`，页面 Scripts 和 Loaded 均为
  `/assets/app-CIOXv7kU.js`，与上述已发布应用一致。用户仍遇到“正在验证…”不可点击，
  延迟后才出现系统验证框；旧版本缓存不能解释这次已核实的反馈。
- 撤回“IndexedDB／Web Lock 耗尽 Safari 用户激活就是根因／已解决”的结论。
  既有测试证明调用时序，不证明 iPhone 原生弹窗呈现。保留加密包装预读优化与授权重读边界，
  不继续盲改延迟、自动重试或降低用户验证要求。
- 当前候选从 `b91a3a36fc7cb61c467347798f61c1f79e8acf39` 隔离开发：按用户要求移除完成
  长按后的扩散环／烟花，保留按住提示；补充 v3 网关原生请求的 AbortSignal。新测试在修复前
  确定复现“锁定后 credentials.get 仍 pending”，修复后锁定、pagehide、freeze、65 秒超时均
  中止旧请求。此缺口可能影响再次验证，但不是已证明的全部真机根因。候选尚未发布。
- 候选本地 `npm run check:full` 从头通过：51 文件／413 项单元集成，23／23 浏览器入口，
  浏览器汇总 252.15 秒；新增 Chromium 可信触摸事件路径通过，Playwright WebKit 专项
  通过（该专项使用鼠标事件和原生凭据接口替身）。不等于 iPhone 真机通过；本轮未合并或发布。
- 用户新录屏约 5.9 秒：画面停在遮蔽页，后段出现按住灰点后消失，未见验证页或系统框。
  与“已进入正在验证页”的症状分开调查；录屏不包含 pointerup/cancel/blur 事件，不能据此
  判定是否完整按住一秒或哪一事件取消入口。不把原始录屏／截图存入仓库。
- Safari 的“使用通行密钥”和 Face ID／设备密码选择属于系统 UI；网页已要求用户验证，
  不能保证跳过系统确认或强制某一种生物识别。其他创建／恢复验证的请求取消范围先记为旁支，
  本批只修改普通 v3 解锁，不扩大恢复或注册行为。

## 已发布应用与范围

正式来源为 `https://ai.shui.click`。完整应用提交 `6cce8b017c98cf270729e28bfabae0ee3e551570`
已由 [PR #26](https://github.com/zdaiwmm/shui-IM/pull/26) 合并并部署。服务器
`deployed-at=20260906T053714Z`（13:37:14，发布批次时间），13:38:29 独立回读确认版本、
镜像、健康、公开产物与 WebSocket 一致。该版尝试修复遮蔽层长按进入验证页后 Safari 偶发
不弹系统密码验证的问题，但后续真机反馈仍失败；上一版本的键盘、阅读器、媒体与手势能力继续保留。

- **设备与验证**：常驻聊天安全码已移除，新增设备流程的六位核对码保留。遮蔽层完成长按后
  使用遮蔽空闲期准备的加密 v3 保险箱包装，减少发起设备密码验证之前的
  IndexedDB／Web Lock 等待；这不证明原生弹窗一定出现。实际解锁仍在生命周期锁内重新读取当前
  持久记录，缓存不作为授权状态。加入前历史边界不变。
- **键盘和输入栏**：原生视口事件按动画帧合并，按关闭／中间／打开目标态提交；中间态只
  移动固定栏，不重排列表或提前显现输入栏。目标稳定后统一结算留白、贴底、分页、阅读锚点
  和向下按钮；无几何键盘、原生抖动与挂起恢复有界处理。可信点按当前移动聊天输入框可
  创建一次最长 1.2 秒的可见失焦交接，避免误触发白色隐私帘；程序化聚焦、重复失焦、超时、
  hidden 与硬生命周期仍失败关闭。真实但仍报告 visible 的窗口离开可能在此短窗口内延迟
  遮挡，这是有界可用性取舍，不是认证，完整边界见 D-026。照片、空闲语音和向下按钮与
  输入栈共用闲置 84%／聚焦 92% 表面透明度与高光，禁用态不让整块背景变淡，并有无模糊、
  降低透明度、高对比和强制色彩回退。关闭引用的键盘意图规则保持不变。
- **菜单、页面和手势**：tips 使用 320ms 对称渐显渐隐与触点变换原点，删除菜单不预选；
  页面进入/退出使用 380ms 双层交接且不露白。语音整块气泡可长按；左滑回复加长拖拽距离，
  仅在松手后回弹并激活。遮蔽长按反馈扩大，并在空白区域播放烟花。
- **文件、安全投影与系统阅读器**：聊天文件自动进入本机保险箱文件投影。点击可阅读白名单
  文件时，在受信点击中预开系统阅读器表面，完成逐字节解密和完整性校验后导航至对象 URL；
  PDF、JSON、纯文本、CSV、Markdown 之外仍只下载，不在应用内执行或任意预览。文件按压不再
  缩小内部 UI。
- **媒体与录音**：聊天图片/视频容器与加载后缩略图边界完全一致，缩略图撑满容器；相册最大化
  按宽高适配容器，弱化所有媒体高斯模糊，并为缩放加入越界阻尼。视频海报尝试有界后续帧，
  避免未解码首帧黑图；录音请求改为兼容蓝牙输入的通用音频约束。

消息与 MLS 状态的原子持久化、普通重试复用 ID/密文、设备历史边界、原始附件逐字节
校验、恢复范围和创建者权限保持原契约。详细交互以 [PRODUCT.md](../../PRODUCT.md)、
[SECURITY.md](../../SECURITY.md)、[TEST_PLAN.md](../../TEST_PLAN.md) 和 D-024/D-025/D-026 为准；自动化
不能外推真实 iPhone/Android 手感。

## 验证与生产证据

- 本轮 [PR 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34013956905) 与
  [精确 main 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34014192269) 均成功：
  构建、52 个测试文件／419 项单元集成、两组共 23 个浏览器入口、通话、凭据扫描、生产
  依赖审计与完整汇总通过。本地最终 `npm run check:full` 从头通过，浏览器汇总 250.01 秒；
  Chromium、Playwright WebKit、系统表面与虚拟 CTAP2.1/PRF 专项通过。
- 本次固定入口返回精确 `DEPLOY_OK`、`DEPLOY_VERIFIED` 并由外层核对回执，总耗时
  88,183ms；发布目录 `/opt/quiet-room/git-releases/20260906T053714Z-6cce8b017c98`，
  已校验冷备份 `/opt/quiet-room/backups/predeploy/data-20260906T053714Z-6cce8b017c98.tar.gz`。
- 13:38:29 独立 `READBACK_OK`：维护标记不存在，应用运行且健康、备份运行且没有健康
  探针，实际镜像均为 `sha256:f05f256b587db9b4cc7d5691b5b93b90be60099ef9302003452f4e23f762e1cb`。
  HTTPS `ok`／`database`／`storage` 全为 true；首页、Service Worker、CSS、应用 JavaScript
  与 preload 和容器产物逐字节一致；公开 WebSocket 新连接成功。未读取真实消息、附件或备份。
  详细证据见 [RELEASING.md](../../RELEASING.md#本次线上发布记录)。

### 上一版本证据（2026-09-06 10:32，仅作历史基线）

- 旧应用 SHA `f7c2f1daa41c00a107685750d4be62cc892c15f2` 由
  [PR #24](https://github.com/zdaiwmm/shui-IM/pull/24) 合并并发布；其精确 CI、一次安全回滚、
  随后成功发布与独立回读证据保留在 [RELEASING.md](../../RELEASING.md#上次线上发布记录2026-09-06-1032)。

## 知识库治理状态

- 固定发布回执和独立生产回读证明线上应用为 `6cce8b017c98cf270729e28bfabae0ee3e551570`；
  本次知识库治理从该精确 main 建立独立分支，只存在于工作树时不算合并或部署。最终文档
  提交在运行时复核，不把包含本页的提交号写成自引用“当前应用值”。
- D-021 已把每次发布后的固定触发点设为外层 `publish.mjs` 核对精确成功回执之后的独立生产回读，并要求在发布任务结束前完成一次轻治理。对账失败与生产失败分开，只重试对应后置步骤，不能重发应用。
- D-025 固化系统阅读器与下载的 MIME 白名单、安全解密交接和保险箱自动文件投影边界；
  D-024 的保险箱本地整理与聊天本地/全员删除边界保持不变。
- D-026 的既有交互边界不变；已发布版本优化遮蔽长按后 WebAuthn 调用前的等待，并取得精确
  测试、CI、发布与独立回读证据，但后续真机反馈否定了“问题已解决”，见页首候选记录。
- 当前事实同步仍由发布任务根据可审查证据完成。工作树已新增仓库内独立结构化生产回读入口，包含阶段耗时、稳定失败分类、`.git` 内脱敏证据和只重跑回读的安全续跑；本地 `npm run check` 已通过 52 个测试文件、419 项测试，发布/回读定向 33 项、`git diff --check` 及将当前 `workflow.md` 纳入 tracked 集合的隔离文档检查也通过。直接文档检查仍会在该既有未跟踪文件正式纳入版本控制前按设计报缺失；本轮改动尚未合并、经 CI 或用于新的真实生产回读。隔离文档 PR 生成尚未实现，因此不能声称知识库已经无人值守自动更新。
- 后续实现应只自动写可机械验证的生产事实；产品、安全、真机和独立审计结论继续审阅。知识库文档提交与已部署应用提交始终分开记录，避免自引用发布循环。

## 仍需处理与系统边界

- **真机界面与权限**：真实 iPhone Safari／Chrome 和 Android Chrome 的工具栏、键盘、
  选择器、权限、播放器、后台恢复和手势尚未验收。网页不能强制原生工具栏永久透明、关闭
  操作系统自己的弹窗、静默授予或永久保留权限，也不能保证系统输入放大镜和原生播放器手势。
- **后台与恢复**：本次回读 `admin-enabled=0`，后台未启用。`sao.shui.click` DNS／TLS、管理员因素初始化、真实通行密钥与恢复矩阵、灾备恢复及独立密码学审计仍待完成。
- **通话**：本次回读 `calls-enabled=0`，TURN 未启用；公网／蜂窝互通、强制中继、长通话和切网尚未真机验证，见 [CALLS.md](../../CALLS.md)。
- **运维与安全门槛**：证书无人值守续期、异故障域不可变备份与外部告警仍待完成，详见 [OPERATIONS.md](../../OPERATIONS.md)。公开高安全声明继续受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。
- **历史扫描误报**：手动完整历史扫描仍可能命中两处旧测试公开 RFC 向量；本轮正常 PR 与 main 凭据扫描均通过，没有增加豁免或改写历史。出处见 [RELEASING.md](../../RELEASING.md#本次线上发布记录)。

## 接手复核

应用发布目标为 `6cce8b017c98cf270729e28bfabae0ee3e551570`；本发布记录随后由独立文档
提交形成，文档 SHA 不代表应用重新部署。原始开发目录仍可能包含不属于本发布的未提交内容，
不得覆盖或混入本次知识库 PR。接手时重新读取 `git status`、HEAD、GitHub main、精确 CI 和
生产 SHA；工作树、本地验证、CI、真机和生产分别取证。后续发布仍遵循
[RELEASING.md](../../RELEASING.md)，不得把本页旧 SHA 当作新的发布授权。
