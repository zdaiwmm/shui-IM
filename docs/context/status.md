# Quiet Room 当前状态

生产证据快照：2026-09-07 08:57:28（Asia/Shanghai）。本页用于接手定位，不代替实时 Git、CI 或生产回读；后续发布记录文档提交不代表应用重新部署。

## iPhone 输入抖动本地复查（2026-09-07）

- 本机 LAN 基线为 `eac6a509c477064daf17dc8514ba3fc2f6b23602`；本机 main 当前停在首轮
  部分修复 `4787e0ef00addcb811d1725aa4bf992367b6722f`。后续候选位于独占工作树
  `/private/tmp/quiet-room-iphone-input-jitter-20260907`，分支 `codex/iphone-input-jitter-20260907`。
- 当前候选：iPhone 原生编辑区域同步增高、IME 临时缩行延到下一帧；底部输入和发送保持原生文档
  位置，用相对消息位移跟随；独立固定底边探针避免浏览器工具栏遮挡。弹起键盘提前定位最新消息，
  包含本地历史分页；关闭时使用合成动画平移消息，输入栏在最终位置直接显现。
- 真机已确认：光标正常、历史位置输入改善、底部输入／发送抖动显著减轻；弹起及时到最新消息，
  输入栏不遮挡且收键盘后能及时出现。最新对照去掉列表重复的固定原点补偿，用户确认
  从历史打开后收键盘“下移连贯了”，五轮采集不再出现原先 376 px 回跳。
  **仍未通过完整真机验收**：标题仍闪，最新列表修复已通过完整自动回归，标题及键盘上移过渡仍在限时真机对照。
- 原生截图证明 DOM top=0 不能单独证明标题正确绘制。sticky 加独立原点补偿的候选已恢复；
  无补偿 fixed/top=0 的独立对照被用户确认标题会消失，未写入源码。聚焦绘制临时对照
  已补采实际操作，仍出现原点切换与闪动／回跳，已撤掉。临时对照都有自动恢复期限。
- 最新列表回跳修复完整 `npm run check:full` 通过：55 文件、465 项单元／集成、24／24 浏览器入口
  （浏览器 291.12 秒）；WebKit 布局专项通过。此前间歇出现的相册返回 88 px 锚点问题已用
  迟到旧页面图片 decode 定向用例稳定复现并修复，原失败用例及完整入口通过，未降低断言。
- 候选保留待继续真机诊断，不集成本机 main，不推送或发布。标题自身坐标与取消合成提示两项对照
  均未消除闪动，下一步继续标题单独文档定位及键盘上移短过渡对照。详细过程和证据层级见
  [本地修复证据](../../audit/2026-09-07-iphone-input-jitter.md)。本条不改变下列生产记录。

## 九月移动交互修复发布（2026-09-07 08:57）

- [PR #37](https://github.com/zdaiwmm/shui-IM/pull/37) 已合并为
  `fd7a8ab7732afff98c4f0a6a3b60c31ea67bf213` 并发布。首次加入与新增设备绑定会在 WebAuthn
  结果返回后等待页面真实可见且聚焦，再继续会话创建、邀请领取或六位安全码步骤；隐藏、
  `pagehide`、`freeze`、锁定、入口替换和总期限仍失败关闭。
- 图片详情边缘缩放、回复左滑阻尼、备份与恢复页层级和蓝牙音频授权交接已随本批上线；未归属
  失焦和硬生命周期仍立即清理。图片拖至边缘的最小视觉尺寸为适配尺寸二分之一，回复位移上限
  为可见视口六分之一。协议、恢复权限、附件校验和设备历史边界未改变。
- iOS 发送滚动与延迟 `visualViewport` 更新期间，标题栏和输入工具栏按绝对目标固定；textarea
  伸缩、消息插入与列表位移仍共用逐帧动画。用户提供的 iPhone／iOS 27／Safari 专项真机证据
  覆盖多行输入、单行／多行发送和高度复位的布局稳定性；本机调试后端没有既有房间授权，因此
  该真机证据不证明真实网络送达。Tesla 蓝牙、首次加入／设备绑定修复后真机复测仍未完成。
- 精确 main [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34050348842)通过构建与
  单元／集成测试、两组浏览器回归、原生通话、生产依赖审计、凭据扫描、文档检查、
  `Full application verification` 和最终 `verify`。固定入口返回 `DEPLOY_OK`／
  `DEPLOY_VERIFIED`；08:57:28 独立 `READBACK_OK` 确认精确版本、镜像、健康、HTTPS、公开与
  容器产物及 WebSocket 一致。

## 输入框与列表联动发布（2026-09-06 21:03）

- [PR #35](https://github.com/zdaiwmm/shui-IM/pull/35) 已合并为
  `fc3c0cdd28f5acac81f32e30b8c57b28536b0f2c` 并发布。输入栏改为布局底边定位，输入框伸缩、
  新消息插入和消息列表位移由同一逐帧更新驱动；标题栏与输入工具栏保持固定。连续组词、伸展
  途中发送和多行发送后的高度复位从当前可见位置平顺衔接，阅读旧消息时保持锚点。
- 新增键盘视口、连续换行、发送打断与历史锚点的浏览器回归。未修改的 main 副本在新增历史
  锚点断言下出现约 44px 上移后跳回；修复候选的 WebKit 专项和最终本地 `npm run check:full`
  均通过，后者覆盖构建、54 个测试文件／454 项单元集成及 24／24 个 Chromium 浏览器入口
  （265.30 秒）。
- PR 首轮 CI 的浏览器第 1 组暴露既有保险箱返回用例在滚动账务提交前读取基线的竞态；产品
  断言保持不变，夹具等待两个动画帧后再记录同一消息与像素偏移。本地主浏览器流程、修订后的
  [PR 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34034272592)及精确 main
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34034568250)全部通过。
- 固定入口发布批次为 `20260906T130200Z`，返回精确 `DEPLOY_OK`／`DEPLOY_VERIFIED`；
  21:03:09 独立 `READBACK_OK` 确认版本、镜像、HTTPS、公开与容器产物、健康和 WebSocket
  一致。自动化、CI 和生产回读不替代真实 iPhone／Android 验收。

## 晚间集中发布（2026-09-06 19:37）

- [PR #31](https://github.com/zdaiwmm/shui-IM/pull/31) 已集中合并遮蔽焦点观察、回复与相册手势、
  已验证密文媒体缓存、语音文案与时间对比度、输入框布局动画，以及版本更新广播、提示条和
  版本说明；[PR #33](https://github.com/zdaiwmm/shui-IM/pull/33) 补齐生产 Docker 构建输入。
- 线上应用为 `981b21612b16bc5b9d1e4007ed051f17a3170256`。固定入口返回精确
  `DEPLOY_OK`／`DEPLOY_VERIFIED`，19:37:32 独立 `READBACK_OK` 确认版本、镜像、HTTPS、
  公开与容器产物、健康和 WebSocket 一致；详情见发布记录。
- 首次发布 `624abe606674140b0c3de5a44043c8b273ad5e02` 在 Docker 构建阶段发现缺少
  `release.json`，停止于维护门和生产切换之前；修复通过新 PR、新 main SHA 和完整 CI 后才
  发布，没有重试失败 SHA。
- 自动化、CI 和生产回读不替代真实 iPhone／Android 验收；下午交互修复的真机缺口不变。

## 晚间批次开发证据（2026-09-06）

- PR #31 于 19:10:55 合并为 `624abe606674140b0c3de5a44043c8b273ad5e02`；生产构建修复
  经 PR #33 于 19:25:50 合并，最终应用 SHA 为 `981b21612b16bc5b9d1e4007ed051f17a3170256`。
- 遮蔽长按进入 v3 网关后新增 1.5 秒静默焦点观察，覆盖 Safari 更新 `hasFocus` 却不派发
  `focus` 的短前台边缘；未聚焦绝不调用 WebAuthn，正常焦点监听、可重试按钮、请求中止与
  生命周期失败关闭保持有效。网页仍不能保证浏览器自有验证弹窗必然呈现。
- 回复左移上限改为实时可见视口宽度三分之一；照片双指缩小松开回到默认 1×，放大保留；视频
  详情与照片共用整屏容器。相册翻页先原子转交残余位移再刷新媒体归位，避免 WebKit 中间居中帧。
- 媒体明文和预览仍只存在于解锁运行时；服务器密文分块可留在本机 IndexedDB，再进入聊天时
  重新完成逐块认证、长度和 SHA-256 校验而无需重复网络下载，损坏缓存删除后回源一次。
- 语音蓝色态文案为“松手发送，左滑取消录制”，红色态为“松手取消录制”；消息时间使用满足
  小字号 4.5:1 对比度的浅灰。来源工作树 `npm run check:full` 已从头通过构建、52 个测试
  文件／422 项单元集成和 23／23 个顺序 Chromium 浏览器入口（257.91 秒）；来源测试本身不等于生产发布或修复后真实 iPhone 验收。
- 版本更新来源工作树已新增单一
  `release.json` 版本／更新报告来源、Service Worker 新版本广播、聊天顶部更新条及更新后首次
  进入聊天的有序版本说明。首次安装不误报更新；待展示状态跨遮罩页刷新保留；只在本机记录
  版本 ID，不修改消息、房间、设备历史或服务器元数据。该路径现已随本批发布并完成生产回读。
- 该来源工作树 `npm run check` 已通过 53 个测试文件、426 项测试；`npm run test:browser` 已通过新增
  版本更新专项在内的 24／24 个浏览器入口，汇总 255.59 秒。构建产物已核对 Service Worker
  版本占位符被当前发布 ID 替换。真实旧版到新版生产升级与真机均未验证。
- 输入框高度候选将 textarea 高度变化并入同一帧的聊天布局提交，保持标题栏、消息列表、输入
  工具栏和发送位移动画同步；任务治理候选补充只按审查清单清理已合并、干净且非活动状态的
  本地任务分支/worktree 命令。最终集中组合 `npm run check:full` 已从头通过构建、54 个测试
  文件／453 项单元集成和 24／24 个 Chromium 浏览器入口（259.98 秒）；文档与差异格式检查通过。
  Docker 修复后 `npm run check` 通过 54 个文件／454 项测试；本机没有 Docker 命令，镜像构建
  由发布服务器真实执行并通过，不能把本机缺失命令记作本地 Docker 验证。

## 下午集中发布与真机边界（2026-09-06，历史功能范围）

- 今天下午分散在多个独立 worktree 与共享目录的改动已按提交、patch 等价性和工作树归属逐一
  对账；早先已经由 PR #26 合并的遮蔽激活补丁未重复引入，其余通行密钥回焦、媒体准备、图片
  查看器、左滑回复、语音反馈、任务隔离与结构化生产回读已由 PR #28 集中合并并发布。
- 通行密钥候选针对真机 probe 显示的后台恢复 `hasFocus=false` 路径：未聚焦时不发原生请求，
  先请求并等待真实焦点且只启动一次；锁定、隐藏、离页、冻结和超时中止等待或遗留请求。
  既有自动化证明调用顺序和失败关闭，不证明 iPhone 系统验证框必然出现。
- 媒体准备中点按不会提前显露，完整认证、长度/SHA-256 校验与解码完成后仍先显示默认模糊
  缩略图；图片查看器按比例适配且无装饰黑边。左滑回复在手指离开原消息行后继续跟随到松手；
  语音取消阈值为 220 CSS 像素，越界不提前停录，发送/取消状态可回拖切换。
- Safari 的通行密钥选择、Face ID／设备密码提示及浏览器工具栏属于系统 UI；网页不能静默验证、
  强制生物识别或保证原生弹窗呈现。修复后的真实 iPhone iOS 27 Safari 尚未验收，不能把本轮
  自动化、CI 或旧版真机对照写成真机通过。

## 已发布应用与范围

正式来源为 `https://ai.shui.click`。当前完整应用提交
`fd7a8ab7732afff98c4f0a6a3b60c31ea67bf213` 已由
[PR #37](https://github.com/zdaiwmm/shui-IM/pull/37) 合并并部署。服务器
`deployed-at=20260907T005618Z`（08:56:18），08:57:28 独立回读确认版本、镜像、健康、
公开产物与 WebSocket 一致。本次新增范围见页首。

- **设备与验证**：常驻聊天安全码已移除，新增设备流程的六位核对码保留。遮蔽层完成长按后
  移除扩散环／烟花并保留按住提示；v3 原生请求支持 AbortSignal，后台恢复未聚焦时先恢复真实
  焦点再发起且仅发起一次。实际解锁仍在生命周期锁内重新读取当前持久记录；加入前历史不变。
  首次加入和新增设备绑定在 WebAuthn 返回后等待真实可见且聚焦，再继续创建、领取和安全码步骤。
- **键盘和输入栏**：原生视口事件按动画帧合并，按关闭／中间／打开目标态提交；中间态只
  移动固定栏，不重排列表或提前显现输入栏。目标稳定后统一结算留白、贴底、分页、阅读锚点
  和向下按钮；无几何键盘、原生抖动与挂起恢复有界处理。可信点按当前移动聊天输入框可
  创建一次最长 1.2 秒的可见失焦交接，避免误触发白色隐私帘；程序化聚焦、重复失焦、超时、
  hidden 与硬生命周期仍失败关闭。真实但仍报告 visible 的窗口离开可能在此短窗口内延迟
  遮挡，这是有界可用性取舍，不是认证，完整边界见 D-026。照片、空闲语音和向下按钮与
  输入栈共用闲置 84%／聚焦 92% 表面透明度与高光，禁用态不让整块背景变淡，并有无模糊、
  降低透明度、高对比和强制色彩回退。发送滚动时固定 iOS 标题栏和输入工具栏，并抑制延迟
  `visualViewport` 回调造成的二次抖动；关闭引用的键盘意图规则保持不变。
- **菜单、页面和手势**：tips 使用 320ms 对称渐显渐隐与触点变换原点，删除菜单不预选；
  页面进入/退出使用 380ms 双层交接且不露白。左滑回复在离开消息行后仍跟随手指，松手才回弹
  并激活，当前上限为可见视口六分之一并使用连续有界阻尼；语音长拖拽通过蓝/红状态与文案预览
  松手结果，越过阈值不提前停止录音。
- **文件、安全投影与系统阅读器**：聊天文件自动进入本机保险箱文件投影。点击可阅读白名单
  文件时，在受信点击中预开系统阅读器表面，完成逐字节解密和完整性校验后导航至对象 URL；
  PDF、JSON、纯文本、CSV、Markdown 之外仍只下载，不在应用内执行或任意预览。文件按压不再
  缩小内部 UI。
- **媒体与录音**：聊天图片/视频容器与加载后缩略图边界完全一致，缩略图撑满容器；相册最大化
  按宽高适配容器且不添加装饰黑边，支持 1/3×–5× 正常缩放和越界阻尼。冷媒体明确显示下载、
  解密、验证预览阶段，完成后仍默认模糊；已验证密文分块可从本机缓存重新认证，损坏时删除后
  回源一次。图片拖至视口边缘时最低缩至适配尺寸二分之一。录音请求继续使用兼容蓝牙输入的
  通用音频约束，授权失焦交由应用生命周期的一次性前台交接统一处理。
- **备份界面**：备份与恢复页使用与设备管理页一致的标题、分组设置表面、状态和危险操作层级；
  原有备份、恢复码、历史恢复、权限和协议语义保持不变。
- **版本更新与布局动画**：`release.json` 同时驱动应用和 Service Worker 的唯一版本；新版广播
  后显示聊天顶部更新条，更新后首次进入聊天展示有序版本说明。textarea 高度变化与标题栏、
  消息列表和输入工具栏在同一布局帧提交；输入栏按底边定位，输入框伸缩、新消息插入与列表
  位移共用逐帧动画，标题栏和输入工具栏保持固定；减少动态效果时关闭对应过渡。
- **发布治理**：写入任务要求独立 worktree；`npm run deploy:readback -- --sha <精确SHA>` 只读核对
  生产 SHA、容器/Image ID、HTTPS、公开产物与 WebSocket，失败只能重跑回读，不能重发应用。

消息与 MLS 状态的原子持久化、普通重试复用 ID/密文、设备历史边界、原始附件逐字节
校验、恢复范围和创建者权限保持原契约。详细交互以 [PRODUCT.md](../../PRODUCT.md)、
[SECURITY.md](../../SECURITY.md)、[TEST_PLAN.md](../../TEST_PLAN.md) 和 D-024 至 D-030 为准；自动化
不能外推真实 iPhone/Android 手感。

## 验证与生产证据

- PR #37 合并为精确 main `fd7a8ab7732afff98c4f0a6a3b60c31ea67bf213`；该 SHA 的
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34050348842)全部成功，覆盖构建、
  单元／集成、两组浏览器回归、原生通话、凭据扫描、文档检查、生产依赖审计、完整应用汇总与
  最终 `verify`。合并前各来源工作树的 `npm run check:full` 证据已在目标提交历史中记录，
  不能替代精确 main CI。
- 固定入口返回精确 `DEPLOY_OK`／`DEPLOY_VERIFIED`，入口总耗时 92,173ms，服务器发布 59 秒；
  发布目录 `/opt/quiet-room/git-releases/20260907T005618Z-fd7a8ab7732a`，已校验冷备份
  `/opt/quiet-room/backups/predeploy/data-20260907T005618Z-fd7a8ab7732a.tar.gz`。08:57:28 独立
  `READBACK_OK`：维护标记不存在，应用运行且健康、备份运行且无健康探针，二者实际镜像匹配
  `sha256:4b52b219a0cfad66da1a1aca4dd0f54d90056f3fa0b55c7be72476d7f2be9972`；HTTPS、数据库、
  存储、公开／容器产物和 WebSocket 均通过。`admin-enabled=0`、`calls-enabled=0`，TURN 不存在。
- PR #35 [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34034272592)及精确 main
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34034568250)全部成功。首轮 PR CI
  仅暴露既有图库返回夹具在滚动账务提交前读取基线的竞态；未放宽产品断言，修订夹具后完整
  套件通过。最终本地 `npm run check:full` 通过 54 个文件／454 项测试和 24 个浏览器入口。
- 固定发布返回精确 `DEPLOY_OK`、`DEPLOY_VERIFIED`，总耗时 362,808ms，其中等待 main CI
  276,063ms、隔离克隆 7,804ms、部署与入口回读 71,883ms；服务器阶段 58 秒。21:03:09
  独立 `READBACK_OK`，维护标记不存在，应用运行且健康、备份运行且无健康探针，实际镜像匹配
  `sha256:1dd0dbdd60dec11d8ec7c92c917e61a4c1afb88ba868632739d6fc27def9762b`；HTTPS、公开／容器
  产物一致性和公开 WebSocket 均通过。

- PR #31 [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34029235057)、PR #33
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34029933055)及精确 main
  [完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/34030231719)最终全部成功。main 首次
  浏览器第 1 组时序失败经日志分类后只重跑同一 SHA，浏览器两组、完整汇总与 `verify` 均通过。
- 本地集中 `npm run check:full` 从头通过 54 个文件／453 项测试和 24 个浏览器入口（259.98 秒）；
  Docker 修复后 `npm run check` 通过 54 个文件／454 项测试。CI 同时覆盖通话、凭据扫描、生产
  依赖审计和完整汇总。
- 固定发布返回精确 `DEPLOY_OK`、`DEPLOY_VERIFIED`，外层核对回执。总耗时 88,711ms，服务器
  阶段 56 秒；发布目录和已校验冷备份路径见发布记录。
- 19:37:32 独立 `READBACK_OK`，维护标记不存在；应用运行且健康、备份运行且无健康探针。
  实际镜像匹配 `sha256:0d8bc95ce285143a9cffe6dfa82dafd4300ef643ae9afe14ca8a1f286e9b17ff`；
  HTTPS 健康、公开／容器产物一致性和公开 WebSocket 均通过。未读取真实消息或备份内容。
- 上一版 `b2c1b65e6a3ee80dcd6eb4b158e3e8182e2f0e49` 的发布与回读记录保留在
  [上一发布记录](../../RELEASING.md#上次线上发布记录2026-09-06-1910)。

## 知识库治理状态

- 固定发布回执和独立生产回读证明线上应用为 `fd7a8ab7732afff98c4f0a6a3b60c31ea67bf213`；
  本次知识库治理从该精确 main 建立独立分支，只存在于工作树时不算合并或部署。最终文档
  提交在运行时复核，不把包含本页的提交号写成自引用“当前应用值”。
- D-021 已把每次发布后的固定触发点设为外层 `publish.mjs` 核对精确成功回执之后的独立生产回读，并要求在发布任务结束前完成一次轻治理。对账失败与生产失败分开，只重试对应后置步骤，不能重发应用。
- D-025 固化系统阅读器与下载的 MIME 白名单、安全解密交接和保险箱自动文件投影边界；
  D-024 的保险箱本地整理与聊天本地/全员删除边界保持不变。
- D-026 至 D-030 的既有边界不变；本批首次加入／设备绑定回焦、iOS 固定栏、媒体与回复手势、
  备份界面和蓝牙音频授权交接已取得精确 main CI、发布与独立回读证据。真机证据只覆盖页首明确
  列出的输入与发送布局范围，不外推至 Tesla 蓝牙、绑定流程、Android 或其他系统表面。
- 当前事实同步仍由发布任务根据可审查证据完成。仓库内独立结构化生产回读入口已合并、通过
  PR/main CI，并在生产发布后真实取得 `READBACK_OK`；隔离文档 PR 生成仍未实现，因此
  不能声称知识库已经无人值守自动更新。
- 后续实现应只自动写可机械验证的生产事实；产品、安全、真机和独立审计结论继续审阅。知识库文档提交与已部署应用提交始终分开记录，避免自引用发布循环。

## 仍需处理与系统边界

- **真机界面与权限**：除页首已专项验证的 iPhone Safari 多行输入与单行／多行发送布局外，
  真实 iPhone Safari／Chrome 和 Android Chrome 的浏览器工具栏、选择器、权限、播放器、
  后台恢复和其他手势仍未验收。网页不能强制原生工具栏永久透明、关闭
  操作系统自己的弹窗、静默授予或永久保留权限，也不能保证系统输入放大镜和原生播放器手势。
- **后台与恢复**：本次回读 `admin-enabled=0`，后台未启用。`sao.shui.click` DNS／TLS、管理员因素初始化、真实通行密钥与恢复矩阵、灾备恢复及独立密码学审计仍待完成。
- **通话**：本次回读 `calls-enabled=0`，TURN 未启用；公网／蜂窝互通、强制中继、长通话和切网尚未真机验证，见 [CALLS.md](../../CALLS.md)。
- **运维与安全门槛**：证书无人值守续期、异故障域不可变备份与外部告警仍待完成，详见 [OPERATIONS.md](../../OPERATIONS.md)。公开高安全声明继续受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。
- **历史扫描误报**：手动完整历史扫描仍可能命中两处旧测试公开 RFC 向量；本轮正常 PR 与 main 凭据扫描均通过，没有增加豁免或改写历史。出处见 [RELEASING.md](../../RELEASING.md#本次线上发布记录)。

## 接手复核

应用发布目标为 `fd7a8ab7732afff98c4f0a6a3b60c31ea67bf213`；本发布记录随后由独立文档
提交形成，文档 SHA 不代表应用重新部署。原始开发目录仍可能包含不属于本发布的未提交内容，
不得覆盖或混入本次知识库 PR。接手时重新读取 `git status`、HEAD、GitHub main、精确 CI 和
生产 SHA；工作树、本地验证、CI、真机和生产分别取证。后续发布仍遵循
[RELEASING.md](../../RELEASING.md)，不得把本页旧 SHA 当作新的发布授权。
