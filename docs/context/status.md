# Quiet Room 当前状态

当前候选记录快照：2026-09-04 23:45（Asia/Shanghai）；下方已发布版本的生产证据快照仍为 2026-09-04 22:11:28。本页用于接手定位，不代替实时 Git、CI 或生产回读；本地验证和文档更新不代表应用已提交或重新部署。

## 当前候选：移动端交互修整

- 分支为 `codex/mobile-interaction-polish`，起点为 `ee81624666323973f343ea5afc571989342d3a2a`；记录时 HEAD 仍为该提交，应用改动存在于未提交工作树。
- 范围：聊天可见时持续采样工具栏／键盘几何、发送只平移消息内容且回执保留媒体节点；缩小并固定媒体尺寸、图片双指缩放／平移、视频原生全屏优先；语音连续水平阻尼和自然膨胀、156px 左滑取消并移除上滑锁定；相册头部对齐、标签平移、空计数隐藏、页面与菜单淡入淡出；精简通行密钥解锁、本机安全菜单和备份概览。上传点击与真实窗口失焦分开处理，既有认证、加密、历史边界和隐私清理契约保持。
- **本地自动验证通过**：最终应用工作树 `npm run check:full` 一次全程通过构建、43 个测试文件的 311 项单元／集成和 19／19 个浏览器入口，浏览器入口汇总耗时 148.51 秒；`npm run test:calls:e2e` 两项专项、最终 WebKit `frontend-lifecycle.e2e.mjs` 完整通过；最终 UI 巡检生成 64 张截图，记录到的浏览器错误为 0。
- **尚未提交／CI／合并／部署**：上述验证不证明 GitHub PR 或精确 main 提交已通过 CI，也不证明候选已发布。已知已部署应用仍为下节的 `b299824888811477f1ed31d223c1ce02be2af4c7`；本次候选没有新的生产回读证据。
- 真实 iPhone Safari 的工具栏与键盘合成、系统授权、原生播放器手势、跨网络通话及 Mac 锁屏后的持续运行尚未进行人工验收。浏览器工具栏透明度、系统输入放大镜和持久媒体授权仍受平台控制。
- 详细需求映射与本地证据见[本轮交互验收](../../audit/2026-09-04-interactions/README.md)、[测试计划](../../TEST_PLAN.md)；新语音与界面契约见决策 D-017、D-018。以下生产与 CI 记录属于上一已发布版本，不自动适用于当前候选。

## 已发布版本与范围

正式来源为 `https://ai.shui.click`。已部署并独立回读确认的完整应用提交为 `b299824888811477f1ed31d223c1ce02be2af4c7`，来自已合并的 [PR #8](https://github.com/zdaiwmm/shui-IM/pull/8)；服务器 `deployed-at` 为 `20260904T140919Z`（22:09:19）。本次变更包含：

- 键盘、浏览器工具栏与输入栏的几何同步，短对话底部布局、发送后最新消息追随及主动上翻阅读时的追随取消。
- 聊天视频本机预览和查看器播放；保险箱使用照片／视频“相册”，包括已有兼容视频及显式保险箱恢复。预览仅在完整原件校验后于内存中生成，不上传或替换原始字节。
- 聊天图片、相册格和视频静态预览默认模糊；首次点击显露，再次打开，下拉与隐私遮罩重置。相册加载骨架与备份页间距同步调整。
- 长按语音录制并松手发送、左滑取消、上滑松手锁定、暂停试听／续录／发送；补齐锁定后的旧语音绑定释放与重新进入回归，保留迟到授权、系统中断和草稿清理边界。
- CI 并行、文档轻量检查、独立完整发布证据门、发布路径规范化与成功回执、服务器发布阶段计时。

原有 QR3 自动密文备份、恢复后换码、显式历史恢复、桌面隐私恢复与通话能力沿用。服务器和后台仍不保存恢复码；后台和通话中继尚未启用。产品与安全契约见 [决策页](./decisions.md)、[恢复备份契约](../../RECOVERY_BACKUPS.md)、[SECURITY.md](../../SECURITY.md) 和 [CALLS.md](../../CALLS.md)。发布目录、冷备份、helper 摘要及详细回读以[本次线上发布记录](../../RELEASING.md#本次线上发布记录)为准。

## 验证与生产证据

- [PR 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33880450265) 与 [精确应用提交的 main 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33880822033) 均通过：构建、311 项单元/集成、19 个浏览器入口、通话浏览器专项、凭据扫描和生产依赖审计，完整应用汇总任务成功。浏览器两组分别为 1／18 个入口，主流程仍包含语音和通话流程。
- 整合工作树本地构建、311 项单元/集成、19 项浏览器分段覆盖及两项通话浏览器专项通过。首次 `check:full` 在语音时长断言超时，补充合成录音等待余量后专项通过；未将该分段覆盖写成单次全程 `check:full` 成功。阶段性证据见 [测试计划](../../TEST_PLAN.md) 和 [语音交互验收](../../audit/2026-09-04-voice/README.md)。
- 固定发布入口使用默认临时目录完整运行，返回 `DEPLOY_OK` 与 `DEPLOY_VERIFIED`；真实路径入口判断和完成回执已在本次发布中生效。切换前冷备份已校验，维护门及开放流量后的数据保留边界保持既有契约。
- 22:11:28 独立回读确认应用 SHA 和发布目录匹配、维护标记解除、应用容器健康、备份容器运行；两容器的实际 Image ID 均与目标镜像一致。HTTPS 健康的 `ok`、`database`、`storage` 均为 true，公开首页、Service Worker 及首页引用的 CSS／JavaScript 与运行中构建产物逐字节匹配。WebSocket 检查来自发布程序的成功验证。
- root 发布 helper 已从本次完整应用提交独立审阅并原子安装，回读确认摘要一致、属主及权限为 `root:root:755`，旧副本保留；分段计时已实际输出。具体摘要与路径集中在[发布记录](../../RELEASING.md#本次线上发布记录)。
- 浏览器测试使用合成会话、虚拟通行密钥与合成媒体，不等于真机验收或独立安全审计。代码上线不证明每台设备已有有效备份：仍需设备解锁、联网并完成成功上传。

## 提速证据与发布边界

- [PR #7](https://github.com/zdaiwmm/shui-IM/pull/7) 完整 CI 曾实测 2 分 37 秒，对比相同应用代码的原流程 3 分 38 秒缩短约 28%。这是此前相同应用范围的 CI 比较，不外推到本次新增功能后的测试耗时或生产部署加速比例。
- 本次固定发布入口总耗时 92.582 秒，服务器发布阶段 61 秒；其中镜像构建 17 秒、启动容器 32 秒、冷备份 4 秒、拉取代码 5 秒。这些计时不代表整个开发、审核、合并与 CI 的总耗时，也没有建立生产发布提速百分比。
- 文档轻量绿色不能用于应用发布；固定入口仍要求精确 main SHA 的最新完整 CI。手动完整检查不构成发布授权，后续发布仍须确认目标提交。

## 仍需处理的事项

- **恢复与后台验收**：`admin-enabled=0`，`sao.shui.click` 尚未启用；后台 DNS/TLS、管理员密码与 Google Authenticator 初始化、维护门及配置挂载验收仍待完成。真实 iPhone/Android 通行密钥与恢复矩阵、灾备恢复及独立密码学审计也未完成。仅剩旧 QR2 文件与恢复码的用户须在迁移前用受信任旧版本恢复；完整条件见 [恢复备份契约](../../RECOVERY_BACKUPS.md)。
- **界面与媒体真机验收**：真实 iPhone Safari 的键盘／工具栏同步、滑动流畅度、视频解码／播放及语音手势与原生权限交互仍未验证，不能由模拟 viewport 或桌面浏览器结果推断通过。
- **通话中继与真机验收**：`calls-enabled=0`，TURN 未启用。客户端仍可尝试直连；公网／蜂窝跨网络、强制中继、长通话与切网可靠性尚未验证。详见 [CALLS.md](../../CALLS.md) 与 [DEPLOYMENT.md](../../DEPLOYMENT.md)。
- **既有运维待办**：证书自动 DNS 续期、异故障域不可变备份与外部告警尚未完成；本次发布未改变这些配置。详见 [OPERATIONS.md](../../OPERATIONS.md)。
- **公开高安全门槛**：独立审计、独立可信客户端、真机矩阵及运营要求仍受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。

## 接手时复核

上一次生产发布完成时，本地 main 已同步上述已部署应用提交；其后发布记录在独立文档分支更新，不代表应用再次部署。当前候选分支与验证层级见本页顶部。重新接手须读取 `git status`、本地 HEAD、GitHub main、目标 CI 和实际生产 SHA；文档版本与已部署应用提交可以不同。后续发布仍须取得当次授权、确认完整目标提交并遵循 [RELEASING.md](../../RELEASING.md)，不能把旧提交号当作新一次发布授权。原有功能分支和独立 worktree 保留；不得将其他任务改动混入候选。
