# Quiet Room 当前状态

生产证据快照：2026-09-05 18:08（Asia/Shanghai）。本页用于接手定位，不代替实时 Git、CI 或生产回读；后续发布记录文档提交不代表应用重新部署。

## 已发布应用与范围

正式来源为 `https://ai.shui.click`。完整应用提交 `2738893b395082504603d4fb0a6360e85d4c4a3f`
已由 [PR #22](https://github.com/zdaiwmm/shui-IM/pull/22) 合并并部署。服务器
`deployed-at=20260905T100553Z`（18:05:53，发布批次时间），18:08 前的独立回读确认版本、
镜像、健康、公开产物与 WebSocket 一致。本轮在上一生产版本上集中补齐设备验证入口、键盘
连续性、tips/页面/遮罩动效、文件系统阅读器、安全投影、媒体容器与预览、手势和蓝牙录音约束。

- **设备与验证**：常驻聊天安全码已移除，新增设备流程的六位核对码保留。遮蔽层完成长按后
  在同一受信点击链路准备设备密码验证，避免浏览器偶发不显示；验证等待不再占住跨标签页
  的保险箱生命周期锁。加入前历史边界不变。
- **键盘和输入栏**：顶部导航与输入栏在可视视口变化时同步固定，消息列表只在 160ms 稳定后
  对齐，减少点输入框白屏、收键盘时输入栏居中和标题抖动；位于底部时保持贴底。输入栏不透明度
  提高；关闭引用仅在键盘原已展开时保持键盘，原未展开时不触发抖动。
- **菜单、页面和手势**：tips 使用 320ms 对称渐显渐隐与触点变换原点，删除菜单不预选；
  页面进入/退出使用 380ms 双层交接且不露白。语音整块气泡可长按；左滑回复加长拖拽距离，
  仅在松手后回弹并激活。遮蔽长按反馈扩大，并在空白区域播放烟花。
- **文件、安全投影与系统阅读器**：聊天文件自动进入本机保险箱文件投影。点击可阅读白名单
  文件时，在受信点击中预开系统阅读器表面，完成逐字节解密和完整性校验后导航至对象 URL；
  PDF、JSON、纯文本、CSV、Markdown 之外仍只下载，不在应用内执行或任意预览。文件按压不再
  缩小内部 UI。
- **媒体与录音**：聊天图片/视频容器与加载后缩略图边界完全一致，缩略图撑满容器；相册最大化
  按宽高适配容器，弱化所有媒体高斯模糊，并为缩放加入越界阻尼。视频海报尝试有界后续帧，
  避免未解码首帧黑图；录音请求改为兼容蓝牙输入的通用音频约束。

消息与 MLS 状态的原子持久化、普通重试复用 ID/密文、设备历史边界、原始附件逐字节
校验、恢复范围和创建者权限保持原契约。详细交互以 [PRODUCT.md](../../PRODUCT.md)、
[SECURITY.md](../../SECURITY.md)、[TEST_PLAN.md](../../TEST_PLAN.md) 和 D-024/D-025 为准；自动化
不能外推真实 iPhone/Android 手感。

## 验证与生产证据

- [PR #22 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33959213106) 与
  [精确 main 完整 CI](https://github.com/zdaiwmm/shui-IM/actions/runs/33959422621) 均成功；后者
  对应上述应用 SHA，通过构建、402 项单元/集成、两组共 23 个浏览器入口、原生通话、凭据
  扫描、生产依赖审计及 `Full application verification`／`verify` 汇总。
- 冻结候选的最终本地 `npm run check:full` 从头通过 51 个测试文件、402 项测试和 23／23 个
  浏览器入口，浏览器部分 223.86 秒；系统阅读器、文件交互、视频与浏览器专项在本机系统
  Chrome 通过。首次 CI 暴露弹窗时序和用户激活问题后修正产品实现，最终 PR/main 全量均通过。
- 固定入口返回 `DEPLOY_OK` 与 `DEPLOY_VERIFIED`，总耗时 89,010ms；部署及入口回读
  70,373ms。服务器部署阶段 57 秒，发布目录为
  `/opt/quiet-room/git-releases/20260905T100553Z-2738893b3950`，已校验冷备份为
  `/opt/quiet-room/backups/predeploy/data-20260905T100553Z-2738893b3950.tar.gz`。
- 独立只读回读返回 `READBACK_OK`：维护标记不存在；应用容器运行且健康，备份容器运行且
  没有健康探针；两者实际 Image ID 均匹配目标镜像
  `sha256:070b2adae9261dca00d69c4e3c1220a6e140cd8d3ed7552c84cc0512a53df363`。
  HTTPS `ok`／`database`／`storage` 全为 true，首页、Service Worker、CSS、应用 JavaScript
  和 preload 脚本与容器产物逐字节一致，公开 WebSocket 独立连接成功。回读未访问真实消息、
  附件或备份内容。
- 交付时长与 token 成本复盘见[2026-09-05 交付复盘](../../audit/2026-09-05-mobile-ux-delivery-retrospective.md)。
  真实 iPhone/Android、原生键盘/选择器/播放器手感和独立安全审计仍未完成。

## 知识库治理状态

- 固定发布回执和独立生产回读证明线上应用为 `2738893b395082504603d4fb0a6360e85d4c4a3f`；
  本次知识库治理从该精确 main 建立独立分支，只存在于工作树时不算合并或部署。最终文档
  提交在运行时复核，不把包含本页的提交号写成自引用“当前应用值”。
- D-021 已把每次发布后的固定触发点设为外层 `publish.mjs` 核对精确成功回执之后的独立生产回读，并要求在发布任务结束前完成一次轻治理。对账失败与生产失败分开，只重试对应后置步骤，不能重发应用。
- D-025 固化系统阅读器与下载的 MIME 白名单、安全解密交接和保险箱自动文件投影边界；
  D-024 的保险箱本地整理与聊天本地/全员删除边界保持不变。
- 当前事实同步仍由发布任务根据可审查证据完成。工作树已新增仓库内独立结构化生产回读入口，包含阶段耗时、稳定失败分类、`.git` 内脱敏证据和只重跑回读的安全续跑；本地 `npm run check` 已通过 52 个测试文件、419 项测试，发布/回读定向 33 项、`git diff --check` 及将当前 `workflow.md` 纳入 tracked 集合的隔离文档检查也通过。直接文档检查仍会在该既有未跟踪文件正式纳入版本控制前按设计报缺失；本轮改动尚未合并、经 CI 或用于新的真实生产回读。隔离文档 PR 生成尚未实现，因此不能声称知识库已经无人值守自动更新。
- 后续实现应只自动写可机械验证的生产事实；产品、安全、真机和独立审计结论继续审阅。知识库文档提交与已部署应用提交始终分开记录，避免自引用发布循环。

## 仍需处理与系统边界

- **真机界面与权限**：真实 iPhone Safari／Chrome 和 Android Chrome 的工具栏、键盘、
  选择器、权限、播放器、后台恢复和手势尚未验收。网页不能强制原生工具栏永久透明、关闭
  操作系统自己的弹窗、静默授予或永久保留权限，也不能保证系统输入放大镜和原生播放器手势。
- **后台与恢复**：本次回读 `admin-enabled=0`，后台未启用。`sao.shui.click` DNS／TLS、管理员因素初始化、真实通行密钥与恢复矩阵、灾备恢复及独立密码学审计仍待完成。
- **通话**：本次回读 `calls-enabled=0`，TURN 未启用；公网／蜂窝互通、强制中继、长通话和切网尚未真机验证，见 [CALLS.md](../../CALLS.md)。
- **运维与安全门槛**：证书无人值守续期、异故障域不可变备份与外部告警仍待完成，详见 [OPERATIONS.md](../../OPERATIONS.md)。公开高安全声明继续受 [PRODUCTION_SECURITY_GATE.md](../../PRODUCTION_SECURITY_GATE.md) 阻塞。
- **历史扫描误报**：手动完整历史扫描仍可能命中两处旧测试公开 RFC 向量；本轮正常 PR 与 main 凭据扫描均通过，没有增加豁免或改写历史。出处见 [RELEASING.md](../../RELEASING.md#本次线上发布记录)。

## 接手复核

应用发布目标为 `2738893b395082504603d4fb0a6360e85d4c4a3f`；本发布记录随后由独立文档
提交形成，文档 SHA 不代表应用重新部署。原始开发目录仍可能包含不属于本发布的未提交内容，
不得覆盖或混入本次知识库 PR。接手时重新读取 `git status`、HEAD、GitHub main、精确 CI 和
生产 SHA；工作树、本地验证、CI、真机和生产分别取证。后续发布仍遵循
[RELEASING.md](../../RELEASING.md)，不得把本页旧 SHA 当作新的发布授权。
