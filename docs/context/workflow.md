# Quiet Room 任务执行与成本控制

2026-09-06 起试行。目标是降低每个验收通过任务的总时间与 token，不能通过降低安全、测试或发布门槛换取速度。本页负责执行策略；详细验证、失败分类和发布规则沿用 [maintenance.md](maintenance.md)、[overview.md](overview.md) 与 [RELEASING.md](../../RELEASING.md)。

## 用户如何开始

### 当前分支模型

2026-09-09 用户确认采用轻量 trunk-based 流程。本节替代下文及局域网流程中将本机 `main`
作为默认集成与验收环境的旧表述，固定交付流程的安全和发布门禁继续有效：

- 任务使用独立 worktree 和短生命周期 `codex/<task>` 分支；本地服务直接从任务 worktree 验证。
- 不维护长期 `dev` 分支，本地 `main` 仅作为同步基线。只有多任务联调才新建临时组合 worktree。
- 验证后通过集成 PR 或 Merge Queue 与 CI 合并远端 `main`；执行下方持续远端交付授权，当前任务明确限制优先。
- 真机调试、稳定 HTTPS 来源、服务健康回读、风险匹配测试继续沿用；不为每个分支常驻一套服务。
- 精确提交发布确认、完整 CI、固定发布入口、独立生产回读、知识库对账和资源清理规则保持有效。

本次从旧基线的 `2b776720e290363096386c7df6a87eb13647d071` 适配规则，保留随后已合入的
固定交付、隔离和安全要求；下文旧本机 main 步骤仅在当前任务明确要求该环境时适用。

在包含本页的 Quiet Room 项目中开始任务，用户只需描述问题、期望结果和已有截图/录屏；无需指定分支名、Git 命令或重复工作流。下面的模型配置由用户在应用中选择；分支、隔离、提交和已授权集成由 agent 按下一节处理。先按 `AGENTS.md` 的 R/S/I/D/P 任务模式及下方需求确认节点选择流程；R 模式只读结束，不启动交付阶段。任务涉及多个故障域时先拆批；直接相互依赖的变化留在同批。

固定默认项不要求用户重复输入：完成适用需求／设计确认并获实施授权后，对 I/D 模式先复现并确定最小修复，完成风险匹配自动验收；仅在 D 模式、且当前请求属于实施或交付时，在独立 worktree 提交后推送本任务分支。无关联的小任务进入远端 Merge Queue；有关联的并发任务进入一次性集成 PR，等待 CI 后合并远端 `main`。本地服务从任务 worktree 验证，本机 `main` 仅同步基线。旁支问题先记录，不扩大本批范围；生产发布需要另外授权和精确提交确认。

### 持续远端交付授权

2026-09-09 用户明确要求将自动验收、推送、创建 PR、等待 CI、合并固定为后续默认流程，并要求不再逐项询问。此为针对 `https://github.com/zdaiwmm/shui-IM.git`（同一仓库的 SSH 地址等效）的持续授权，后续会话从本机 main 读取后沿用；它替代旧规则中“默认不 fetch/push/PR/合并”或“每次重新授权远端交付”的表述。

- 启动条件：完成[需求澄清与设计确认](#需求澄清与设计确认)中的适用确认并获正式实施授权；提出问题、提出需求或仅确认可以制作原型，不启动本节自动交付。S 模式只按设计阶段的材料同步权限执行。
- 范围仅限用户交办的本任务及其必要修复、测试和知识库更新；允许核验远端、同步主线、提交、推送任务分支、创建或加入集成 PR／Merge Queue、等待必需 CI 并在通过后合并 main。不得顺带纳入其他任务、强推、直接推送 main 或绕过分支保护。
- 自动验收属于交付必做项：按风险运行现有测试门禁，UI 改动补相关浏览器验收；缺少依赖时按锁文件安装。每类失败最多进行一次有针对性的修复和重跑；重复或环境、权限、外部服务性失败分类记录后停止，不修改无关代码。测试失败不能只写“未运行”便结束，也不能以单元测试通过替代交互验收。
- 核对精确候选、PR head、最新 CI 和可合并状态后执行合并并回查合并结果；候选变化后重新评估受影响验证。报告代码 SHA、PR、测试、CI 和实际交付终点，真机缺口单列。
- 只分析、只出方案、不提交、不推送、暂不合并等明确限制优先；纯问答不触发写入。生产发布、其他仓库和未确认的产品或安全语义不包含在本授权中。
- 沙箱或自动审批出现阻塞时先提交已有授权和精确远端证据申请必要权限；不能绕过拒绝。只有确实无法继续时报告具体阻塞，不将同一已授权动作重新当作普通确认问题。

这是每次 I/D 实施任务内按模式执行的交付规则，不是定时后台任务，不额外创建调度器；R 模式不进入本节的远端动作或 CI 等待。

移动端交互问题默认目标环境为用户提供的 **iPhone / iOS 27 / Safari**（2026-09-06 用户提供，未独立核实具体设备和版本），用户无需每次重写。明确指定其他环境时，本任务按指定环境复现和验收；单次覆盖不自动改变长期默认。服务端、协议、文档和运维任务不强行套用手机环境。

默认环境决定复现与验收重点，不表示能访问对应真机，也不缩减既有风险匹配回归。不能把桌面 WebKit、模拟 UA 或其他版本通过写成该真机通过。只有具体机型、版本或运行方式确实影响定位且现有材料不足时，才询问必要信息；缺少复现证据时先报告缺口，不声称已复现。

| 类型 | 建议起始配置 | 使用范围 |
| --- | --- | --- |
| 常规开发 | GPT-5.6 Sol / medium | 普通功能、明确根因的局部修复、常规审查 |
| 疑难或安全 | GPT-6 Astra / high | 键盘与生命周期难题、并发状态、MLS、恢复、设备历史、存储与部署异常诊断 |
| 机械整理 | GPT-5.6 Luna / low | 已核实证据摘要、链接修正、无产品语义变化的文档同步 |

这些是试运行建议，不是本项目性能排名。以应用实际可选项为准；不默认使用 xhigh、max 或 ultra。模型和强度需在应用任务设置中选择，写入本页不会自动改变配置。无法读取实际配置时标记“未知”，不能把建议值当作实际值。

短任务保持同一配置，不为每个节点新建会话。复杂任务可在根因明确后采用 Sol / medium 实现，但只有预计后续工作足够多、值得交接时才切换。默认单 agent，本页不授权自动派生子 agent。强度提升不能代替缺失的设备或运行证据。

官方依据：[推理强度与成本](https://developers.openai.com/api/docs/guides/deployment-checklist#set-up-reasoningeffort)、[Astra 测试与验证指导](https://developers.openai.com/api/docs/guides/latest-model#testing-and-verification)。API 能力说明不等于桌面应用已具备自动路由功能。

## 需求澄清与设计确认

2026-09-15 用户确认本流程及轻量保存修订，适用于 `zdaiwmm/shui-IM` 的全部变更需求，包括功能、缺陷、页面、重构、配置、依赖、测试、文档和协作规则。本节替代“收到常规需求即可直接实施”的旧约定；不降低既有隔离、安全、测试和生产发布门禁。

### 阶段与授权

| 阶段 | agent 的动作与产物 | 允许推进的条件 |
| --- | --- | --- |
| R 调查与澄清 | 只读核对现状，复述目标，区分事实、假设及未知；先自行查明仓库可回答的问题，再分轮追问影响方向的细节 | 不修改文件；纯问答、排查或审计可在此结束。发现修复机会先提方案 |
| 第一次确认 | 给出目标、范围／排除项、具体行为、异常边界、实现思路、关键取舍、兼容及风险、验收标准和下一步权限 | 用户确认明确版本和范围；简单需求可一轮完成，不因简单自动免确认 |
| S 原型设计 | 涉及页面新增／优化时，制作可交互原型和业务流程图，记录设计依据及模拟限制，供用户体验和审阅 | 第一次确认只授权设计；在独立任务 worktree 制作本需求材料，不修改正式业务代码／接口／依赖／生产配置 |
| 第二次确认 | 一并展示对应版本的需求、原型、流程图、遗留限制，确认行为和视觉结果 | 用户明确认可当前版本并允许实施；仅认可视觉不等于确认未解决的业务规则 |
| I 正式实施 | 按已确认内容修改；维护最小需求记录，逐项核对需求、流程与原型，完成风险匹配验证 | 非页面需求在第一次确认包含实施授权后进入；页面／混合需求先完成第二次确认 |
| D 远端交付 | 按持续授权提交、推送、PR、CI、合并并回查 | 适用确认和正式实施授权均具备；仅设计材料同步不授权合并 main |
| P 生产发布 | 沿用固定发布、独立回读与对账 | 用户另外确认本批精确提交；需求／原型认可不授权生产发布 |

页面包括页面内弹窗、面板、导航、文案反馈与交互状态的新增或优化。混合需求先确认相关页面，再进入本项正式实施；若需独立先做非页面部分，先明确拆分范围及授权。

第一轮追问优先解决目标、操作对象、触发条件、作用范围、业务／数据／安全边界和成功标准。给出建议与理由，不把所有实现细节转交用户；方向或关键规则仍未知时不将推测写成已确认需求。必要的只读调查可继续；复现需要写文件、启动会写数据的服务或进行验证实验时，先说明最小实验范围并取得对应授权，不借调查提前实施。

### 原型与业务流程图

- 默认交付最小可运行的 HTML/CSS/JavaScript 原型，附启动方式和必要版本信息；不要求另一台电脑安装某个个人 Skill 或付费设计工具才能查看。仅制作本需求受影响的界面和流程，不复制整个项目。
- 原型主要操作可点击、路径可走通，返回／取消／关闭行为明确；按需求覆盖加载、空状态、成功、失败、重试及权限限制。覆盖受影响的移动／桌面尺寸、浅色／深色主题和必要无障碍状态。
- 先核对根 `DESIGN.md`、`PRODUCT.md` 的相关契约、实际生效的样式及现有组件／页面。共享 token 依据 `src/design-system.css`，并核实入口加载与页面覆盖规则；颜色、字体、间距、圆角、图标、动效沿用项目规范。记录参考基线和相关路径；文档／实现漂移或新增视觉方向须明确并确认，不自行裁定。
- 使用模拟数据及独立预览入口，不接入真实聊天、恢复材料或生产写接口；避免导入会初始化真实业务、Service Worker 或真实存储的应用入口。不将原型纳入生产路由、公共产物或部署流程。
- 流程图默认保存 Mermaid 源码并提供可查看的渲染结果；呈现参与者、入口、前置条件、步骤、分支及结果，按需包括取消、失败、重试、多设备、锁定和恢复。步骤与原型、验收项能够对应。仅样式调整也标明所在业务流程及行为保持范围。
- 交付前自行核验主要路径、状态切换与视觉依据；列明模拟能力、未覆盖内容和真实设备限制。系统权限弹窗、键盘及安全行为的模拟不作为真实实现或真机证据。工具受限时报告缺口，不用静态截图冒充可交互原型或自行跳过确认。

### 确认、变更与接续

- 使用[需求记录模板](../requirements/TEMPLATE.md)。确认必须对应方案版本、原型／流程图版本（不适用则说明）、范围、确认时间、用户表达的最小脱敏摘要与下一步权限；引用可核实的会话标识或消息定位（有则记录，无则标明未提供），不要求固定口令，不臆造签字或确认。
- 沉默、等待超时、自动工具审批、CI 通过、代码存在、相似历史任务均不算确认。“方案认可但先不做”保持待执行；“原型好看”不扩展到业务或实施授权。原有用户明确授权持续有效，不对同一版本和范围重复询问。
- 范围、业务／数据／安全规则、关键交互、视觉方向或验收结果变化时，暂停受影响实施，更新版本与差异，只重新确认受影响部分；无关部分沿用确认。主线变化只有影响上述结果或可行性时才使相关确认需要复核。不影响结果的技术细节和常规测试调整由 agent 处理。
- 用户可以明确指定本次跳过原型或直接实施；记录例外的范围及依据，不自动改变长期默认。简单、紧急或已写代码不是 agent 自行豁免理由；安全与生产发布门禁仍有效。
- 状态明确区分：澄清中、待需求确认、原型制作中、待设计确认、已授权实施、实施中、待验收／交付、已合并、已发布、阻塞或取消。状态不能代替证据，不把原型完成写成产品完成。
- 换电脑／换会话先核对规则来源、需求记录、实际分支／HEAD、确认版本与范围和下一步；本机 Git 交接清单不自动跨电脑同步。记录缺失或矛盾时标为未知，只澄清缺失的授权，不能凭代码或分支名推断。旧已合并任务不补造历史确认，活动任务按可核实已有授权接续。
- 实施结束以验收项逐项对应实现、原型／流程版本及验证证据，列出差异和未验证项。测试、浏览器、真机、CI、合并和生产各自记录；保留后文风险匹配门禁。

### 需求材料的轻量保存

- 纯讨论不自动写入。进入 S 或 I 后，一项需求在 `docs/requirements/<需求编号>/README.md` 保存最小方案与确认摘要；需要时同目录保存 `flow.md` 和 `prototype/` 源码，引用现有产品契约而不复制长文档。模板不代表已经确认。
- S 模式可将本需求必要记录与原型源码提交、推送到独立任务分支用于审阅／跨电脑接续；明确标注待确认版本，默认不合并 main、不启动正式实施，也不因材料同步执行产品全量本地测试或等待交付 CI。远端自动触发的 CI 仍按既有配置运行。进入 I/D 后随本任务交付必要确认材料。
- 按节点保存需求确认稿、待审原型及最终确认稿；不逐轮提交每次探索。跨电脑需要接续时只补同步必要中间状态。同路径持续修改，由 Git 管版本，不复制 v1/v2/最终版等整套目录；版本标签或提交／内容摘要用于识别确认对象。
- Git 默认只收简短 Markdown、Mermaid 与最小原型源码。优先引用仓库既有样式和资源并记录基线，不复制完整项目、依赖、构建产物、缓存或字体。截图／录屏／大型图片及演示素材默认本地保存；确需长期跨电脑访问时按已确认存储方式另存，仓库只记稳定定位与脱敏说明，不上传未经授权的内容。临时原型和本地媒体放在不纳入版本控制的位置。
- 提交前检查本次新增／修改文件大小、总增量、二进制、内嵌 Base64 素材及误纳入的依赖／产物；发现异常增长先改用源码、已有资源或最小占位素材。不能通过压成大包规避检查。未确认必须纳入的大文件不提交。
- 删除当前文件不会清除 Git 历史占用；不自动重写历史或引入 Git LFS。未来大量二进制管理需求另行评估。本条是 agent 的审查要求，不声称已有自动体积或人工确认校验器。

## 执行顺序

1. 接手：复核工作树和 HEAD，按项目地图读取必要资料，并只读核对本机 `main`、本机 `origin/main` 与 GitHub 实时 `main` 的 SHA 和时间线；所有写入任务必须先通过下节开工隔离门槛，不能等发现同文件冲突后才隔离。进入任务树后运行 `npm run repo:doctor -- --local`，检查本机 Node、公共及工作树 Git 元数据写入、开发工具；远端不可核实时保留未知状态，不能把本机旧提交当作当前基线。
2. 定义完成：按上方需求澄清与设计确认给出方案与验收，完成适用确认并获实施授权。已确认的任务卡不额外重复审批；未确认时不得直接实施。
3. 诊断：先保留复现步骤与失败证据，提出可检验的假设，再修改。移动 UI 明确设备、系统、浏览器；模拟环境通过不能写成真机验收通过。
4. 实现：围绕当前验收做最小改动。旁支问题记录为后续项；只有阻断当前目标或必要的安全修复才纳入，并说明原因。
5. 验证：开发中定向反馈，候选冻结后按既有门禁执行。纯文档用文档检查；普通代码通常 check；跨页面、生命周期、附件等用 check:full；通话按地图补专项。
6. 审查与交付：审查具体正确性和安全问题；通过后不反复扩大审查。记录证据层级、已解决内容、未验证范围。仅 D/P 模式执行远端交付或发布步骤；发布必须满足既有授权与精确提交要求。

## 固定交付流程

2026-09-08 用户要求固定整条流程并让后续新会话读取；2026-09-09 进一步确认上方持续远端交付授权。本节复用现有脚本，
每个任务先核对当前用户请求的范围、适用需求／设计确认及明确限制；获得正式实施授权后自动执行本任务远端交付，
不能把其他任务的发布授权当作当前授权。生产仍每次确认精确 SHA，
部署细节以 [RELEASING.md](../../RELEASING.md) 为准。没有相应授权时停止在已授权阶段。

### 阶段与完成证据

新会话首先报告：`规则来源 SHA；实际工作目录／分支／HEAD；未提交状态；当前任务目标与授权范围`。R 模式用一行报告即可；S/I/D/P 模式再按阶段记录必要证据。
同一轮执行只更新变化项。按下表推进，每个阶段分别记证据，不能只写“完成”。

| 阶段 | 固定动作 | 完成证据与边界 |
| --- | --- | --- |
| 接手 | 一次读取 Git 状态、HEAD、远端地址、worktree 清单和状态页，按项目地图补读必要文档 | 明确规则版本、目录归属与任务范围；不读取凭据、消息或备份内容 |
| 需求与设计确认 | 按上方确认流程确定方案；页面需求另做原型和业务流程图 | 记录版本、用户确认摘要及下一步权限；S 材料同步不授权正式实施／合并 |
| 连接与同步 | 用 `npm run repo:doctor` 核验 GitHub 并获取需要的远端引用；干净 main 用 `npm run repo:pull` 快进 | 同时记录本机 main、本机 origin/main 和 GitHub main SHA；fetch 只更新引用，不声称工作目录已切换；ahead／diverged 转独立集成；远端未知不得创建依赖旧基线的任务树 |
| 盘点 | 优先读取共享交接记录和待合并 PR，再查本批任务；固定分支、head、依赖、测试与去向 | 分类为待纳入、已纳入、明确排除、归属待确认；不能把所有旧分支默认合入 |
| 集成 | 在当前任务的集成授权范围内，从核实基线新建独立 worktree，按依赖顺序合入，解决技术冲突，冻结候选 | 不修改其他任务现场；产品／安全语义冲突先澄清；保存源 head 到集成提交的映射 |
| 候选验证 | 用户可见发布先更新 `release.json` 唯一 ID 和说明；按风险运行现有门禁 | 记录命令、候选 SHA／文件树、结果与真机缺口；失败先分类 |
| PR | 当前任务获推送授权后用 `npm run repo:push` 推送已提交任务并创建 PR；必需检查通过且获得合并授权后合并 | 核对 PR 精确 head、CI 与可合并状态；合并时匹配已审阅 head，不用管理员绕过保护 |
| main 核验 | 回取合并提交，在本机集成授权范围内快进干净 main；准备发布时等精确 main 最新完整 CI | PR CI 不替代精确 main CI；仅同步 Git 不声称本机服务或真机已验收 |
| 发布确认 | 准备完整 40 位 SHA、版本 ID、变更范围、CI、预检和未验证项，请用户确认本次发布提交 | 本批目标改变才重新确认；main 后续非必要提交进入下一批，旧发布记录和本流程都不是确认 |
| 发布与回读 | 确认后只用 `node scripts/publish.mjs --sha <SHA>`，核对精确成功回执后独立 `npm run deploy:readback -- --sha <SHA>` | 分别取得 `DEPLOY_VERIFIED` 与独立 `READBACK_OK`；不从本地脏目录传代码到生产 |
| 对账与收尾 | 第二个不含部署配置的工作树更新发布记录和状态，文档检查、PR、CI，在相应授权范围内合并；再按已有清理规则核验资源 | 线上应用 SHA 保持本次已发布值；文档提交不触发再次部署；清理或保留均报告原因 |

### 固定批次发布（2026-09-10）

用户确认：**main 持续集成，生产按固定批次发布最新已验证版本**。发布准备期间完成的非必要改动进入下一批，不追逐不断变化的 main。

- 相关并发任务先固定源提交，在独立集成分支集中解决冲突、运行组合验证，然后提交一个集成 PR；开发会话不再分别反复同步 main。不能先写 main 再补 PR。互不相关的小修复进入 Merge Queue 独立交付。
- 批次冻结时记录完整目标提交、纳入功能、明确排除项、版本 ID 和验证证据。后续提交不自动进入本批；确需加入阻断修复时重新冻结并验证，发布目标变化后重新确认。
- 当前 main 前进不使原精确提交的授权失效。批次冻结后无关变化进入下一批，不要求当前候选追赶；只有同文件、依赖、安全或行为变化确实阻断时，才重建一次集成候选并重新冻结。发布入口校验获批提交仍是 main 的祖先、其最新完整 main CI 成功，并在隔离副本固定该提交；不改发最新 main。若生产已是同版只做回读；若生产已超过或分叉于候选则拒绝常规发布。
- 发布保持单写者；不要同时启动多个生产发布任务。现有服务器锁保留；本次没有修改或安装 root helper，也不实现持续自动发布。
- 发布版本在冻结前由 `npm run release:prepare -- --base <任务HEAD> --notes <JSON数组文件>` 生成计划，审阅打印的计划后用同一 base 加 `--apply <计划文件>` 应用。工具统一分配 UTC 日期序号、秒级时间戳并原样保留旧版本对象；文件变化拒绝旧计划，同一计划可幂等续跑。分支间仍通过集成审查解决版本碰撞，工具不声称提供跨机器全局锁。
- 冻结后用 `npm run delivery:evidence -- run check:full` 等允许的现有 npm 门禁执行并自动记录 SHA、文件树、结果和耗时。`summary` 汇总本工作树记录，`summary --remote` 另读取 GitHub CI/PR 元数据。证据保存在工作树 Git 目录，不写原始输出、密钥或生产内容；脏树及不同文件树结果标为不适用，CI 摘要不代替发布器的完整 verify 校验。
- 不为补自动验收汇总另建产品 PR；PR 描述引用上述结果即可。发布后的事实对账继续只修改既有允许文档。真实设备、发布与回读证据独立记录，不由单元或模拟测试推断。

### 避免重复与误合并

- **连接复用已有配置**：复用已验证的仓库 SSH 配置或现有认证。Git SSH 与 GitHub CLI API 认证分别核验，
  不为每条命令重建密钥或重复登录。GitHub 的 `ssh -T` 应结合认证成功提示判断，不能仅按非零退出码认定失败。
  网络／沙箱失败与密钥错误分开诊断；不输出 token 或私钥，不关闭主机校验。
  配置路径按部署文档复用，不把本机路径硬编码进仓库，也不向文档工作树复制部署配置。
- **先祖先、后补丁**：`git branch --no-merged` 只说明图关系，不等于任务未集成。
  已是 main 祖先的任务不重复合；squash／cherry-pick 结合 PR head、合并提交、文件树或 `git range-diff` 核对。
  `git cherry`、相同标题或相似 diff 只能辅助定位，不能独立证明所有功能已纳入或允许删除旧分支。
- **未提交内容先保留**：对归属不明且非本次必需的改动明确暂不纳入，继续处理已确认任务；
  不自动 stash、移动、暂存或重放旧补丁。它确实影响本批验收或用户要求纳入时，先确认范围再适配新基线。
- **验证针对冻结候选**：开发中定向测试，冻结后跑风险匹配门禁；相同文件树已通过且没有新证据时不重复本地全量。
  在有推送授权时，可让 PR CI 与本地验证同时运行，但本地必需验证未完成前不合并。
  纯知识库改动只跑文档检查；CI 按既有策略选择全量时正常等待，不临时修改白名单或跳过门禁。
- **等待复用同一进程**：用 `gh run watch <run-id> --exit-status --interval 30` 等待；
  工具返回控制权后保留同一会话，不重复开启 watcher。结束后核对最新 run、attempt、SHA 和完整汇总。
- **证据新旧分开**：操作前复核实时 main 和生产，其他任务可能已更新它们；历史状态页不是锁。
  文档对账提交领先应用版本是正常现象，不能因此产生第二次发布。

### 中断与失败续跑

接续时恢复本任务精确提交、PR／CI run、发布／回读证据，核对当前任务的授权记录和外部状态。
无法核实的授权或结果标为未知；不能因为相似任务过去获准而继续写入。共享交接登记沿用下文固定字段，
自动测试、PR、CI 和生产回执保存在现有载体，不再造一套“已完成”的真相来源。

| 已有证据／失败位置 | 下一步 |
| --- | --- |
| 本地端口 `EPERM`、下载或网络失败 | 记录环境失败并申请必要执行权限；重跑受阻验证，不改产品断言 |
| 冻结候选的必需测试失败 | 分类并修复，候选改变后重验；不把旧 SHA 成功结果挪给新 SHA |
| PR 已合并、main CI 未完成 | 找到该合并 SHA 的最新 CI 并继续等待；不重复建 PR 或合并 |
| 发布未进入切换，预检失败 | 处理明确原因；核实本次获批 SHA 与 main 后使用同一固定入口 |
| 已进入切换但回执不明 | 先独立回读，记录 `PRODUCTION_STATE_UNRESOLVED`；禁止盲目重发 |
| 已有 `DEPLOY_VERIFIED`，独立回读失败 | 同一 SHA 只重跑回读；linked worktree 的证据写入限制按 RELEASING.md 在精确发布克隆处理 |
| 已有 `READBACK_OK`，对账／文档 PR 失败 | 保存同一份脱敏证据，标记 `CONTEXT_SYNC_BLOCKED`；只继续对账，不再发布 |
| 对账已合并、清理保留／失败 | 只检查本批精确资源；活动目录、未知文件、忽略文件等按工具结果保留，不强删 |

最终报告包含实际完成的终点、代码／文档 SHA、验证与生产证据、未纳入内容和未完成项。
用户不需要手动选择 Git 命令；agent 负责在当前任务明确授权的范围内执行固定步骤。

## 分支与集成由 agent 管理

整条交付的阶段入口见上方[固定交付流程](#固定交付流程)；本节继续定义隔离、集成与授权边界。

本节固定用户已明确提出的偏好：用户专注问题与期望结果，不必手动创建或合并分支。例行本地分支、worktree、任务提交与授权范围内的集成不再逐项询问。明确要求只分析、只出方案、不提交或不合并时，以该次要求为准。

### 开工隔离门槛

本机环境准备与后续阶段的权限处理统一按下节[本机工作树与依赖准备](#本机工作树与依赖准备)执行。

适用于代码、测试和文档写入。纯问答和只读调查无需创建。此门槛优先于后文的环境复用描述。

1. 修改前执行 `git status --short`、`git rev-parse --show-toplevel`、`git branch --show-current`、`git rev-parse HEAD` 和 `git worktree list --porcelain`。明确实际执行目录，不能以应用任务名称代替路径核验。
2. 仓库的共享主工作树是接手、本机集成与本机测试服务目录，默认只读，不直接编写改动。非 main 分支、干净工作树、自己刚打开会话均不构成独占证明。只有本会话先前创建/获分配的隔离 worktree，或可核实的本任务接续记录，才允许复用；禁止把另一个活动任务的 worktree 当成本任务环境。
3. 没有独占环境时，由 agent 核实 GitHub 实时 `main` 和本机 `origin/main`，再核对本机 `main` 是否一致；用户未要求远端写入不影响这项只读基线核验。从选定且已记录的最新基线创建新的 `codex/<任务简名>` 分支与独立目录；远端不可核实时不得以旧本机提交继续开工。目录选择当前权限允许的位置；必要时申请执行权限。Git linked worktree 的分支引用通常写入主仓库的公共 `.git`，因此“工作树可写”不等于“可创建分支”：创建前必须验证公共 Git 元数据可写。若创建失败是该权限边界，立即用受控授权重试分支创建并回读 `git branch --show-current`；不得在 detached worktree 中修改代码，也不能退回共享目录写入。
4. 进入新目录后重复核对真实路径、分支、HEAD 和状态。若应用提供的是 detached HEAD worktree，修改前先为其建立任务分支。随后向用户给出一条开工回执：`工作树：绝对路径；分支：名称；基线：SHA；归属：本任务新建/有证据复用`。
5. 之后所有编辑、依赖安装、构建、测试和 Git 命令都使用该绝对工作目录；不能仅改变一个 shell 的当前目录就假定其他工具也已切换。应用会话显示的目录可能仍是源项目，实际工具路径与 Git 核验结果才是证据。
6. 提交前再次核对路径、分支和差异归属，只提交本任务内容。收尾列出实际提交 SHA，不能只说“规则已写入”就算完成持久化。

### 本机环境准备与受限执行

- 新 worktree 不携带 `node_modules`。缺少开发工具时由 agent 在本任务目录执行
  `npm ci --include=dev --no-audit --no-fund`，然后重跑 `npm run repo:doctor -- --local`。
  不用临时 `npx` 下载替代锁定依赖，也不共享其他任务可变的 `node_modules`。
- `EPERM`、`EACCES`、DNS 失败及受限环境中的 keyring 读取失败，先按环境权限故障处理。
  用执行工具的受控提升权限重跑原操作；不要直接递归 `chmod` 公共 `.git`，也不要把
  受限环境中的 `gh auth status` 失败直接认定为令牌失效或反复要求用户登录。
- Git 操作需写公共 `.git` 和本工作树 Git 管理目录。工作树文件可写不代表 Git 元数据可写；
  `--local` 对两个目录进行实际写入探测。检查不会自动修改权限或分支。
- 获授权的提升权限仍不可用时，在允许的路径新建独占完整克隆作为隔离替代，并建立
  `codex/<任务>` 分支；核对基线，仅迁移本任务已确认差异。原工作树保留，不能删除或覆盖
  已有目录、未知改动、凭据或数据。此替代适用本节隔离门槛，不自动授予远端操作权限。
- 用户已授权的依赖准备、Git 提交、网络同步由 agent 执行。只有主机环境重试也证明
  凭据失效，或自动审批明确拒绝必要操作且无可行替代时，才向用户报告具体剩余动作。

示例命令由 agent 替换任务名、路径和核实后的基线后执行，用户无需输入：

```sh
git -C /Users/zhouding/ss worktree add -b codex/task-name /Users/zhouding/ss-worktrees/task-name main
git -C /Users/zhouding/ss-worktrees/task-name rev-parse --show-toplevel
git -C /Users/zhouding/ss-worktrees/task-name branch --show-current
git -C /Users/zhouding/ss-worktrees/task-name rev-parse HEAD
git -C /Users/zhouding/ss-worktrees/task-name status --short
```

这是可审查的 agent 执行门槛，不是应用自动创建 worktree 的设置或禁止写入的系统沙箱。只有提交并集成这些指令后，新基线才会携带规则；旧任务和旧 worktree 必须单独读取更新后的入口，不能假定会自动刷新。

- 开工先核对 HEAD、分支、工作树改动和已有 worktree。当前已是本任务独占的 worktree/分支时直接复用；否则从核实过的基线自动建立独立 worktree 与 `codex/<任务简名>` 分支，重名时添加后缀。公共 Git 元数据写入被拒绝时，自动申请一次受控授权重试；仍失败则停止写入并转交协调器预创建，不能以 detached HEAD 继续。后续命令明确指定该 worktree，避免在多个会话共用的目录切分支。
- 依赖安装使用任务级 npm 缓存：`NPM_CONFIG_CACHE=/private/tmp/quiet-room-npm-cache-<任务> npm ci --ignore-scripts`。共享缓存出现 root-owned/EPERM 时切换到任务级缓存，安装后先检查 `node_modules/.bin/tsc` 和 `node_modules/.bin/vitest`，再运行构建与测试；必要时仅对安装命令申请一次受控授权。
- 本机开发基线必须采用已核实的 GitHub 实时 `main`；本机 `main` 仅是可回读的本地快照。每次写入任务先核对 `origin/main` 与 GitHub `main`，记录三者 SHA；网络无法核实必须说明远端状态未知并停止依赖旧基线的开工。依赖尚未集成的任务时记录依赖提交，从该提交开始；不能自行混入其他开发分支。
- 未提交改动先确认归属；不得自动 stash、reset、移动、暂存或提交其他任务的修改。已有本任务改动可按精确差异迁移到隔离目录并核对，来源归属不明时只隔离处理明确属于本任务的部分。
- 开发任务完成风险匹配验证后提交本任务分支，按持续远端交付授权推送并进入集成 PR 或 Merge Queue，等待 CI 后合并，再返回分支、基线 SHA、提交 SHA、验证证据、未验证项与依赖。暂存只使用明确路径或可核对的差异块，不使用无差别 `git add .`。纯问答不创建分支或提交。
- 单纯提出问题不默认授权合并到 main 或生产发布。用户说“把这些任务一起合并”“统一集成”或“合并发布”时，agent 自行发现对应任务的实际分支/提交并核对，不要求用户手工抄 SHA 或执行 Git 命令。只有任务归属确实无法确定时才询问缺失信息；不能仅凭相似分支名纳入代码。
- 集成使用独立 worktree 和集成分支，固定参与任务及其精确提交，按依赖顺序整合。选择合并或 cherry-pick 前检查提交祖先和已有补丁，避免重复引入；不修改开发会话的工作树。技术冲突在已有契约内解决；产品或安全语义冲突不得凭猜测裁决。
- GitHub 同步阶段验证最终组合后，按已有仓库规则创建 PR、核对 CI 并完成已授权合并；检查、保护规则或权限不满足时明确报告阻塞，不绕过。多个开发任务各自通过的测试不能替代最终组合验证。
- 合并和生产发布是不同动作。生产继续遵守 RELEASING.md：已有发布授权和精确目标提交确认后才能使用固定入口；agent 展示目标及证据供确认，用户不必自行查找提交号。发布成功后独立回读并对账。仅合并任务不会自动部署。
- 发布成功后按下节清理本批资源，无需用户再次提出清理要求；不满足条件的资源保留并记录原因。

以上是 agent 的操作规则，不是已经运行的后台调度器。不会自动创建其他会话或更改模型；并行仍须遵守任务授权与共享文件依赖约束。未合并的本页不会自动出现在从 main 新建的 worktree；过渡期间可读取源目录本页执行，不得为了携带规则复制整个脏工作树。

### 本机工作树与依赖准备

2026-09-10 用户确认固定本机工作树位置并复用依赖缓存。以下是 agent 执行规则，不能修改应用沙箱权限，也不承诺所有后续阶段无阻塞。

- 本机默认从已打开的 `/Users/zhouding/ss` 只读接手，由 agent 在同层级的 `/Users/zhouding/ss-worktrees/<任务名>` 显式创建任务树；集成与文档对账树也使用该根目录。其他电脑先核实当地仓库路径，再采用同层级专用根目录，不照抄本机绝对路径。不自动搬迁、清理已有工作树。
- 已获分配且核实属于本任务的应用工作树或旧任务树直接复用，不重复隔离；detached HEAD 先创建任务分支。共享目录的干净状态或分支名不能证明归属。
- 创建前核对 Git common dir（`git rev-parse --path-format=absolute --git-common-dir`）、目标路径和当前会话可写范围。操作会写目标目录及共享 Git 元数据；`ls`、`test -w` 或终端成功不证明沙箱内可写。已知超出权限时，对精确创建命令申请必要执行权限；权限失败后使用工具的正式审批机制，不以改目录、chmod、完全磁盘访问权限或修改 AGENTS 冒充解决。审批拒绝时报告动作、原因与尚缺权限，不在共享目录修改。
- 创建部分失败时先检查目标分支和 worktree 注册状态，再续跑；不盲删锁文件、分支或未知目录。成功后核验真实路径、任务分支、基线和干净状态，后续各工具显式使用该绝对目录。一次命令获批不表示后续编辑、安装或 Git 写入永久获批。
- 代码任务检查项目 Node 版本要求、npm、`package.json` 与 `package-lock.json`。全新树执行 `npm ci --prefer-offline`，每树保留独立 `node_modules`；使用 `npm config get cache` 核实并复用既有用户缓存，不为每任务重设全局配置、不共享软链接 `node_modules`、不擅自迁移 pnpm。缓存减少重复下载，仍有解包及安装脚本开销。
- 同一独占树已有成功安装记录，且 package/lockfile、Node/npm 与安装选项未变、依赖完整时复用；缺少记录、依赖损坏或上述条件变化时重新安装。`npm ci` 会重建该树依赖，不在运行中的共享环境执行。缓存、下载、安装脚本或浏览器运行遇沙箱限制时申请对应最小权限；记录失败阶段，只重跑受阻步骤。
- 环境就绪检查按任务选择必要工具与定向命令；纯文档任务可直接运行文档检查，无需安装产品依赖或跑全量测试。产品基线测试失败先分类，修复既有失败的任务可保留复现证据继续实现；不能将失败标记通过。实现后的风险匹配门禁仍必须完成。

### 验证与远端交付的阻塞处理

进入阶段前执行 `npm run env:check -- docs|test|browser|delivery`（选择一个模式）；入口只检查本机工具和依赖可用性，不联网、不安装、不读取部署凭据。失败先补对应缺项，成功不代替权限审批、浏览器实际启动、认证或发布预检。

浏览器 CI 使用独立 runner 并发：第 1 组一个任务，第 2 组通过 `--shard 1/4` 至 `--shard 4/4` 分成四个任务；所有分片成功才允许完整验证汇总通过。分片可用 `--list` 核对覆盖，失败可在独占环境重跑精确分片。本机同一 checkout 内仍顺序运行浏览器入口；本机并行分片必须使用同一候选的不同独占环境，不能在共享缓存和固定端口尚未隔离时直接后台启动四条命令。分片降低关键路径但增加 runner 数和准备开销，实际加速取决于配额和耗时分布。

单元测试沿用 Vitest 文件并发；CI 的构建、浏览器、通话和审计已有独立 job，可同时运行。集成同一目标分支、同一树的安装与构建、生产切换保持单写者；生产回读必须在切换成功后，对账必须在回读后。可提前运行独立的本地检查或获授权的 CI，不能打乱这些依赖关系。

提速顺序：开发中定向反馈，冻结候选后一次风险匹配门禁；本任务已有推送授权时 PR CI 可与本地验证并行，合并仍等待所有必需检查。复用缓存与同一树安装结果，CI 等待沿用已有 run；用已有发布阶段计时区分安装、测试、外部等待与切换，不凭总会话时长判断瓶颈。浏览器入口可能共享端口和 Vite 缓存，未经隔离和耗时验证不得直接并发；PR 与 main CI 的既有门禁不因提速跳过。

固定目录只解决位置一致性。每个阶段仍需核实自身前提，不能把 worktree 创建成功当成测试、合并或发布可用证明：

| 阶段 | 前提与续跑方式 |
| --- | --- |
| 本地验证 | 当前任务依赖及所需浏览器可用，端口、进程清理、缓存和产物目录权限满足；环境失败申请相应权限后重跑受阻项，产品失败按既有分类修复 |
| 多任务集成 | 核实源提交、授权和独占集成树；合入后重新判断锁文件与依赖，冻结组合候选后运行风险匹配验证，不能复用各分支测试充当组合结果 |
| 提交与同步 | Git 元数据写入仍可能需执行审批；远端同步另需当前任务授权、网络与现有认证。不要把沙箱网络错误直接判成密钥失效 |
| PR 与合并 | 当前任务推送/合并授权、精确 head CI 与分支保护满足；缺权限或 CI 失败按实际原因处理，已合并则续等对应 main CI，不重复合并 |
| 发布与回读 | 沿用 RELEASING.md 的精确 SHA 授权、固定入口、独立发布环境和回读证据位置；普通开发树准备成功不代替发布预检。切换结果不明先回读，已有成功发布只续跑回读或对账 |

沿用当前任务已有授权，不反复询问同一业务授权；系统执行审批与业务授权是两层条件。执行审批不能授予推送、合并或生产发布权。前置条件仍不满足时列明已完成阶段、实际错误、待执行步骤；不得承诺调整文档即可消除 CI、网络、真实冲突或生产确认。权限模型参考 [OpenAI 官方安全说明](https://developers.openai.com/codex/security/)。

## 本机 main 与局域网验收

家里和公司分别运行独立的本机测试环境。本机 `main` 是当前电脑的局域网验收候选，不等于 `origin/main`，也不等于生产。完整状态分层、集成步骤、进程回读、通行密钥来源要求和跨电脑同步边界见[本机局域网测试与跨电脑同步](../workflows/local-lan-testing.md)。

### 有条件自动集成

2026-09-08 用户明确确认本规则：后续 Quiet Room 实施任务完成本任务提交和风险匹配验证后，
默认由 agent 集成本机 main、按需更新局域网服务并独立回读，无需逐次要求用户重复授权。
本条是用户确认的持续本机授权，优先于旧文档中“每次等待本机集成授权”的描述；
只分析、只出方案、暂不集成等当前任务的明确限制优先。单纯问答不触发集成。

- 只纳入归属明确、已完成且已提交的本任务精确候选；不得自动收拢其他旧分支、半成品或未确认需求。
- 同一电脑同时只有一个集成操作。使用公共 Git 目录下 `quiet-room-local-integration.lock` 目录的原子 `mkdir`
  取得互斥锁，记录当前任务、PID、基线和候选；占用时等待或报告。未知／遗留锁不得直接删除，先核实归属。
  锁覆盖 main 更新、必要重启、服务回读和对账；本任务完成后只释放自己持有的锁。此为 agent 执行规则，不是后台调度器。
- 锁内复核 main 的分支、HEAD、未提交状态与实际服务归属。脏 main 或归属不明改动使集成暂停；
  不自动 stash、reset、覆盖或提交他人内容。锁外完成候选验证时，锁内发现 main 已前进须重新检查组合。
- 有分叉时在独立集成 worktree 合并；语义明确的技术冲突自动解决。产品／安全契约冲突不得猜测，
  保留现场并询问。无文本冲突也要检查依赖、共享生命周期和行为影响，最终组合通过风险匹配验证后才快进 main。
- 记录合入前 main、任务候选、集成提交和验证证据。必要重启只作用于已确认的本机测试进程；
  当前已知正在进行真机验证或通话时先等待合适窗口，不打断活动验证。重启后检查目标代码与后端健康。
- 集成或回读失败只续跑失败阶段；不以强制回退 main、迁移／清空数据或重复生产发布来处理。
  本节仅定义旧本机集成边界；远端操作现按持续远端交付授权，生产发布和测试数据迁移仍不在本节范围内。

1. 所有改动仍在独立任务 worktree 完成、验证和提交；共享本机 `main` 目录不得直接编写代码或文档。
2. 用户已授权本机集成时，先核对共享目录位于 `main`、工作树干净且固定候选 SHA。可快进时使用 `git merge --ff-only`；不能快进时转到独立集成 worktree 解决组合和冲突并复验，禁止覆盖用户改动。
3. 集成后核对本机 `main` 精确 SHA，并确认实际局域网前后端进程从该主工作树运行。前端回读目标代码、后端健康检查成功后，才能记录“本机服务已回读”。是否重启按变更类型与回读结果判断，不能仅因进程存在或页面可打开就写成已更新。
4. 通行密钥真机验收必须使用稳定、设备信任、证书主机名匹配的 HTTPS 来源，并记录设备、系统、浏览器、来源与通过范围。自动测试和服务回读不能替代真机结果。
5. 远端交付执行本页持续授权，生产发布独立确认。每轮分别报告任务候选、本机 main、自动测试、本机服务、真机、GitHub 和生产状态。

## GitHub 远端交付与跨任务集成

完成适用需求／设计确认并获正式实施授权的任务按[持续远端交付授权](#持续远端交付授权)默认进入本节，通过集成 PR 或 Merge Queue 与 CI 合并远端 main；禁止直接推送 main。生产发布独立确认。只分析、只出方案、不提交或不推送等明确限制优先。

Merge Queue 由 GitHub 的分支保护配置负责排队和生成临时组合；启用前必须将 `verify` 设为必需检查，并保留 CI 的 `merge_group` 触发。仓库文件只声明检查入口，不代表远端队列已启用；队列未启用时由一个集成会话按本节处理有关联任务。

1. 开发完成后，先在本任务工作树保存归属明确的任务提交，保证工作树干净，记录起始基线和精确候选 SHA；不为等待其他任务而反复改写本任务分支。
2. 无文件、依赖或语义交集的小任务直接推送独立分支并加入 Merge Queue。队列在每个前序合并后重建临时组合并运行必需检查；开发会话不手工循环 rebase。
3. 有交集的并发任务先由一个集成会话固定源 head，在独立集成分支按依赖顺序合并，集中解决冲突并运行组合验证，然后只创建一个集成 PR。产品、安全语义不明确时保留阻塞，不替用户决定。
4. 即使 Git 没有文本冲突，也检查主线对最终组合的依赖、共享文件和验收影响。按照最终组合运行风险匹配验证；修复冲突造成的变化必须重新验证，记录测试对应的候选 SHA 或精确工作树。
5. 交付时再次查询主线引用。如果与冻结基线不同，默认标记“主线已前进，进入下一批”；只有新的变化确实阻断当前候选时才重建一次集成候选、重新冻结并验证。
6. 登记任务、批次、工作树、分支、起始基线、冻结主线 SHA、精确候选 SHA、集成 head、远端回查结果、变更文件、验证证据、未验证项、依赖与冲突提示。分支后续新增提交会使原登记过期，不自动进入已经冻结的批次。

### 共享交接登记

清单工具尚未实现时，由 agent 在公共 Git 目录（通过 git rev-parse --path-format=absolute --git-common-dir 定位）下 quiet-room-deliveries 保存一任务一 JSON。文件名使用任务分支 UTF-8 字节的 SHA-256 摘要，先写唯一临时文件再原子替换；同一任务只能由其负责会话更新。记录固定字段：version=1、task、batchId、branch、worktree、startBase、frozenMain、syncedMain、head、integrationHead、remoteHead、changedFiles、verification、unverified、dependencies、status、updatedAt。未知值明确标记，不填推测的通过结论。

status 使用 ready、needs_recheck、push_failed 或 blocked；ready 仅表示开发交接就绪，不代表真机通过或授权发布。登记为 ready 前必须核对远端 head 与候选一致。会话报告同样的摘要，记录中不得包含凭据、消息、原始日志或恢复材料。登记不产生产品提交，本机记录不会自动同步到其他电脑。该约定由 agent 执行，不是已运行的自动清单服务。

### 集成会话的职责

- 同批由一个集成会话负责。读取共享交接记录并重新核对 Git 与任务归属，不能盲信 JSON 的就绪状态。先固定参与任务和精确提交，后续新增提交进入下一批；确需补入阻断修复时重新冻结并验证。
- 与 main 的冲突尽量在开发交付前处理；尚未合并任务彼此间的冲突在集成分支按依赖顺序处理。不能为了提前消除冲突把其他任务的半成品擅自合入开发分支。
- 无文本冲突不等于行为兼容，仍检查共享状态、生命周期和安全契约，并验证最终组合。默认只为本批最终组合建立集成 PR；确需单独审查的任务另行安排。现有 CI 触发及精确 main 门禁不因该约定而跳过。
- 共享记录不含排他执行锁，尚不支持多个集成会话同时自动领取任务；不能把清单目录当作已实现的并发队列。远端 Merge Queue 的排队和临时组合由 GitHub 执行，相关任务的集成 PR 仍由一个集成会话负责。持续后台集成与自动模型路由均未实现。

## 发布后资源清理

用户已授权将本批开发工作树和任务分支清理纳入发布收尾。由 agent 在独立生产回读与知识库对账完成后执行，不要求用户手动操作或重复授权；它不是发布脚本中的后台删除程序。只合并未发布的任务默认保留，除非用户明确要求提前清理。

固定机械核验入口为 `npm run tasks:cleanup -- --plan /绝对路径/plan.json`，默认只检查；agent 审阅结果后对同一计划添加 `--apply`。计划由 agent 根据已有交接与发布证据生成，用户不必填写。具体格式、信任边界、保留原因和续跑见[清理脚本说明](../workflows/task-cleanup.md)。脚本严格保留任何忽略文件和无法证明完整集成的 squash 改动；不能为通过检查把未经核实的确认项填成 true。

顺序为：`精确发布回执 → 独立回读成功 → 知识库对账 → 逐项核验 → 清理符合条件的本批资源 → 报告`。清理失败不改变已验证的生产结论，不回滚、不重发，只处理清理步骤。

### 必须同时满足的条件

- 范围已知：来自本批任务交接或创建记录的精确工作树路径、分支名和末端 SHA。不能按日期、名称前缀或“看起来旧”批量认领。共享主目录、main、其他任务资源及服务器发布目录、备份、部署配置不在本规则范围内。
- 成果已集成：核对最新远端 main 与对应已合并 PR，确认本批代码已进入发布版本。普通合并可检查提交祖先；squash/cherry-pick 必须核对 PR 原始 head、合并提交及完整改动映射，不能把“无祖先关系”直接认定为未合并或可丢弃。分支合并后新增的提交必须另行核实，有未集成内容就保留。
- 文件可丢弃：检查已暂存、未暂存和未跟踪内容；同时列出被忽略的文件路径，避免忽略的配置、附件、笔记或证据被工作树移除。已知可再生成的依赖/构建缓存不阻塞清理；未知内容、用户材料或未保存证据均保留。检查路径与状态即可，不读取凭据内容，不使用 git clean 清空现场。
- 证据已保存：分支与提交、PR、发布/回读及必要诊断记录已保存到不会随该工作树删除的持久位置。尤其检查位于该 worktree 专属 Git 管理目录内的回执，移除工作树会一并移除它们；只保存必要的脱敏证据，不复制部署凭据。未合并的知识库文档分支不清理。
- 无使用者：确认没有活动会话、构建、测试、开发服务器或后续任务依赖该工作树。无法确认就保留。当前执行会话实际依赖的工作树不得自行移除；在独立的集成/收尾工作树处理其他已结束任务，当前工作树标记为待后续清理，不为删掉自己创建无限收尾工作树。

### 执行与失败处理

1. 优先使用固定脚本完成以下机械检查与操作，不临时重写删除命令。执行前重新核对路径、分支末端 SHA、状态与工作树使用情况；发现变化就跳过该项。若工作树被锁定，保留并记录原因，不自动解锁。
2. 先用 `git worktree remove <精确路径>` 正常移除工作树，不使用 --force 或 rm -rf；再清理该任务分支。普通祖先已合并且满足上述条件时使用 `git branch -d <精确分支>`。
3. squash/cherry-pick 导致正常分支删除被拒绝时，不改用无条件强制删除。仅在改动已完整集成且末端 SHA 未变化的证据充分时，使用带旧 SHA 校验的 `git update-ref --no-deref -d refs/heads/<精确分支> <已核对SHA>` 删除引用；证据不足就保留。不得仅凭 -d 是否成功判断成果是否已集成。
4. 本批远端任务分支也可清理：须核对对应 PR 已合并、远端末端与已集成的任务 head 精确一致，且没有其他 PR 或任务依赖。删除请求必须以该末端 SHA 为条件（显式 force-with-lease 的删除校验），远端变化即停止；不得无条件删除或强推共享分支。已被平台删除则记录已不存在。
5. 删除后分别回查工作树登记、本地引用和远端引用，只报告实际确认的结果。网络或权限失败时保留待清理状态；已经完成的部分不重做，不扩大删除范围。

收尾用短表记录：`资源路径/分支 | 已清理/已不存在/保留/失败 | 原因或证据`。无需复制完整日志或每次新增审计文件，复用任务交付或已有发布记录。真机尚未验收不单独阻止已满足上述条件的清理；后续同问题反馈从最新核实基线新建修复分支，不能把清理成功写成真机验收通过。

## 无进展时如何处理

- 同一假设连续两次被证据否定，或主动诊断约 15–20 分钟没有新增证据：重新列出已排除原因、缺失证据和下一项可区分假设的实验。计时是检查点，不是放弃任务的期限；CI 等待不算主动诊断。
- 产品失败：修实现；夹具失败：修测试可移植性且保留产品断言；网络、下载、排队失败：处理基础设施并有限重试。没有完成分类前不同时改产品和断言。
- 根因推理明显超出当前配置时建议升级；缺少真机、权限或运行日志时优先补证据。不得声称已经切换模型，除非有设置成功的依据。
- 已通过检查只有因新变更、新失败或未解决风险才重跑。重跑说明触发原因和影响范围；最终候选仍须满足现有完整门禁。
- CI 等待和发布尽量交给现有固定命令，输出保留摘要与日志位置，错误时读取相关片段。发布切换结果不明时先独立回读，不盲目重发。

## 最小任务卡

澄清阶段可在会话中填写；进入设计／实施后按[需求记录模板](../requirements/TEMPLATE.md)保存一项需求一份最小脱敏记录，不为每轮聊天或同需求内的小改动新增文件。本卡补充技术执行信息，不替代用户确认。

```text
需求编号 / 当前阶段 / 记录路径：
方案与原型、流程版本 / 确认依据 / 授权范围：
目标与故障域：
基线 HEAD / 工作树归属：
验收：最多三项可观察结果
模型 / 强度：实际可核实值，否则未知
相关入口：文件与必要契约
验证：定向命令 + 最终门禁 + 真机要求
当前证据 / 尚缺证据：
下一步：一个具体动作
```

用户发起常规修复只需提供问题与期望，录屏可选；固定流程和默认环境无需重复：

```text
问题：……
期望结果：……
```

交接时补充改动文件、已运行验证及其基线、失败分类、未验证项和下一步。不复制完整聊天或原始日志。后续独立任务只有在当前上下文已混杂或需要隔离执行时才另开，并沿用已授权目标。

## 前 5–10 个任务的试运行记录

每个任务交付时在回复中附一条短记录，优先复用 PR 或现有交付记录。用户统一收集对照；不自动创建监控、定时任务或第三方分析。

```text
类型 | 实际模型/强度 | 主动处理耗时 | 外部等待耗时 | token及统计口径 | 验收证据 | 额外全量次数/原因 | 返工情况
```

无法可靠取得的指标写“未知”。token 应注明是否包含缓存输入、推理和重试；账户额度百分比不是单任务 token，任务跨夜时长不是主动工作时长。后续反馈出现问题才更新返工情况，不在刚交付时推断未来没有返工。

按同类任务比较总成本、验收结果与返工，样本少时只作趋势判断。先消除无证据重试与重复验证，再决定是否降低模型或强度。没有实测前不承诺节省比例。

## 后续工程改进

以下记录本轮工程化状态；本页仍没有实现自动模型切换：

- 工作树已将独立生产回读收敛为 `npm run deploy:readback -- --sha <精确SHA>`，以版本化、可复用且不触发切换的入口替代临时命令；合并、CI 和真实生产使用仍需分别取证。
- 回读入口已记录固定阶段耗时、稳定失败分类和 `.git` 内脱敏结构化证据；失败续跑只允许对同一 SHA 重新回读，不能自动重发。发布入口自身的既有预检、CI、隔离克隆及服务器阶段计时保持不变。
- 隔离文档 PR 对账器仍未实现；后续明确授权实现时应单独建立运维任务，先阅读相关安全与发布文档及测试。
